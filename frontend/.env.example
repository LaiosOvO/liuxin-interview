# 前端构建期注入的 NEXT_PUBLIC_* 变量。
# build 时这些会被替换成字面量，运行时无法再改（output: 'export' 模式约束）。

# 后端 API 基址：开发环境写完整 URL；生产 nginx 同源时留空
NEXT_PUBLIC_API_URL=http://localhost:8000

# 当前运行模式：demo / prod（PITFALLS #10 防演示模式上线）
# footer 会显式显示，演示模式邮件全部路由到 DEMO_INBOX
NEXT_PUBLIC_APP_MODE=demo
