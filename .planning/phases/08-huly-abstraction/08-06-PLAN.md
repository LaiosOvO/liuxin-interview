---
phase: 08-huly-abstraction
plan: 06
type: execute
wave: 2
depends_on:
  - "08-05"
files_modified:
  - scripts/seed_huly_users.py
  - scripts/seed_huly_workspace.py
  - backend/sidecars/huly-bridge/src/admin.ts
  - backend/sidecars/huly-bridge/src/index.ts
  - .env.example
  - README.md
  - frontend/tests/e2e/huly_im_flow.spec.ts
  - docs/screenshots/.gitkeep
  - docs/e2e-test-report-phase-8-huly-2026-05-17.md
  - backend/tests/integration/test_huly_seed.py
autonomous: false
requirements:
  - HULY-08
  - HULY-09

must_haves:
  truths:
    - "scripts/seed_huly_users.py 可执行，读取业务 DB 13 个 users → 调 sidecar admin API 在 Huly 批量注册账号 + 加入 workspace laios"
    - "用 admin email/password token + signUpJoin 路径，避开 createInviteLink autoJoin Forbidden (Pitfall #1, 已源码定位 verifyAllowedServices)"
    - "脚本幂等：跑 2 次 0 错误（已存在的 account log 'skipped'，不重复 signUp）"
    - "sidecar 新增 src/admin.ts — 暴露 POST /api/admin/signup_join (内部专用，BRIDGE_TOKEN + 额外 ADMIN_TOKEN 双重保护)"
    - ".env.example 加完整 HULY_* 段：HULY_VERSION / HULY_URL / HULY_ACCOUNTS_URL / HULY_WORKSPACE / HULY_SERVER_SECRET / HULY_BRIDGE_URL / HULY_BRIDGE_TOKEN / HULY_BOT_ACCOUNT_UUID / HULY_ADMIN_EMAIL / HULY_ADMIN_PASSWORD / HULY_ADMIN_TOKEN / HULY_DEFAULT_PASSWORD"
    - "README §1 加 Huly 部署 runbook（前置部署 huly-stack → 设置 HULY_ADMIN_* → 跑 seed → IM_PROVIDER=huly 重启）"
    - "E2E 用 browser-harness (Playwright via webapp-testing skill) - it.charlie 在 Huly DM 对 bot 发 我要离职 → 真起流程 → 浏览器看到 Huly UI 消息回复 → 业务表 flow_instances 真有新行"
    - "E2E 报告含至少 5 张截图：Huly 登录 / DM 列表 / 输入 我要离职 / bot 回复 / 后端日志 + flow_instances 行"
  artifacts:
    - path: "scripts/seed_huly_users.py"
      provides: "13 用户批量在 Huly 注册（幂等）"
      min_lines: 100
      contains: "signUpJoin"
    - path: "backend/sidecars/huly-bridge/src/admin.ts"
      provides: "sidecar admin API (signUp 路径，BRIDGE_TOKEN + ADMIN_TOKEN 双保护)"
      min_lines: 80
    - path: "frontend/tests/e2e/huly_im_flow.spec.ts"
      provides: "Playwright E2E (Huly UI 起流程)"
      min_lines: 80
      contains: "我要离职"
    - path: "docs/e2e-test-report-phase-8-huly-2026-05-17.md"
      provides: "E2E 报告 + 5+ 截图"
      min_lines: 60
  key_links:
    - from: "scripts/seed_huly_users.py"
      to: "backend/sidecars/huly-bridge/src/admin.ts"
      via: "httpx POST /api/admin/signup_join with X-Bridge-Token + X-Admin-Token"
      pattern: "/api/admin/signup_join"
    - from: "backend/sidecars/huly-bridge/src/admin.ts"
      to: "@hcengineering/account-client"
      via: "getAccountClient(adminToken).signUpJoin(email, password, first, last, inviteId, workspace)"
      pattern: "signUpJoin|createInvite"
    - from: "frontend/tests/e2e/huly_im_flow.spec.ts"
      to: "backend/src/offboarding_flow/api/internal_huly.py"
      via: "Playwright 操作 Huly UI → sidecar listener poll → POST /api/internal/huly/event → dispatch → flow_instances"
      pattern: "page\\.fill|page\\.click|expect.*flow_instances"
---

<objective>
完成 8B Huly 接入的"身份对齐 + E2E 验收"：
- HULY-08：写 seed 脚本把业务 DB 13 个 users 一次性在 Huly 注册账号 + 加入 workspace `laios`，
  避开 Pitfall #1（createInviteLink autoJoin Forbidden，已源码定位）。
- HULY-09：补 .env.example 完整 HULY_* 段 + README §1 加 Huly 部署 runbook。
- E2E：用 browser-harness（webapp-testing skill = Playwright）跑 it.charlie 在 Huly DM 对
  bot 发"我要离职" → 真起流程 + 截图 + 验证 flow_instances 表新行。

Purpose: 这是 Phase 8B 的"用户验收"门 — 完成此 plan 后 Goal "it.charlie 在 Huly UI 起流程"
真正达成。E2E 必须用 browser-harness（CLAUDE.md §2.2 硬性要求），不允许 curl + 假设浏览器行
为。

Output:
- `scripts/seed_huly_users.py` 幂等脚本 + `scripts/seed_huly_workspace.py`（workspace 预建）
- sidecar 新增 admin API（signUp 走 admin token 路径，避开 service token Forbidden）
- `.env.example` 完整 HULY_* 段 + README Huly 部署 runbook
- `frontend/tests/e2e/huly_im_flow.spec.ts` Playwright E2E
- `docs/e2e-test-report-phase-8-huly-2026-05-17.md` E2E 报告 + 5+ 截图
- 集成测试覆盖 seed 幂等性
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-huly-abstraction/08-CONTEXT.md
@.planning/phases/08-huly-abstraction/08-RESEARCH.md
@.planning/phases/08-huly-abstraction/08-05-SUMMARY.md
@.planning/phases/08-huly-abstraction/08-04-SUMMARY.md

# 关键源码
@backend/sidecars/huly-bridge/src/auth.ts
@backend/sidecars/huly-bridge/src/index.ts
@backend/sidecars/huly-bridge/src/middleware.ts
@backend/src/offboarding_flow/state_store/models.py
@backend/src/offboarding_flow/cli/seed_demo.py
@.env.example
@README.md

<interfaces>
<!-- 关键已有契约（必须遵循） -->

From backend/src/offboarding_flow/state_store/models.py (User 表):
```python
class User(Base):
    __tablename__ = "users"
    __table_args__ = {"schema": "app"}
    username: Mapped[str]   # 主键
    email: Mapped[str]      # 业务 DB 邮箱（可能 @company.com）
    full_name: Mapped[str]
    role: Mapped[str]       # applicant / manager / hr / hr_admin / admin / it_admin / finance / security / receptionist
    manager_email: Mapped[str | None]
    department: Mapped[str | None]
```

From backend/src/offboarding_flow/cli/seed_demo.py (已存在的 seed 模式):
```python
# 现有 seed 用 SQLAlchemy 直接 INSERT；本 plan 的 seed_huly_users 类似入口（python -m offboarding_flow.cli.seed_huly_users）
# 复用 settings + new_session
```

From @hcengineering/account-client (Huly SDK):
```typescript
interface AccountClient {
  login(email: string, password: string): Promise<LoginInfo>
  signUp(email: string, password: string, first: string, last: string): Promise<SignUpInfo>
  signUpJoin(email: string, password: string, first: string, last: string,
              inviteId: string, workspace: string): Promise<SignUpInfo>
  selectWorkspace(workspaceUrl: string): Promise<WorkspaceLoginInfo>
  createInvite(exp: number, emailMask: string, limit: number, role: string): Promise<{id: string}>
  // createInviteLink(autoJoin: true) → Forbidden (Pitfall #1)
  // 用 createInvite(无 autoJoin) → 拿到 inviteId → 再 signUpJoin
}

import { getAccountClient } from '@hcengineering/server-client'
const accountClient = getAccountClient(adminToken)  // adminToken 是真人 admin login 后拿的
```

C-1 约束（README §3）：
- username 是跨系统主键
- Huly email 用 `{username}@demo.local`（用 @demo.local 而不是真实 @company.com 是为了演示模式不污染真实邮箱域）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sidecar admin API + 配置补全 + seed_huly_workspace 预建 workspace</name>
  <files>
    backend/sidecars/huly-bridge/src/admin.ts
    backend/sidecars/huly-bridge/src/middleware.ts
    backend/sidecars/huly-bridge/src/index.ts
    backend/sidecars/huly-bridge/tests/admin.test.ts
    scripts/seed_huly_workspace.py
    .env.example
  </files>
  <action>
    **Reference-first（强制）**：动手前读：
    - `~/ai/ref/agent/platform/server/account/src/operations.ts`（verifyAllowedServices 源码 — 已在 RESEARCH §Pitfall #1 截图）
    - `~/ai/ref/agent/platform/foundations/core/packages/account-client/src/client.ts`（signUpJoin 完整签名）
    - 更新 reading doc `docs/reading-huly-platform-2026-05-17.md`（追加 "seed 路径 admin token + signUpJoin" 段）

    1. `src/middleware.ts` 扩展（在 Plan 05 已有 bridgeAuth 基础上加）：
       - export `adminAuth(adminToken: string): RequestHandler`
       - 校验 `req.header('X-Admin-Token') === adminToken`，否则 403 `{error: 'admin_auth_failed'}`
       - 注释说明：这是 sidecar 内部 admin 路径的**第二道**保护（第一道仍是 BRIDGE_TOKEN），防 LLM 通过普通 BRIDGE_TOKEN 调 signUp 创建账号

    2. `src/admin.ts` — sidecar admin API：
       - export `function mountAdminRoutes(app: Express, config: BridgeConfig)`
       - 内部 helper：`async function getAdminClient(): Promise<AccountClient>` — 用 HULY_ADMIN_EMAIL/PASSWORD 调 `accountClient.login()` 拿真人 admin token，再 `getAccountClient(adminToken)` 返回 admin 权限的 client（**缓存 token**，避免每次重新 login）
       - `POST /api/admin/signup_join` — 走 bridgeAuth + adminAuth 双 middleware
         body: `{username, email, password, first_name, last_name, role?}`
         1. 检查 username 在 Huly 是否已存在：用 admin client 查 `SocialIdentity { value: email }`
            - 已存在 → 返回 `{ok: true, skipped: true, account_uuid: existing.account}`（幂等）
         2. 调 `accountClient.signUp(email, password, first_name, last_name)` 拿 sign up token → 拿 newAccountUuid
            - 如果 signUp 返回的 token 已有 workspace 权限，直接 200
         3. 调 `accountClient.createInvite(exp=now+86400, emailMask=email, limit=1, role=role||'USER')` 拿 inviteId
            - **注意：** 不传 autoJoin（Pitfall #1，verifyAllowedServices 会 Forbidden）
         4. 用 newAccount token 调 `signUpJoin(email, password, first, last, inviteId, HULY_WORKSPACE)` 把新账号加入 workspace
         5. 返回 `{ok: true, account_uuid: newAccountUuid, skipped: false}`
       - 错误处理：catch PlatformError → 400 + 原始 errorCode 字段（不暴露 stack）

    3. `src/index.ts` 在 Plan 05 基础上加：
       - import `mountAdminRoutes`
       - `mountAdminRoutes(app, config)` — 注意 admin routes 走 `app.use('/api/admin', bridgeAuth(token), adminAuth(adminToken), adminRouter)`
       - log 启动期"已 mount admin API（双重鉴权 BRIDGE + ADMIN）"

    4. `tests/admin.test.ts`（vitest）：
       - 测试 1：缺 X-Admin-Token → 403
       - 测试 2：缺 X-Bridge-Token → 401（即使 X-Admin-Token 对）
       - 测试 3：account 已存在 → 返回 `{skipped: true}`，signUp 不被调用
       - 测试 4：account 不存在 → signUp + createInvite + signUpJoin 三步顺序调用
       - 测试 5：signUp 抛 PlatformError(AccountAlreadyExists) → 转为 200 skipped（兜底幂等）
       - 测试 6：createInvite + signUpJoin 失败 → 400 + 透明错误

    5. `scripts/seed_huly_workspace.py` — 一次性脚本预建 workspace `laios`：
       - 入口 `python -m offboarding_flow.cli.seed_huly_workspace`
       - 读 `HULY_ADMIN_EMAIL/PASSWORD/HULY_WORKSPACE` env
       - 调 sidecar `POST /api/admin/create_workspace` body=`{name: HULY_WORKSPACE}`（注：本 plan 不实现该 endpoint；如果 Huly v0.7 已有 workspace 则脚本仅 log + exit 0；新建 workspace 走 sidecar admin route，由 admin client 调 createWorkspace API）
       - **简化（v1）**：如果 HULY_WORKSPACE 已存在（手动在 Huly UI 预建过 + admin 邀请过 bot），脚本仅打印 "workspace 已存在，跳过"，不强制实现 createWorkspace（避免范围爆炸）
       - 失败时友好提示"请按 deploy/huly/README.md 手动在 Huly UI 创建 workspace 'laios' 并邀请 admin"
       - 退出码 0 if ok else 1

    6. `.env.example` 加完整 HULY_* 段（在合适位置追加，参考已有 `# === Huly (Phase 8B) ===` 占位 — 如无则新建）：
       ```
       # === Huly Platform (Phase 8B - 可选) ===
       # 仅在 IM_PROVIDER=huly 或 DOC_PROVIDER=huly 时需要
       HULY_VERSION=v0.7.423
       HULY_URL=http://192.168.2.44:8087
       HULY_ACCOUNTS_URL=http://192.168.2.44:8087/_accounts
       HULY_WORKSPACE=laios
       HULY_SERVER_SECRET=changeme_must_match_huly_stack_secret
       HULY_BRIDGE_URL=http://huly-bridge:7777
       HULY_BRIDGE_TOKEN=changeme_bridge_token_32_chars
       HULY_BOT_ACCOUNT_UUID=
       # === Huly admin (仅 seed 时用; seed 完可清空) ===
       HULY_ADMIN_EMAIL=admin@huly.local
       HULY_ADMIN_PASSWORD=changeme_admin_password
       HULY_ADMIN_TOKEN=changeme_admin_token_for_sidecar_api
       HULY_DEFAULT_PASSWORD=changeme_seeded_user_default_password
       ```
       - gitleaks 必须把这些识别为 placeholder 不拦截（changeme_* 前缀已是约定）
  </action>
  <verify>
    <automated>cd backend/sidecars/huly-bridge &amp;&amp; npx vitest run tests/admin.test.ts --reporter=verbose &amp;&amp; npx tsc --noEmit &amp;&amp; grep -c "HULY_" /Users/admin/ai/resume/interview/liuxin/hr/.env.example</automated>
  </verify>
  <done>
    - admin.ts + middleware adminAuth 实现，6 vitest 用例 PASS
    - `tsc --noEmit` 0 error
    - .env.example 含 12 个 HULY_* 变量
    - seed_huly_workspace.py 可执行（在 workspace 已存在场景 exit 0）
    - reading doc 已更新追加 admin token + signUpJoin 段
    - pre-commit gitleaks 不拦截 .env.example
  </done>
</task>

<task type="auto">
  <name>Task 2: seed_huly_users.py + 集成测试覆盖幂等 + README Huly 部署 runbook</name>
  <files>
    scripts/seed_huly_users.py
    backend/tests/integration/test_huly_seed.py
    README.md
  </files>
  <action>
    1. `scripts/seed_huly_users.py`：
       ```
       入口：python -m offboarding_flow.cli.seed_huly_users [--dry-run]
       ```
       - 读 settings（HULY_BRIDGE_URL / HULY_BRIDGE_TOKEN / HULY_ADMIN_TOKEN / HULY_DEFAULT_PASSWORD）
       - 用 `new_session()` 查 `select(User)` 拿 13 个用户
       - 对每个 user：
         - email = `f"{user.username}@demo.local"` （C-1 约束）
         - first_name = user.full_name.split(" ", 1)[0] if " " in user.full_name else user.username
         - last_name = user.full_name.split(" ", 1)[1] if " " in user.full_name else ""
         - password = HULY_DEFAULT_PASSWORD
         - role = "USER"（所有 seed 用户都是 workspace USER；admin/bot 用 service token 不走 signUp）
         - 调 sidecar `POST /api/admin/signup_join` with `X-Bridge-Token` + `X-Admin-Token`
         - 处理 response：
           - 200 + skipped=true → log "[seed_huly_users] {username} 已存在，跳过"
           - 200 + skipped=false → log "[seed_huly_users] {username} 新建 account_uuid={uuid}"
           - 4xx → log "[seed_huly_users] {username} 失败：{error}" + 累加 error_count
         - 5xx → 抛异常（sidecar / Huly 挂了，停止 seed）
       - `--dry-run` 模式：仅打印每个 user 将做什么，不发请求
       - 结尾 summary：`seeded={n}, skipped={m}, failed={k}`
       - exit code: 0 if failed==0 else 1
       - 中文 log，结构化 JSON 可选（用 logging.basicConfig + format）

    2. 集成测试 `backend/tests/integration/test_huly_seed.py`（5 用例）：
       - 用 respx mock sidecar `/api/admin/signup_join`（注：seed 脚本走 HTTP 不直接调 Huly，所以可 mock sidecar）
       - 测试 1：13 个 user 都成功 → exit 0 + 输出含 "seeded=13"
       - 测试 2：13 个 user 都 skipped（mock 返回 skipped=true）→ exit 0 + "skipped=13"
       - 测试 3：3 个 failed → exit 1 + "failed=3"
       - 测试 4：sidecar 5xx → 异常 + exit 非 0（停止后续 seed）
       - 测试 5：--dry-run → 不发 HTTP（respx 0 调用）+ exit 0 + 打印每个 user
       - **遵循 CLAUDE.md §2.3 集成测试禁止 mock DB**：用真 PostgreSQL fixture + seed 13 user 数据（复用现有 seed_demo helper）

    3. `README.md` §1 加 Huly 部署 runbook（在合适位置，建议在 "## 部署" 章节下加 §1.X Huly 部署）：
       ```markdown
       ### Huly 集成部署（Phase 8B，可选）

       离职流程支持把 IM/Doc 通道切换到 Huly Platform。以下是完整部署 runbook：

       #### 前置条件
       - 192.168.2.44 已部署 Huly v0.7.423（参考 deploy/huly/README.md）
       - Huly workspace `laios` 已通过 Huly UI 手动创建
       - 已有 Huly admin 账号（email/password）

       #### 步骤 1 — 配置 .env
       复制 .env.example 中的 HULY_* 段到 .env，填入：
       - HULY_SERVER_SECRET（必须与 huly-stack 容器的 SERVER_SECRET 一致）
       - HULY_BRIDGE_TOKEN（32 字符随机 secret，sidecar ↔ backend）
       - HULY_ADMIN_EMAIL / HULY_ADMIN_PASSWORD（手动创建的 admin 凭证）
       - HULY_ADMIN_TOKEN（32 字符随机 secret，仅给 seed 脚本用）
       - HULY_DEFAULT_PASSWORD（13 个 seed 用户的默认密码）

       #### 步骤 2 — 启动 huly-bridge sidecar
       ```bash
       docker compose --profile huly up -d huly-bridge
       # 等 30 秒 healthcheck 转绿
       curl http://localhost:7777/healthz  # 应返回 {ok: true, huly_connected: true}
       ```

       #### 步骤 3 — Seed 13 个用户到 Huly
       ```bash
       # 先 dry-run 预览
       docker exec offboarding-backend uv run python -m offboarding_flow.cli.seed_huly_users --dry-run
       # 真 seed
       docker exec offboarding-backend uv run python -m offboarding_flow.cli.seed_huly_users
       # 期望输出：seeded=13, skipped=0, failed=0
       ```

       #### 步骤 4 — 切换业务 backend 到 Huly
       修改 .env：
       ```
       IM_PROVIDER=huly
       DOC_PROVIDER=huly
       ```
       重启 backend：
       ```bash
       docker compose restart backend
       ```
       backend 日志应显示 `[huly_listener] 启动（webhook 模式）`

       #### 步骤 5 — 验证
       打开 Huly UI（http://192.168.2.44:8087），用 it.charlie 登录（默认密码 = HULY_DEFAULT_PASSWORD）→
       在与 offboarding-bot 的 DM 输入"我要离职" → bot 应回复"流程已启动..."

       #### 回滚
       ```
       IM_PROVIDER=mattermost
       DOC_PROVIDER=outline
       ```
       重启 backend 即可。**13 个 Huly account 不会被自动删除**（手动在 Huly admin 删除）。
       ```
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/integration/test_huly_seed.py -x --tb=short -v &amp;&amp; uv run python -m offboarding_flow.cli.seed_huly_users --dry-run 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    - seed_huly_users.py 可执行（--dry-run 不抛异常）
    - 5 集成测试 PASS
    - README §Huly 集成部署 段已写
    - 脚本幂等：同环境跑 2 次第二次全 skipped
    - 中文 log / 中文 README
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E browser-harness (Playwright) — it.charlie 在 Huly UI 起流程 + 截图报告</name>
  <what-built>
    - `frontend/tests/e2e/huly_im_flow.spec.ts` — Playwright spec 模拟 it.charlie 登录 Huly UI → 找 bot DM → 发"我要离职" → 等回复 → assertion
    - `docs/e2e-test-report-phase-8-huly-2026-05-17.md` — E2E 报告含 5+ 截图
    - 截图存到 `docs/screenshots/huly-flow-{step}.png`
  </what-built>
  <how-to-verify>
    **前置（必须在 192.168.2.44 有完整 huly-stack + huly-bridge + backend 都跑起来）：**
    - `docker compose up -d` 启动现有 5 个 service
    - `docker compose --profile huly-stack up -d` 启动 Huly 14 容器
    - `docker compose --profile huly up -d huly-bridge` 启动 sidecar
    - `docker exec offboarding-backend uv run python -m offboarding_flow.cli.seed_demo`（建 13 业务 user）
    - `docker exec offboarding-backend uv run python -m offboarding_flow.cli.seed_huly_users`（13 user 同步到 Huly）
    - `.env` 切 `IM_PROVIDER=huly DOC_PROVIDER=huly` + `docker compose restart backend`

    **必须用 webapp-testing skill (Playwright) 写 E2E**（CLAUDE.md §2.2 硬性要求）：

    1. 写 `frontend/tests/e2e/huly_im_flow.spec.ts`：
       ```typescript
       import { test, expect } from '@playwright/test'

       test('it.charlie 在 Huly DM 发"我要离职" → 流程启动 → bot 回复', async ({ page }) => {
         // Step 1: 打开 Huly 登录页
         await page.goto(process.env.HULY_URL || 'http://192.168.2.44:8087')
         await page.screenshot({ path: 'docs/screenshots/huly-flow-1-login.png', fullPage: true })

         // Step 2: 用 it.charlie 登录
         await page.fill('[name="email"]', 'it.charlie@demo.local')
         await page.fill('[name="password"]', process.env.HULY_DEFAULT_PASSWORD!)
         await page.click('button[type="submit"]')
         await page.waitForSelector('[data-testid="chunter-channels"]', { timeout: 15000 })
         await page.screenshot({ path: 'docs/screenshots/huly-flow-2-loggedin.png', fullPage: true })

         // Step 3: 找到 bot DM（selector 在 Huly UI 探查后填）
         await page.click('text=offboarding-bot')   // 注：selector 需在真机探查后调整
         await page.waitForSelector('[data-testid="chat-input"]')
         await page.screenshot({ path: 'docs/screenshots/huly-flow-3-dm-opened.png', fullPage: true })

         // Step 4: 发"我要离职"
         await page.fill('[data-testid="chat-input"]', '我要离职')
         await page.keyboard.press('Enter')

         // Step 5: 等 bot 回复（最多 10s — sidecar 2s poll + dispatch + post）
         const reply = page.locator('text=/流程已启动|您的离职流程已创建/i')
         await expect(reply).toBeVisible({ timeout: 15000 })
         await page.screenshot({ path: 'docs/screenshots/huly-flow-4-bot-reply.png', fullPage: true })

         // Step 6: 验证业务 DB（通过 backend HTTP API，因为 Playwright 不直连 DB）
         const flowResp = await page.request.get('http://192.168.2.44:8000/api/flows?employee_id=it.charlie')
         expect(flowResp.status()).toBe(200)
         const flows = await flowResp.json()
         expect(flows.length).toBeGreaterThanOrEqual(1)
         expect(flows[0].status).toBe('in_progress')
       })
       ```
       - selector 可能需在 Huly UI 真机探查（用 `npx playwright codegen`）— 提交前必须人工跑通

    2. 跑 E2E：
       ```bash
       cd frontend
       HULY_URL=http://192.168.2.44:8087 HULY_DEFAULT_PASSWORD=<from .env> \
         npx playwright test tests/e2e/huly_im_flow.spec.ts --headed --reporter=list
       ```
       期望：1 passed + 4 张截图生成到 docs/screenshots/

    3. 补一张 backend 日志截图（terminal 截图，证明 dispatch_message 被调用）：
       - `docker compose logs backend | grep -E "dispatch_message|HulyListener|/api/internal/huly/event"` → 截屏存 `docs/screenshots/huly-flow-5-backend-log.png`

    4. 写 `docs/e2e-test-report-phase-8-huly-2026-05-17.md`：
       - 测试目标 + 前置环境（版本号 + 容器清单）
       - 5 步截图嵌入（用 `![描述](screenshots/huly-flow-N-xxx.png)`）
       - tool 调用链路图（it.charlie 操作 → Huly UI → sidecar 2s poll → fetch backend webhook → BRIDGE_TOKEN 验 → HulyListener.handle_webhook → dispatch_message → BotService.dispatch → start handler → 创建 flow_instances → huly_im_provider.post_to_channel → sidecar /api/im/send_dm → Huly UI 看到回复）
       - 业务 DB 截图：`docker exec offboarding-postgres psql -U offboarding -d offboarding -c "SELECT id, employee_id, status, created_at FROM app.flow_instances WHERE employee_id='it.charlie' ORDER BY created_at DESC LIMIT 3"` → 终端截屏 `huly-flow-6-flow-instance.png`
       - 结论：PASS / FAIL 每步
       - 已知问题（如有）：例如某 selector 在 Huly v0.7.423 与 v0.7.500 不同 → 记录 + 文档化升级流程

    5. （可选 — 加分）回切验证：把 `IM_PROVIDER=mattermost` 重启 → 在 Mattermost 再做同样的"我要离职"流程 → 截图证明回切 0 影响。
  </how-to-verify>
  <resume-signal>
    上传 5+ 截图（4 Huly UI + 1 backend log + 1 DB query）+ E2E 报告 md + Playwright spec 文件，确认全 PASS 后回复 "approved" 或描述失败项
  </resume-signal>
  <files>
    frontend/tests/e2e/huly_im_flow.spec.ts
    docs/screenshots/.gitkeep
    docs/e2e-test-report-phase-8-huly-2026-05-17.md
  </files>
  <action>
    详见 <how-to-verify>。执行人需要按 5 个子步顺序操作：
    1. 准备前置环境（启容器 + seed_demo + seed_huly_users + IM_PROVIDER=huly 重启 backend）
    2. 写 Playwright spec `frontend/tests/e2e/huly_im_flow.spec.ts`（含 6 步：登录 → 找 DM → 发消息 → 等回复 → 截图 → 查业务 DB）
    3. 跑 E2E 并生成 4 张 Huly UI 截图
    4. 截 backend log 截图 + 业务 DB 查询截图
    5. 写 E2E 报告 docs/e2e-test-report-phase-8-huly-2026-05-17.md（含 5+ 截图嵌入 + 链路图）
    （可选）回切 IM_PROVIDER=mattermost 验证 0 影响
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; HULY_URL=http://192.168.2.44:8087 HULY_DEFAULT_PASSWORD=$HULY_DEFAULT_PASSWORD npx playwright test tests/e2e/huly_im_flow.spec.ts --reporter=list 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - frontend/tests/e2e/huly_im_flow.spec.ts 写完且本机 npx playwright test 通过
    - 5+ 张截图存到 docs/screenshots/huly-flow-*.png
    - docs/e2e-test-report-phase-8-huly-2026-05-17.md 完成（含截图嵌入 + tool 调用链路图 + PASS/FAIL 结论）
    - it.charlie 在 Huly UI 真起流程（app.flow_instances 真有新行 + bot 真回复）
    - 人工 checkpoint：用户回复 approved
  </done>
</task>

</tasks>

<verification>
端到端验证（Plan 06 整体）：

1. sidecar admin API：`cd backend/sidecars/huly-bridge && npx vitest run tests/admin.test.ts` — 6 PASS
2. seed 集成测试：`cd backend && uv run pytest tests/integration/test_huly_seed.py -v` — 5 PASS
3. seed 脚本幂等性：跑 2 次 `python -m offboarding_flow.cli.seed_huly_users` 第二次必须 0 错误 + 全 skipped
4. .env.example：`grep -c "HULY_" .env.example` ≥ 12
5. README 含 Huly 部署段：`grep -c "Huly" README.md` ≥ 5
6. E2E（Task 3 checkpoint）：Playwright spec + 5+ 截图 + 报告齐全；it.charlie 真起流程

must_haves 反向校验：
- Truth 1：`grep -n "signUpJoin\|signUp" scripts/seed_huly_users.py backend/sidecars/huly-bridge/src/admin.ts | wc -l` ≥ 3
- Truth 2：admin.ts 用 `createInvite`（无 autoJoin）+ `signUpJoin`；`grep -n "autoJoin" backend/sidecars/huly-bridge/src/` 应只在注释中出现（说明为什么避开）
- Truth 3：集成测试 2「13 都 skipped」PASS
- Truth 4：`grep -n "adminAuth\|X-Admin-Token" backend/sidecars/huly-bridge/src/admin.ts backend/sidecars/huly-bridge/src/middleware.ts` 命中
- Truth 5：`.env.example` 12 个 HULY_*
- Truth 6：README 有 Huly 部署段（5+ Huly 关键词）
- Truth 7-8：E2E 报告 + 5+ 截图存在
</verification>

<success_criteria>
- 13 个业务 user 一次性 seed 到 Huly（幂等）
- sidecar admin API 用 admin token + signUpJoin（避开 service token Forbidden）
- .env.example 含完整 HULY_* 段 + README 有部署 runbook
- E2E 用 browser-harness (Playwright) 跑通 it.charlie 在 Huly DM 起流程
- 5+ 截图 + E2E 报告完整
- 11 个新测试（6 vitest + 5 pytest）全 PASS
- 0 回归（IM_PROVIDER=mattermost 默认场景所有现有测试通过）
- 中文 docstring / commit / README
</success_criteria>

<output>
完成后写 `.planning/phases/08-huly-abstraction/08-06-SUMMARY.md` 含：
- seed_huly_users.py 实现要点 + 命令 + 输出示例
- sidecar admin.ts 新增 API 清单 + 双重鉴权（BRIDGE + ADMIN token）说明
- .env.example 12 个 HULY_* 变量清单 + 各自来源（哪个是 huly-stack 同 / 哪个是新生成）
- README §Huly 集成部署 章节 anchor
- E2E 截图清单（5+ 张）+ 报告路径
- 关键决策记录：
  - 为什么不用 createInviteLink autoJoin（Pitfall #1 已源码定位 verifyAllowedServices）
  - 为什么 sidecar admin 路径需要 BRIDGE + ADMIN 双重 token（防止业务侧通用 BRIDGE_TOKEN 被滥用做账号创建）
  - 为什么 seed_huly_workspace 不强制创建 workspace（v1 容忍手动 UI 创建，避免 Huly v0.7 createWorkspace API 不稳）
  - HULY_DEFAULT_PASSWORD 安全提示（演示用，真实场景每个 user 单独密码）
- Phase 8 完成度自检：
  - 8A (ABS-01..05): Plan 01 + 02 完成
  - 8B (HULY-01..09): Plan 03 + 04 + 05 + 06 完成（HULY-09 部分由本 plan + Plan 03 共同完成 .env.example 和 README）
  - 8C (MCP-01..06): Plan 07 完成（独立 wave 与 8B 并行）
- 跨 phase 验收提示：
  - 全量 pytest（IM_PROVIDER=mattermost 默认）必须保持 0 回归
  - IM_PROVIDER=huly 启动场景手动 smoke 通过
  - 切换不需要改任何业务代码（NFR-01 达成）
</output>
