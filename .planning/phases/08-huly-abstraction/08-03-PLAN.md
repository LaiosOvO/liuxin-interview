---
phase: 08-huly-abstraction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pull_huly_images.sh
  - docker-compose.yml
  - .env.example
  - deploy/huly/README.md
  - deploy/huly/HULY_IMAGES.md
  - backend/tests/integration/test_huly_compose_profiles.py
autonomous: false
requirements:
  - HULY-01
  - HULY-02

must_haves:
  truths:
    - "scripts/pull_huly_images.sh 可执行，读取 HULY_VERSION env 拉 14 个镜像（9 hardcoreeng + 5 infra）"
    - "docker-compose.yml 含 huly-stack profile（CockroachDB + Redpanda + ES + MinIO + 9 个 hardcoreeng 服务），yml 语法合法"
    - "docker-compose.yml 含 huly profile（仅 huly-bridge service 占位）"
    - "默认 profile 启动（不传 --profile huly-stack）时 0 影响现有 5 个 service"
    - ".env.example 加 HULY_VERSION / HULY_URL / HULY_WORKSPACE / HULY_SERVER_SECRET / HULY_BRIDGE_TOKEN / HULY_ACCOUNTS_URL 6 个占位"
    - "脚本与 compose 在 192.168.2.44 跑过一次 healthcheck 全绿（人工 checkpoint 确认）"
  artifacts:
    - path: "scripts/pull_huly_images.sh"
      provides: "Huly 14 镜像一键拉取"
      contains: "HULY_VERSION"
    - path: "docker-compose.yml"
      provides: "huly-stack + huly profile"
      contains: "profiles:"
    - path: "deploy/huly/HULY_IMAGES.md"
      provides: "14 个镜像清单 + 端口映射 + 资源预估"
      min_lines: 40
  key_links:
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "${HULY_VERSION} / ${HULY_SERVER_SECRET} 等变量引用"
      pattern: "\\$\\{HULY_"
---

<objective>
为 8B Huly 接入准备基础设施：拉齐 14 个 Huly 镜像（9 个 hardcoreeng 业务服务 + 5 个基础设施：
CockroachDB / Redpanda / Elasticsearch / MinIO / Redis）、加 docker-compose `huly-stack` 与
`huly` 两个 profile（默认不启动，按需 `--profile huly-stack` 启动），更新 .env.example 加 6 个
HULY_* 占位变量。

Purpose: 让 Plan 04 起 sidecar 时有 Huly 后端可连；让运维一条命令在 192.168.2.44 跑起 Huly
完整 stack；同时确保现有 5 个 service（postgres/redis/backend/frontend/nginx）默认行为 0 变化。

Output:
- `scripts/pull_huly_images.sh` 一键拉镜像（含进度 + 失败重试）
- `docker-compose.yml` 增量加 14 个 service + 2 个 profile
- `.env.example` 加 6 个 HULY_* 占位
- `deploy/huly/HULY_IMAGES.md` 镜像清单 + 端口映射 + 资源预估文档
- `deploy/huly/README.md` 部署 runbook
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-huly-abstraction/08-CONTEXT.md
@.planning/phases/08-huly-abstraction/08-RESEARCH.md

# 关键源码
@docker-compose.yml
@.env.example

<interfaces>
<!-- 14 个 Huly 镜像清单（综合 RESEARCH §3.1 + huly-selfhost 官方文档） -->

业务服务（9 个 hardcoreeng/*，统一版本 v0.7.423）：
- hardcoreeng/account            — Account 服务，端口 3000，对外暴露
- hardcoreeng/front              — 静态前端 SPA + Wiki UI
- hardcoreeng/transactor         — Tx 协调器（主写入）
- hardcoreeng/collaborator       — 协作编辑（CRDT / Yjs）
- hardcoreeng/workspace          — Workspace 后台 worker
- hardcoreeng/stats              — 指标聚合
- hardcoreeng/rekoni             — 文本提取 / OCR
- hardcoreeng/print              — PDF / 导出渲染
- hardcoreeng/love               — 视频通话信令（v1 可关闭）

基础设施（5 个）：
- cockroachdb/cockroach:v24.x    — Huly 主数据库（PostgreSQL 兼容）
- redpandadata/redpanda:v24.x    — Kafka 兼容消息队列
- elasticsearch:8.12.x           — 搜索引擎
- minio/minio:latest             — S3 兼容对象存储
- redis:7.4                      — 已有（如复用 offboarding-redis 则不重复起；推荐独立 huly-redis 端口 6381）

镜像总下载量：~7-9GB；运行时 RAM ~4-6GB（CONTEXT §部署约束）

端口映射建议（避免与 offboarding 5433/6380/8000/3000/8065 冲突）：
- huly-front       8087:8080
- huly-account     3007:3000
- huly-transactor  3008:3000
- huly-collaborator 3009:3000
- cockroach        26257:26257（DB） + 8888:8080（admin UI）
- elasticsearch    9200:9200
- minio            9091:9000（API） + 9092:9001（console）
- redpanda         9092:9092 + 9644:9644
- huly-redis       6381:6379
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写 pull_huly_images.sh + HULY_IMAGES.md 镜像清单文档</name>
  <files>
    scripts/pull_huly_images.sh
    deploy/huly/HULY_IMAGES.md
    deploy/huly/README.md
  </files>
  <action>
    1. 创建 `scripts/pull_huly_images.sh`（bash + set -euo pipefail）：
       - 顶部 shebang + 中文 header 注释（用途 / 使用方法 / 资源消耗预估）
       - 读 env: `HULY_VERSION=${HULY_VERSION:-v0.7.423}`
       - 数组定义 9 个 hardcoreeng 镜像 + 5 个基础设施
       - for loop 调 `docker pull` — 失败时 retry 3 次（10s/30s/60s 指数退避）
       - 进度输出：`[3/14] pulling hardcoreeng/transactor:v0.7.423 ...`
       - 总计耗时 + 成功 / 失败数量统计
       - 末尾打印 `docker images | grep -E "hardcoreeng|cockroachdb|redpanda|elasticsearch|minio"` 验证
       - chmod +x

    2. 创建 `deploy/huly/HULY_IMAGES.md`（中文文档）：
       - 标题 + 用途说明
       - 表格列出 14 镜像：image / version / 端口 / 用途 / 资源占用（CPU / RAM）
       - "如何拉镜像" 段：`./scripts/pull_huly_images.sh` + 备选 `HULY_VERSION=v0.7.500 ./scripts/pull_huly_images.sh`
       - "如何启动" 段：`docker compose --profile huly-stack up -d`
       - "依赖关系" 段：sidecar 必须等 account/front/transactor 三个 healthy
       - "如何降级 / 升级" 段：所有 9 个 hardcoreeng/* 同一版本号；升级时全量 down → 改 HULY_VERSION → re-pull → up
       - "已知 Pitfall" 段：版本不一致会导致 schema mismatch（参考 RESEARCH §Pitfall 2 SERVER_SECRET）

    3. 创建 `deploy/huly/README.md`（中文 runbook，~20 行）：
       - "Huly 接入快速开始" — 3 步：拉镜像 / 编辑 .env / `docker compose --profile huly-stack up -d`
       - "完整启动检查" — 7 个 healthcheck endpoint（front /healthz、account /api/v1/ping 等）
       - "故障排查" — 镜像拉不到、CockroachDB init 失败、Elasticsearch OOM 三个常见问题及 fix
       - 引用 `HULY_IMAGES.md` 与上游 huly-selfhost 文档链接

    全局规则：bash 脚本用 `set -euo pipefail`；所有 echo 中文；脚本顶部加 dry-run 提示（如 `--dry-run` 仅打印不真拉）。
  </action>
  <verify>
    <automated>bash -n scripts/pull_huly_images.sh &amp;&amp; test -x scripts/pull_huly_images.sh &amp;&amp; test -f deploy/huly/HULY_IMAGES.md &amp;&amp; wc -l deploy/huly/HULY_IMAGES.md | awk '$1 >= 40 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - `scripts/pull_huly_images.sh` 语法合法 + 可执行 + 含 14 镜像清单
    - `HULY_IMAGES.md` ≥ 40 行含端口表
    - `deploy/huly/README.md` 含 3 步快速开始
  </done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml 增量加 huly-stack + huly 两个 profile + .env.example 更新</name>
  <files>
    docker-compose.yml
    .env.example
    backend/tests/integration/test_huly_compose_profiles.py
  </files>
  <action>
    1. **先读** `docker-compose.yml` 当前状态（Read tool），理解现有 5 个 service 结构、network 命名（`offboarding-net`）、volume 命名约定。

    2. 在 `docker-compose.yml` **末尾追加**（不修改任何现有 service）：
       - 14 个 service，全部带 `profiles: ["huly-stack"]`：
         - `huly-cockroach`：cockroachdb/cockroach:v24.1, command `start-single-node --insecure --listen-addr=0.0.0.0:26257 --http-addr=0.0.0.0:8080`, volume `huly-cockroach-data:/cockroach/cockroach-data`, ports 26257:26257 + 8888:8080, healthcheck `cockroach sql --insecure -e "SELECT 1"`
         - `huly-redpanda`：redpandadata/redpanda:v24, ports 9092:9092 + 9644:9644
         - `huly-elastic`：elasticsearch:8.12.0, env `discovery.type=single-node + xpack.security.enabled=false + ES_JAVA_OPTS=-Xms1g -Xmx1g`, ports 9200:9200, volume `huly-elastic-data:/usr/share/elasticsearch/data`
         - `huly-minio`：minio/minio, command `server /data --console-address ":9001"`, env `MINIO_ROOT_USER + MINIO_ROOT_PASSWORD`, ports 9091:9000 + 9092:9001
         - `huly-redis`：redis:7.4, port 6381:6379
         - `huly-account`：hardcoreeng/account:${HULY_VERSION}, env `SERVER_SECRET / ACCOUNTS_URL / DB_URL / TRANSACTOR_URL / FRONT_URL / MINIO_*`, depends_on huly-cockroach (condition: service_healthy) + huly-elastic + huly-minio, ports 3007:3000
         - `huly-transactor`：hardcoreeng/transactor:${HULY_VERSION}, depends_on account, port 3008:3000
         - `huly-collaborator`：hardcoreeng/collaborator:${HULY_VERSION}, depends_on transactor, port 3009:3000
         - `huly-workspace`：hardcoreeng/workspace:${HULY_VERSION}, depends_on transactor
         - `huly-stats`：hardcoreeng/stats:${HULY_VERSION}
         - `huly-rekoni`：hardcoreeng/rekoni:${HULY_VERSION}
         - `huly-print`：hardcoreeng/print:${HULY_VERSION}
         - `huly-love`：hardcoreeng/love:${HULY_VERSION}（v1 可选 — 加注释 "若不需视频通话可注释整段"）
         - `huly-front`：hardcoreeng/front:${HULY_VERSION}, depends_on account + transactor + collaborator, env `ACCOUNTS_URL / COLLABORATOR_URL / UPLOAD_URL`, port 8087:8080, healthcheck `wget -qO- http://localhost:8080/`

       - 加 `huly-bridge` service（profile: ["huly"]，**仅占位**，build 路径指向 `./backend/sidecars/huly-bridge` — Plan 04 才真正实现 Dockerfile + src/）：
         ```yaml
         huly-bridge:
           profiles: ["huly"]
           build:
             context: ./backend/sidecars/huly-bridge
             dockerfile: Dockerfile
           container_name: offboarding-huly-bridge
           environment:
             HULY_URL: ${HULY_URL}
             HULY_ACCOUNTS_URL: ${HULY_ACCOUNTS_URL}
             HULY_WORKSPACE: ${HULY_WORKSPACE}
             SERVER_SECRET: ${HULY_SERVER_SECRET}
             BACKEND_URL: http://backend:8000
             BRIDGE_TOKEN: ${HULY_BRIDGE_TOKEN}
             PORT: "7777"
           ports:
             - "7777:7777"
           restart: unless-stopped
           healthcheck:
             test: ["CMD", "wget", "-qO-", "http://localhost:7777/healthz"]
             interval: 10s
             timeout: 3s
             retries: 5
             start_period: 30s
         ```

       - 末尾 `volumes:` 段加 `huly-cockroach-data` / `huly-elastic-data` / `huly-minio-data`
       - 所有 huly-* service 加到现有 `networks: [offboarding-net]`（如 huly-stack 需要隔离网络则新建 `huly-net` + 跨网，但 v1 用同一网更简单）

    3. 更新 `.env.example`（在末尾追加 "# Huly Phase 8" 段）：
       ```
       # Huly Platform (Phase 8 — opt-in via --profile huly-stack / huly)
       HULY_VERSION=v0.7.423
       HULY_URL=http://192.168.2.44:8087
       HULY_ACCOUNTS_URL=http://192.168.2.44:3007
       HULY_WORKSPACE=laios
       HULY_SERVER_SECRET=changeme_random_64_chars_aligned_with_huly_stack
       HULY_BRIDGE_TOKEN=changeme_random_32_chars_for_backend_to_bridge_auth
       ```
       - 加 `# 注意` 注释：HULY_SERVER_SECRET 必须与 huly-account / huly-transactor 容器 env 完全一致

    4. 创建 `backend/tests/integration/test_huly_compose_profiles.py`（pytest.mark.integration，**不真启动容器**，仅静态校验）：
       - 测试 1：`yaml.safe_load(open("docker-compose.yml"))` 0 error（YAML 合法）
       - 测试 2：services 字段含 14 个 huly-* + 1 个 huly-bridge + 现有 5 个
       - 测试 3：所有 huly-* service profiles 含 "huly-stack"；huly-bridge profiles 含 "huly"
       - 测试 4：所有 ${HULY_*} 变量在 .env.example 中有声明
       - 测试 5：huly-cockroach / huly-front 有 healthcheck 字段
       - 测试 6：volumes 段含 3 个 huly-* volume
       - 测试 7：网络段 huly-* service 都在 offboarding-net

    全局规则：不删 / 不改现有 compose service；所有新加 service 名以 `huly-` 前缀；secrets 走 env 不硬编码。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/integration/test_huly_compose_profiles.py -x -v &amp;&amp; docker compose config --profile huly-stack --quiet 2>&amp;1 | head -5</automated>
  </verify>
  <done>
    - docker-compose.yml YAML 合法 + 含 14 huly-* + 1 huly-bridge service
    - 默认 profile 启动 `docker compose up -d` 仍只启 5 个现有 service（不影响现状）
    - `docker compose --profile huly-stack config` 输出合法
    - `.env.example` 含 6 个 HULY_* 占位
    - 7 个静态校验测试 PASS
    - pre-commit gitleaks 通过（无硬编码 secret）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工在 192.168.2.44 跑一次拉镜像 + huly-stack 启动验证</name>
  <what-built>
    完整 Huly stack docker-compose 编排 + 一键拉镜像脚本 + 6 个 .env 占位变量。
    Plan 04 起 sidecar 时必须有可连的 Huly 后端，所以本 plan 必须在 192.168.2.44 真跑一次。
  </what-built>
  <how-to-verify>
    在 192.168.2.44 宿主机执行（不在 dev 本机）：

    1. **拉镜像**（首次约 15-20 分钟，按网速）：
       ```bash
       cd /path/to/offboarding-flow
       export HULY_VERSION=v0.7.423
       ./scripts/pull_huly_images.sh
       ```
       预期：14 个镜像全部 Status: Downloaded；脚本末尾打印成功统计

    2. **编辑 .env**：
       ```bash
       cp .env.example .env  # 如未 .env
       # 生成 HULY_SERVER_SECRET 与 HULY_BRIDGE_TOKEN（64 / 32 字符）：
       openssl rand -hex 32  # → HULY_SERVER_SECRET
       openssl rand -hex 16  # → HULY_BRIDGE_TOKEN
       # 填入 .env
       ```

    3. **启动 huly-stack**：
       ```bash
       docker compose --profile huly-stack up -d
       # 等 60 秒（cockroach init + ES warm up）
       docker compose --profile huly-stack ps
       ```
       预期：14 个 huly-* container 全部 healthy（healthcheck STATUS = healthy）

    4. **访问 Huly UI**：浏览器打开 http://192.168.2.44:8087 — 应看到 Huly 登录页

    5. **验证现有服务无影响**：
       ```bash
       docker compose ps  # 不带 --profile — 仍应只列 5 个现有 service
       curl http://192.168.2.44/api/health  # backend 仍 200
       ```

    6. **可选**：在 Huly UI 手动注册一个 admin workspace `laios`（首次安装必需，Plan 04 sidecar 启动前必须存在）— 详见 deploy/huly/README.md

    问题排查：
    - 镜像拉不到：尝试 `docker pull` 单镜像验证网络
    - CockroachDB 启动慢：`docker logs huly-cockroach` 看 init log
    - Front 502：可能 transactor 未 ready，等 30s 再试
  </how-to-verify>
  <resume-signal>
    回复以下任一：
    - "approved" — 已 192.168.2.44 验证通过（14 healthy + UI 可访问 + 现有 5 service 0 影响）
    - "issues: {描述}" — 详述哪一步失败 + log 节选
    - "skip-deployment" — 暂不真部署，仅靠静态校验（标 must_haves Truth 6 deferred）
  </resume-signal>
</task>

</tasks>

<verification>
端到端验证（Plan 03 整体）：
1. `bash -n scripts/pull_huly_images.sh` — 0 error
2. `docker compose config --profile huly-stack --quiet` — 0 error
3. `cd backend && uv run pytest tests/integration/test_huly_compose_profiles.py -v` — 7 个测试 PASS
4. `git diff docker-compose.yml` — 仅看到追加，不应有现有 service 修改
5. `gitleaks detect --no-git` — 0 secret 泄露

人工 checkpoint（192.168.2.44）：14 个 huly-* 容器 healthy + UI 可访问 + 现有服务 0 影响。

must_haves 反向校验：
- ✅ Truth 1：脚本存在且可执行
- ✅ Truth 2：YAML 含 huly-stack profile + 14 service
- ✅ Truth 3：huly-bridge 在 huly profile
- ✅ Truth 4：默认启动 5 service 不变
- ✅ Truth 5：.env.example 6 变量
- ✅ Truth 6：人工 checkpoint 确认（或 skip-deployment 标 deferred）
</verification>

<success_criteria>
- 14 个 Huly 镜像可一键拉取
- docker-compose huly-stack profile 完整可启动
- huly-bridge profile 占位（Plan 04 实现）
- .env.example 6 个 HULY_* 占位
- pre-commit gitleaks 通过
- 现有 5 service 默认行为 0 变化
- 人工在 192.168.2.44 跑通一次（或 deferred 至 Plan 06 e2e）
</success_criteria>

<output>
完成后写 `.planning/phases/08-huly-abstraction/08-03-SUMMARY.md` 含：
- 14 镜像清单 + 实际拉取耗时（如已部署）
- docker-compose 增量 diff 摘要
- .env.example 新增变量
- 部署验证截图路径（如 192.168.2.44 已跑通）
- 后续 plan 复用：huly-bridge service 名 + 端口 7777 + BRIDGE_TOKEN 鉴权约定
</output>
