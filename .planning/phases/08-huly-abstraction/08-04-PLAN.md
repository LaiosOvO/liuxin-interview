---
phase: 08-huly-abstraction
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/sidecars/huly-bridge/package.json
  - backend/sidecars/huly-bridge/tsconfig.json
  - backend/sidecars/huly-bridge/.dockerignore
  - backend/sidecars/huly-bridge/Dockerfile
  - backend/sidecars/huly-bridge/src/index.ts
  - backend/sidecars/huly-bridge/src/config.ts
  - backend/sidecars/huly-bridge/src/auth.ts
  - backend/sidecars/huly-bridge/src/types.ts
  - backend/sidecars/huly-bridge/src/middleware.ts
  - backend/sidecars/huly-bridge/tests/auth.test.ts
  - backend/sidecars/huly-bridge/tests/middleware.test.ts
autonomous: false
requirements:
  - HULY-03
  - HULY-04

must_haves:
  truths:
    - "backend/sidecars/huly-bridge/ 是独立 Node + TypeScript 项目，npm install / tsx 可启动"
    - "src/auth.ts 用 generateToken(systemAccountUuid, undefined, {service:'offboarding-bot'}) 模式（已源码验证 RESEARCH §3.3.1）"
    - "src/index.ts 启动 express server + setMetadata SERVER_SECRET + 调 connect(HULY_URL, {token, workspace})"
    - "src/middleware.ts 含 bridgeAuth(BRIDGE_TOKEN) X-Bridge-Token 校验"
    - "GET /healthz 返回 {ok: true, huly_connected: bool}"
    - "Dockerfile 可 build 镜像（tsx runtime，无 tsc 编译步骤）"
    - "在 192.168.2.44 启动后可成功连 Huly v0.7.423 stack（healthz huly_connected=true）"
  artifacts:
    - path: "backend/sidecars/huly-bridge/package.json"
      provides: "Node sidecar 依赖清单（express + @hcengineering/*@0.7.423）"
      contains: "@hcengineering/api-client"
    - path: "backend/sidecars/huly-bridge/src/auth.ts"
      provides: "service token 生成（initAuth + serviceToken 函数）"
      contains: "generateToken"
    - path: "backend/sidecars/huly-bridge/src/index.ts"
      provides: "entrypoint：起 express + 连 Huly + mount routes 占位"
      min_lines: 60
    - path: "backend/sidecars/huly-bridge/Dockerfile"
      provides: "镜像构建（Node 22-alpine + tsx）"
      min_lines: 15
  key_links:
    - from: "backend/sidecars/huly-bridge/src/index.ts"
      to: "backend/sidecars/huly-bridge/src/auth.ts"
      via: "initAuth(SERVER_SECRET, ACCOUNTS_URL) + serviceToken()"
      pattern: "initAuth|serviceToken"
    - from: "backend/sidecars/huly-bridge/src/index.ts"
      to: "@hcengineering/api-client"
      via: "import { connect } 用 sysToken 连 workspace"
      pattern: "connect\\(.*workspace"
---

<objective>
建立 Node sidecar 基础骨架：TypeScript + Express 项目结构 + service token 鉴权 + HTTP middleware
（BRIDGE_TOKEN 校验）+ /healthz endpoint + Dockerfile 容器化。Plan 05 在此基础上实现 IM / Doc
路由 + 反向 listener。

Purpose: Python backend 不能直接用 Huly TS SDK，必须经 Node sidecar 转一层（RESEARCH §Pattern 1）。
本 plan **只搭骨架不实现业务路由**，使 Plan 05 能并行（路由实现）+ Plan 06 能并行（Python provider）。

Output:
- `backend/sidecars/huly-bridge/` 完整 Node 项目
- service token 模式实现（已源码验证）
- BRIDGE_TOKEN HTTP middleware
- /healthz 反映 Huly 连接状态
- Dockerfile 可 build + 容器内启动
- vitest 单测覆盖 auth + middleware
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-huly-abstraction/08-CONTEXT.md
@.planning/phases/08-huly-abstraction/08-RESEARCH.md

# 参考实现（已源码验证）
# - hcengineering/platform:services/telegram-bot/pod-telegram-bot/src/utils.ts (serviceToken)
# - hcengineering/platform:services/telegram-bot/pod-telegram-bot/src/start.ts (setMetadata)
# - hcengineering/platform:foundations/core/packages/token/src/token.ts (generateToken 签名)

<interfaces>
<!-- 关键 Huly SDK 接口（v0.7.423）— sidecar 必须使用的契约 -->

From @hcengineering/server-token:
```typescript
import { generateToken } from '@hcengineering/server-token'
// generateToken(accountUuid: AccountUuid, workspaceUuid?: WorkspaceUuid, extra?: ServiceExtra): string
// extra: { service: string; admin?: 'true' }
```

From @hcengineering/core:
```typescript
import { systemAccountUuid, type AccountUuid } from '@hcengineering/core'
// systemAccountUuid: AccountUuid — 系统账号常量；service token 用它
```

From @hcengineering/platform:
```typescript
import { setMetadata } from '@hcengineering/platform'
import serverToken from '@hcengineering/server-token'
// setMetadata(serverToken.metadata.Secret, SERVER_SECRET) — 必须先于 generateToken 调用
// setMetadata(serverToken.metadata.Service, 'offboarding-bot')
```

From @hcengineering/api-client:
```typescript
import { connect, type PlatformClient } from '@hcengineering/api-client'
// connect(url: string, options: { token: string; workspace: string }): Promise<PlatformClient>
// PlatformClient 含 findAll / createDoc / addCollection / updateDoc / removeDoc
```

From @hcengineering/server-client:
```typescript
import serverClient from '@hcengineering/server-client'
// setMetadata(serverClient.metadata.Endpoint, accountsUrl)
// setMetadata(serverClient.metadata.UserAgent, 'offboarding-bot/1.0')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Node 项目 + 依赖清单 + Dockerfile + tsconfig</name>
  <files>
    backend/sidecars/huly-bridge/package.json
    backend/sidecars/huly-bridge/tsconfig.json
    backend/sidecars/huly-bridge/.dockerignore
    backend/sidecars/huly-bridge/Dockerfile
    backend/sidecars/huly-bridge/.gitignore
    backend/sidecars/huly-bridge/README.md
  </files>
  <action>
    1. 创建目录结构：`backend/sidecars/huly-bridge/{src,tests}/`

    2. `package.json`：
       - `name: "offboarding-huly-bridge"`, `version: "0.1.0"`, `type: "module"`
       - scripts:
         - `"dev": "tsx watch src/index.ts"`
         - `"start": "tsx src/index.ts"`
         - `"test": "vitest run"`
         - `"test:watch": "vitest"`
         - `"lint": "eslint src/ tests/ --ext .ts"`
       - dependencies：
         - `"express": "^4.21.2"`
         - `"@hcengineering/api-client": "0.7.423"`
         - `"@hcengineering/server-token": "0.7.423"`
         - `"@hcengineering/server-client": "0.7.423"`
         - `"@hcengineering/chunter": "0.7.423"`
         - `"@hcengineering/contact": "0.7.423"`
         - `"@hcengineering/document": "0.7.423"`
         - `"@hcengineering/core": "0.7.423"`
         - `"@hcengineering/platform": "0.7.423"`
       - devDependencies：
         - `"typescript": "^5.6.0"`
         - `"tsx": "^4.19.0"`
         - `"vitest": "^2.1.0"`
         - `"@types/express": "^4.17.21"`
         - `"@types/node": "^22.5.0"`
         - `"eslint": "^9.0.0"`
       - **关键注释**（package.json 不支持注释，放 README.md）：如 npm 公网拉不到 @hcengineering/*@0.7.423，按 RESEARCH §3.3.7 兜底路径：用 `pnpm` + 从 huly-selfhost Dockerfile 提 tarball

    3. `tsconfig.json`：
       - `target: "ES2022"`, `module: "ESNext"`, `moduleResolution: "Bundler"`, `strict: true`, `esModuleInterop: true`, `skipLibCheck: true`, `allowSyntheticDefaultImports: true`, `resolveJsonModule: true`
       - `include: ["src/**/*", "tests/**/*"]`

    4. `Dockerfile`：
       ```dockerfile
       # 基于 node:22-alpine（已部署 Huly 同栈）
       FROM node:22-alpine AS base
       WORKDIR /app

       # 仅 copy package.json 利用 layer cache
       COPY package.json package-lock.json* ./
       RUN npm ci --omit=dev || npm install --omit=dev

       # copy src
       COPY src/ ./src/
       COPY tsconfig.json ./

       # 暴露端口
       EXPOSE 7777

       # 健康检查（与 docker-compose healthcheck 对齐）
       HEALTHCHECK --interval=10s --timeout=3s --retries=5 --start-period=30s \
           CMD wget -qO- http://localhost:7777/healthz || exit 1

       # 用 tsx 直接运行 TS（无需 build 步骤）
       CMD ["npx", "tsx", "src/index.ts"]
       ```

    5. `.dockerignore`：node_modules, tests/, *.test.ts, .git, .env

    6. `.gitignore`：node_modules/, dist/, *.log, .env

    7. `README.md`（中文 ~30 行）：
       - "Huly Bridge Sidecar" 标题
       - 为什么需要 sidecar（Python 没 Huly SDK；TS SDK 是唯一可靠路径）
       - 启动方式（dev / docker）
       - 依赖说明 + npm 拉不到时的兜底路径（pnpm + tarball）
       - 端口 7777 + endpoint 清单（/healthz + /api/im/* + /api/doc/* — Plan 05 实现）
       - 与 Python backend 之间的 BRIDGE_TOKEN 鉴权说明

    全局规则：所有 TS 注释中文；commit message `feat(huly-bridge): 初始化 Node sidecar 骨架（HULY-03）`。
  </action>
  <verify>
    <automated>cd backend/sidecars/huly-bridge &amp;&amp; test -f package.json &amp;&amp; test -f tsconfig.json &amp;&amp; test -f Dockerfile &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('tsconfig.json','utf8').replace(/\/\/.*$/gm,''))"
