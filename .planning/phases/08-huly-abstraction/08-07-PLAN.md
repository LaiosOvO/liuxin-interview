---
phase: 08-huly-abstraction
plan: 07
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - backend/pyproject.toml
  - backend/src/offboarding_flow/config.py
  - backend/src/offboarding_flow/mcp/__init__.py
  - backend/src/offboarding_flow/mcp/server.py
  - backend/src/offboarding_flow/mcp/auth.py
  - backend/src/offboarding_flow/mcp/tools_read.py
  - backend/src/offboarding_flow/mcp/tools_write.py
  - backend/src/offboarding_flow/mcp/runner.py
  - backend/src/offboarding_flow/mcp/registry.py
  - backend/src/offboarding_flow/main.py
  - backend/tests/unit/mcp/__init__.py
  - backend/tests/unit/mcp/test_auth.py
  - backend/tests/unit/mcp/test_tools_read.py
  - backend/tests/unit/mcp/test_tools_write.py
  - backend/tests/integration/test_mcp_http.py
  - docs/mcp-claude-desktop-config.example.json
  - docs/e2e-test-report-phase-8-mcp-2026-05-17.md
  - README.md
autonomous: false
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
  - MCP-05
  - MCP-06

must_haves:
  truths:
    - "FastMCP 3.3.1+ 已加入 backend/pyproject.toml dependencies"
    - "backend/src/offboarding_flow/mcp/server.py 含 FastMCP 实例 + middleware add 调用 + 7 read tools 全注册"
    - "7 read tools (list_flows / get_flow / get_node_form / get_user_assignments / get_handover_docs / get_final_summary / get_meeting_summary) 全部用 mcp.tool decorator + 中文 docstring + 返回 dict 结构化数据"
    - "4 write tools (advance_node / return_node / reject_node / submit_handover_doc) 仅在 settings.mcp_allow_write=True 时通过 _maybe_register_write_tools 注册（默认 False → 不暴露）"
    - "MagicLinkAuthMiddleware (mcp/auth.py) 复用 auth.jwt_service.decode_token；stdio mode (headers=空) 跳过；http mode 必须 Bearer JWT；解码后 set_state user_sub + user_role"
    - "write tools 内部再做 verify_actor_can_handle(node, sub, role) 闸门（PRD §15.3 红线 + C-5）"
    - "mcp/runner.py 支持 python -m offboarding_flow.mcp.runner stdio|http 双 mode（MCP-05），http 用 transport=streamable-http 不用 deprecated SSE"
    - "main.py 在 settings.mcp_http_mounted=True 时通过 mcp.http_app() mount 到 FastAPI /mcp/* 路径（避免多容器）"
    - "MCP tools 全部只读业务 DB (app.*)，不直接访问 LangGraph checkpoint（C-3 双层状态分离）"
    - "docs/mcp-claude-desktop-config.example.json 包含 stdio mode 完整 uv run 启动模板（MCP-06）"
    - "E2E (browser-harness 截图 Claude Desktop) — 用户问 it.charlie 的流程到哪一步了 → Claude 调 get_flow → 返回 DAG markdown"
  artifacts:
    - path: "backend/src/offboarding_flow/mcp/server.py"
      provides: "FastMCP 实例 + tool 注册入口"
      min_lines: 60
      contains: "FastMCP"
    - path: "backend/src/offboarding_flow/mcp/auth.py"
      provides: "MagicLinkAuthMiddleware (复用 magic-link JWT)"
      min_lines: 50
      contains: "decode_token"
    - path: "backend/src/offboarding_flow/mcp/tools_read.py"
      provides: "7 个 read tools 实现"
      min_lines: 200
    - path: "backend/src/offboarding_flow/mcp/tools_write.py"
      provides: "4 个 write tools (默认禁用)"
      min_lines: 120
    - path: "backend/src/offboarding_flow/mcp/runner.py"
      provides: "stdio / http 双 mode entry"
      min_lines: 25
    - path: "docs/mcp-claude-desktop-config.example.json"
      provides: "Claude Desktop 配置模板"
      contains: "offboarding-flow"
  key_links:
    - from: "backend/src/offboarding_flow/mcp/auth.py"
      to: "backend/src/offboarding_flow/auth/jwt_service.py"
      via: "from offboarding_flow.auth.jwt_service import decode_token"
      pattern: "decode_token"
    - from: "backend/src/offboarding_flow/mcp/tools_read.py"
      to: "backend/src/offboarding_flow/services/flow_service.py"
      via: "复用 list_flows / get_flow / list_nodes"
      pattern: "from offboarding_flow.services"
    - from: "backend/src/offboarding_flow/mcp/tools_write.py"
      to: "backend/src/offboarding_flow/services/node_service.py"
      via: "复用 advance / return / reject (已 upsert 安全)"
      pattern: "node_service\\.(advance|return|reject)"
    - from: "backend/src/offboarding_flow/main.py"
      to: "backend/src/offboarding_flow/mcp/server.py"
      via: "mcp.http_app() mount 到 FastAPI /mcp/*"
      pattern: "mcp\\.http_app|mount.*mcp"
---

<objective>
完成 8C 流程 MCP 化：用 FastMCP 3.x 写一个 MCP server，把"离职流程读 + 受控写"暴露为 MCP
tools，让 LLM 客户端（Claude Desktop / Cursor / 任意 MCP-aware agent）能直接读流程状态 +
（可选）推进节点。**鉴权复用 magic-link JWT**（C-2 不引入第二 token 体系）；**write tools 默认
禁用 + 闸门 verify_actor_can_handle**（C-5 + PRD §15.3 AI 边界红线）。

Purpose: Phase 8C Goal — Claude Desktop 配 MCP 后能 get_flow 返回 DAG markdown；LLM 集成
不再受限于 IM 通道。这是项目"AI 能力开放"层最后一公里，与 8B Huly 解耦可并行（只依赖 8A 抽
象层）。

Output:
- mcp/ 完整模块（server / auth / tools_read / tools_write / runner / registry）
- pyproject.toml 加 fastmcp 依赖
- main.py 加 http mode mount（不增容器）
- Claude Desktop 配置模板 + E2E 截图报告
- 28 个新单测 + 集成测试覆盖 stdio + http 双 mode + 鉴权 + 闸门
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-huly-abstraction/08-CONTEXT.md
@.planning/phases/08-huly-abstraction/08-RESEARCH.md
@.planning/phases/08-huly-abstraction/08-01-SUMMARY.md

# 关键源码（reference-first 必读）
@backend/src/offboarding_flow/auth/jwt_service.py
@backend/src/offboarding_flow/auth/role_router.py
@backend/src/offboarding_flow/services/flow_service.py
@backend/src/offboarding_flow/services/node_service.py
@backend/src/offboarding_flow/services/handover_service.py
@backend/src/offboarding_flow/services/applicant_summary_service.py
@backend/src/offboarding_flow/services/meeting_service.py
@backend/src/offboarding_flow/state_store/models.py
@backend/src/offboarding_flow/main.py
@backend/pyproject.toml

<interfaces>
<!-- 关键已有契约（复用而非新写） -->

From backend/src/offboarding_flow/auth/jwt_service.py (已存在):
```python
def decode_token(token: str) -> JWTPayload:
    """解码 magic-link JWT；失败抛 JWTError。"""

@dataclass(frozen=True)
class JWTPayload:
    sub: str
    role: str
    exp: int
    jti: str
    aud: str | None = None
```

From backend/src/offboarding_flow/services/flow_service.py:
```python
async def list_flows(session, status=None, limit=50) -> list[FlowInstance]: ...
async def get_flow(session, flow_id: UUID) -> FlowInstance | None: ...
async def list_nodes(session, flow_id: UUID) -> list[NodeState]: ...
```

From backend/src/offboarding_flow/services/node_service.py:
```python
async def advance(session, *, flow_id, node_id, result_text, actor, idempotency_key=None) -> NodeState: ...
async def return_to_upstream(session, *, flow_id, node_id, reason, actor) -> NodeState: ...
async def reject(session, *, flow_id, node_id, reason, actor) -> FlowInstance: ...
```

From backend/src/offboarding_flow/auth/role_router.py:
```python
def verify_actor_can_handle(node, actor_sub, actor_role) -> None:
    """非授权抛 PermissionDeniedError；放行 return None。"""
```

From FastMCP 3.3.1+ API (参考 RESEARCH §Code Examples 8C):
```python
from fastmcp import FastMCP, Context
from fastmcp.server.middleware import Middleware, MiddlewareContext
from fastmcp.server.dependencies import get_http_headers
from fastmcp.exceptions import ToolError

mcp = FastMCP("name")
mcp.add_middleware(MyMiddleware())

@mcp.tool
async def my_tool(arg: str, ctx: Context = None) -> dict:
    sub = ctx.get_state("user_sub")
    return {}

# stdio
mcp.run()

# http (Streamable HTTP, 不用 SSE)
mcp.run(transport="streamable-http", host="0.0.0.0", port=7788, path="/mcp", stateless_http=True)

# mount 到 FastAPI (避免多容器)
app.mount("/mcp", mcp.http_app())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastMCP 依赖 + MCP server 骨架 + 7 read tools + 单测</name>
  <files>
    backend/pyproject.toml
    backend/src/offboarding_flow/mcp/__init__.py
    backend/src/offboarding_flow/mcp/server.py
    backend/src/offboarding_flow/mcp/auth.py
    backend/src/offboarding_flow/mcp/tools_read.py
    backend/src/offboarding_flow/mcp/registry.py
    backend/src/offboarding_flow/config.py
    backend/tests/unit/mcp/__init__.py
    backend/tests/unit/mcp/test_auth.py
    backend/tests/unit/mcp/test_tools_read.py
  </files>
  <action>
    **Reference-first（强制）**：动手前读：
    - https://gofastmcp.com/getting-started + https://gofastmcp.com/servers/auth/token-verification
    - https://gofastmcp.com/integrations/claude-desktop
    - RESEARCH §Code Examples 8C + Pitfall #7 / #8 / Pattern 4
    - 写 reading doc 到 `docs/reading-fastmcp-2026-05-17.md` 含：FastMCP 3.x decorator API、
      Middleware on_call_tool 模式、stateless_http=True 时 ctx state 传递、Claude Desktop 配置陷阱

    1. `backend/pyproject.toml` dependencies 加 `"fastmcp>=3.3.1"`；执行 `uv lock` 一次

    2. `backend/src/offboarding_flow/config.py` Settings 扩展：
       - `mcp_allow_write: bool = Field(default=False, env="MCP_ALLOW_WRITE")`
       - `mcp_http_port: int = Field(default=7788, env="MCP_HTTP_PORT")`
       - `mcp_http_mounted: bool = Field(default=False, env="MCP_HTTP_MOUNTED")` — True 时 FastAPI mount /mcp

    3. `mcp/__init__.py`（模块 docstring 说明三件套：server / auth / tools）+ `mcp/registry.py`：
       - `READ_TOOLS: list[str] = ["list_flows", "get_flow", "get_node_form", "get_user_assignments", "get_handover_docs", "get_final_summary", "get_meeting_summary"]`
       - `WRITE_TOOLS: list[str] = ["advance_node", "return_node", "reject_node", "submit_handover_doc"]`
       - 提供给 auth middleware / 测试 / 文档共享

    4. `mcp/auth.py` — MagicLinkAuthMiddleware（参考 RESEARCH Pattern 4）：
       - 三模式行为：
         - stdio mode（headers 为空 dict）→ 信任本地，set_state user_sub=`local-stdio` / user_role=`admin`
         - HTTP mode + 缺 Authorization → 抛 ToolError "缺少 Bearer"
         - HTTP mode + 错 token → 抛 ToolError "无效"
         - HTTP mode + 合法 JWT → decode → ctx.set_state user_sub + user_role
       - 实现 `class MagicLinkAuthMiddleware(Middleware)` + `async def on_call_tool(self, context, call_next)`
       - 用 `get_http_headers()` 取 headers；用 `decode_token(token)` 解码
       - 失败统一抛 `ToolError`，不暴露内部异常细节

    5. `mcp/tools_read.py` — 7 read tools（参考 RESEARCH §Code Examples 8C）：
       - 每个 tool 都用 `@mcp.tool` decorator + 中文 docstring（LLM 看的描述）+ async 函数 + 返回 dict
       - 用 `from offboarding_flow.state_store.session import new_session` 拿 session
       - 复用 `flow_service` / `handover_service` / `applicant_summary_service` / `meeting_service`
       - 严格 7 个 tool 签名：
         - `list_flows(filter='active', limit=50, ctx=None)` → `{flows: [...], total: int}`
         - `get_flow(flow_id, ctx)` → `{flow, nodes, current_node, dag_markdown}`（dag_markdown 用 helper 生成）
         - `get_node_form(flow_id, node_id, ctx)` → `{node, payload, result_text, assignee, role}`
         - `get_user_assignments(username, ctx)` → `{assignments: [{flow_id, node_id, title, urgency}]}`
         - `get_handover_docs(flow_id, ctx)` → `{docs: [{node_id, title, url, submitted_at}]}`
         - `get_final_summary(flow_id, ctx)` → `{summary_markdown, ai_disclaimer}`
         - `get_meeting_summary(meeting_id, ctx)` → `{tasks, blockers, decisions, ai_disclaimer}`
       - **AI disclaimer 强制（PRD §15.3）**：所有含 LLM 生成内容的返回 dict 必须含 `ai_disclaimer: "*由 AI 生成 — 所有建议必须经 HR 人工确认后执行；AI 不会自动操作任何节点*"`
       - 每个 tool 顶部 log `[mcp] tool=xxx sub=xxx role=xxx`（审计 who-queried-what）

    6. `mcp/server.py`（参考 RESEARCH §Code Examples）：
       - 顶部 `from fastmcp import FastMCP`
       - `settings = get_settings(); mcp = FastMCP("offboarding-flow")`
       - `mcp.add_middleware(MagicLinkAuthMiddleware())`
       - 顶层 `from offboarding_flow.mcp import tools_read  # noqa: F401` 触发 @mcp.tool 注册
       - 定义 `_maybe_register_write_tools()`：仅 `settings.mcp_allow_write` 时 `from offboarding_flow.mcp import tools_write` + log warning（Pitfall #8 真禁用而非仅描述）
       - 模块底部调一次 `_maybe_register_write_tools()`

    7. 单测 `tests/unit/mcp/test_auth.py`（5 用例）：
       - stdio mode（mock get_http_headers={}）→ set_state user_sub=local-stdio + call_next 调用
       - http mode + 缺 Authorization → ToolError 含"缺少 Bearer"
       - http mode + 非 Bearer 前缀 → ToolError
       - http mode + 无效 JWT → ToolError 含"无效"
       - http mode + 合法 magic-link JWT → set_state 调用 + payload.sub / role 注入

    8. 单测 `tests/unit/mcp/test_tools_read.py`（8 用例）：
       - list_flows 空 DB → `{flows:[], total:0}`
       - list_flows 3 个 flow → 数量正确
       - get_flow 不存在 → ToolError 含"未找到"
       - get_flow 存在 → 返回 nodes + dag_markdown 非空
       - get_node_form 已知节点 → 返回 payload + assignee
       - get_user_assignments(hr.alice) → 返回该用户分配的所有节点
       - get_final_summary 有 summary → 返回 + 含 ai_disclaimer
       - get_handover_docs 有 docs → 返回 list（含 URL）
       - **遵循 CLAUDE.md §2.3 集成测试禁止 mock DB**：用真 PostgreSQL fixture（已有 db_session + seed helper）

    全局规则：所有 tool docstring 必须中文 + 包含返回结构示例；ctx.get_state 取 user_sub / user_role 用于审计 log。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv add fastmcp &amp;&amp; uv run pytest tests/unit/mcp/test_auth.py tests/unit/mcp/test_tools_read.py -x --tb=short -v 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - pyproject.toml 含 fastmcp>=3.3.1，uv.lock 已更新
    - mcp/ 新模块 5 个文件（init/server/auth/tools_read/registry）实现完毕
    - config.py 含 mcp_allow_write / mcp_http_port / mcp_http_mounted
    - 13 个单测（5 auth + 8 tools_read）全 PASS
    - `ruff check backend/src/offboarding_flow/mcp/` + `mypy backend/src/offboarding_flow/mcp/` 0 error
    - reading doc `docs/reading-fastmcp-2026-05-17.md` 已写
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 write tools (默认禁) + runner (stdio/http 双 mode) + main.py mount + 集成测试</name>
  <files>
    backend/src/offboarding_flow/mcp/tools_write.py
    backend/src/offboarding_flow/mcp/runner.py
    backend/src/offboarding_flow/main.py
    backend/tests/unit/mcp/test_tools_write.py
    backend/tests/integration/test_mcp_http.py
  </files>
  <action>
    1. `mcp/tools_write.py` — 4 write tools（参考 RESEARCH §Code Examples + Pitfall #4 / #8）：
       - 模块顶部 `from offboarding_flow.mcp.server import mcp`
       - 每个 tool 严格三层保护：
         1. middleware 已 set_state user_sub / user_role
         2. tool 内 `node = await flow_service.get_node(s, flow_id, node_id)` + `verify_actor_can_handle(node, sub, role)` 抛 ToolError if PermissionDeniedError（C-5）
         3. 调用 `node_service.advance/return/reject` 走业务 service（已 upsert + 双写）
       - 4 个 tool 签名：
         - `advance_node(flow_id, node_id, result_text, idempotency_key=None, ctx=None)` → `{ok, next_node_id, next_assignee, next_title, ai_disclaimer}`
         - `return_node(flow_id, node_id, reason, idempotency_key=None, ctx=None)` → 回退到上游
         - `reject_node(flow_id, node_id, reason, idempotency_key=None, ctx=None)` → 终止流程
         - `submit_handover_doc(flow_id, node_id, title, markdown, idempotency_key=None, ctx=None)` → 调 handover_service.submit_handover_doc
       - tool docstring 显式标明"写操作 — 会改变流程状态"+"权限：调用者必须是该节点的 assignee（或具备其 role）"+"幂等：建议传 idempotency_key（UUID）防重复"
       - 返回 dict 必须含 `ai_disclaimer: "*由 AI 操作 — 该写入已记录到 action_logs.actor 字段以便审计*"`

    2. `mcp/runner.py`（参考 RESEARCH §Code Examples）：
       - `def run_stdio() -> None`: `from offboarding_flow.mcp.server import mcp; mcp.run()  # 默认 stdio`
       - `def run_http() -> None`: `mcp.run(transport="streamable-http", host="0.0.0.0", port=settings.mcp_http_port, path="/mcp", stateless_http=True)`
         - **关键**：用 `transport="streamable-http"`（MCP 2025-11 spec），不用 deprecated SSE
         - `stateless_http=True`（Pitfall #7：ctx state per-request，同链路调用安全）
       - `if __name__ == "__main__"`: 读 `sys.argv[1]` 选 stdio/http，invalid 输入 exit(2)
       - logging.basicConfig(level=INFO)

    3. `main.py` lifespan / setup 改造（mount FastMCP 到 FastAPI 避免多容器，参考 RESEARCH §Open Q #6）：
       - 在 FastAPI app 实例化后、router include 之后：
         ```python
         if settings.mcp_http_mounted:
             from offboarding_flow.mcp.server import mcp
             # FastMCP 3.x http_app() 返回 starlette 兼容 ASGI sub-app
             app.mount("/mcp", mcp.http_app())
             logger.info("[MCP] 已 mount FastMCP HTTP /mcp/* 到 backend FastAPI")
         ```
       - 默认 `mcp_http_mounted=False`，不影响现有 backend；演示 / 远程 LLM 客户端用时切 True
       - stdio mode 与此无关（Claude Desktop subprocess 启动 runner.py）

    4. 单测 `tests/unit/mcp/test_tools_write.py`（10 用例）：
       - **关键**：write tools 注册依赖 settings.mcp_allow_write=True；测试需 monkeypatch settings + reload server module
       - 测试 1：MCP_ALLOW_WRITE=false → write tools 不在 mcp 实例 tool 列表
       - 测试 2：MCP_ALLOW_WRITE=true → 4 个 write tools 已注册
       - 测试 3：advance_node + 权限不足 → ToolError 含"权限不足"
       - 测试 4：advance_node + 正确权限 → 调 node_service.advance + 返回 next_node_id
       - 测试 5：advance_node + 重复 idempotency_key → 安全（action_logs UNIQUE）
       - 测试 6：return_node + 权限 OK → 调 node_service.return_to_upstream
       - 测试 7：reject_node + 权限 OK → 调 node_service.reject
       - 测试 8：submit_handover_doc + 权限 OK → 调 handover_service
       - 测试 9：write tool 内 sub 取 ctx.get_state('user_sub') 非 None
       - 测试 10：write tool 返回 dict 含 ai_disclaimer

    5. 集成测试 `tests/integration/test_mcp_http.py`（5 用例）：
       - 用 httpx.AsyncClient + asgi-lifespan + MCP_HTTP_MOUNTED=true 启 FastAPI
       - 测试 1：GET /mcp/ → 200（FastMCP 服务可访问）
       - 测试 2：POST /mcp/ (JSON-RPC tools/list) 缺 Authorization → 4xx + body 含 Bearer 提示
       - 测试 3：POST /mcp/ + 错 Bearer → 4xx
       - 测试 4：POST /mcp/ + 合法 magic-link JWT (hr.alice) + tools/list → 7 read tools 返回
       - 测试 5：MCP_ALLOW_WRITE=true + 合法 JWT + tools/call advance_node → 业务 DB 真推进（验证 action_logs 有新行）
       - **遵循 CLAUDE.md §2.3 集成测试禁止 mock DB**：用真 PostgreSQL + seed 数据
       - **遵循 CLAUDE.md §1.3**：测试用 httpx.AsyncClient + asgi-lifespan 而非 TestClient

    6. 全量回归：`cd backend && uv run pytest -x` — Plan 02 / 05 后基线 + 28 新增（13 + 10 + 5），0 回归
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/mcp/test_tools_write.py tests/integration/test_mcp_http.py -x --tb=short -v 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - tools_write.py 含 4 write tool，注册闸门 + verify_actor_can_handle 闸门双保险
    - runner.py 支持 stdio / http 双 mode，http 用 streamable-http
    - main.py mount /mcp/* 路径（mcp_http_mounted=True 时）
    - 15 个新测试（10 单测 + 5 集成）全 PASS
    - MCP_ALLOW_WRITE=false 测试验证 write tools 真不暴露（不只是描述）
    - 全量 pytest 0 回归
    - `ruff check backend/src/offboarding_flow/mcp/tools_write.py backend/src/offboarding_flow/mcp/runner.py` 0 error
    - `mypy backend/src/offboarding_flow/mcp/tools_write.py backend/src/offboarding_flow/mcp/runner.py` 0 error
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Claude Desktop E2E + browser-harness 截图 + README 更新</name>
  <what-built>
    - `docs/mcp-claude-desktop-config.example.json`：Claude Desktop stdio mode 完整配置（uv run 启动 runner.py + JWT_SECRET env + MCP_ALLOW_WRITE=false 默认安全）
    - `docs/e2e-test-report-phase-8-mcp-2026-05-17.md`：E2E 截图报告（含 5 个浏览器/桌面端截图）
    - `README.md` 加 §MCP 集成段：3 个使用场景（Claude Desktop / Cursor / web LLM agent）+ 配置示例 + 安全提示
  </what-built>
  <how-to-verify>
    在本地 Mac 上：

    1. 准备前置：
       - 确认 backend 容器已起：`docker compose up -d backend postgres redis`
       - 跑一遍 seed_demo：`docker exec offboarding-backend uv run python -m offboarding_flow.cli.seed_demo`
       - 确认 `.env` 含 `JWT_SECRET=<...>` + `POSTGRES_*` 配置

    2. 配 Claude Desktop（macOS）：
       - 打开 `~/Library/Application Support/Claude/claude_desktop_config.json`
       - 把 `docs/mcp-claude-desktop-config.example.json` 内容合并进 `mcpServers` 字段
       - 替换 `JWT_SECRET` 为本地 .env 真值；保持 `MCP_ALLOW_WRITE=false`
       - 完全退出 Claude Desktop（Cmd+Q）→ 重启

    3. 在 Claude Desktop 输入框问以下问题（每个问题截图保存）：
       a. "列出当前所有进行中的离职流程" → Claude 应自动调 `list_flows` → 返回 markdown 表格
       b. "it.charlie 的流程到哪一步了？" → Claude 应自动调 `get_flow` → 返回 DAG markdown
       c. "hr.alice 现在有哪些待办？" → Claude 应自动调 `get_user_assignments` → 列待办节点
       d. "看一下流程 {flow_id} 的最终聚合报告" → Claude 应自动调 `get_final_summary` → 返回 markdown + 含 ai_disclaimer
       e. （尝试调用写工具验证默认禁）："帮我推进 it.charlie 的当前节点" → Claude 应回复"我没有这个工具"或"write tools 未启用"

    4. **必须用 webapp-testing skill (browser-harness/Playwright) 截图**（CLAUDE.md §2.2 硬性要求）：
       - 用 Playwright 启 Chromium → 打开 Claude Desktop（或用 macOS Cmd+Shift+5 截图加 Playwright 截 backend log）
       - 替代方案：用 `screencapture` macOS 内置 + 把图存到 `docs/screenshots/mcp-claude-desktop-{a,b,c,d,e}.png`

    5. 写 E2E 报告 `docs/e2e-test-report-phase-8-mcp-2026-05-17.md`：
       - 每个问题：截图 + tool 调用日志（backend MCP middleware 的 audit log）+ 返回结构 + 结论 PASS/FAIL
       - 关键：测试 e（write tools 默认禁）必须证明 Claude 真的没看到这些 tools

    6. 更新 README.md：在合适位置（建议 README §3 协议层抽象 后面）加 §MCP 集成 section：
       - 三个场景：Claude Desktop / Cursor / web LLM agent
       - 三个配置示例（stdio + http + mount-to-fastapi）
       - 安全提示：MCP_ALLOW_WRITE 默认 false 的原因 + 启用时的额外注意事项
       - 链接到 docs/e2e-test-report-phase-8-mcp-2026-05-17.md
  </how-to-verify>
  <resume-signal>
    上传 5 张截图 + E2E 报告文件 + README 更新 commit hash 后，回复 "approved" 或描述任何 fail 项
  </resume-signal>
  <files>
    docs/mcp-claude-desktop-config.example.json
    docs/e2e-test-report-phase-8-mcp-2026-05-17.md
    docs/screenshots/.gitkeep
    README.md
  </files>
  <action>
    详见 <how-to-verify>。执行人需要按 6 个子步顺序操作：
    1. 准备前置（确认 backend 已起 + seed_demo + .env 含 JWT_SECRET）
    2. 配 Claude Desktop ~/Library/Application Support/Claude/claude_desktop_config.json（合并 docs/mcp-claude-desktop-config.example.json 内容）
    3. 在 Claude Desktop 输入 5 个问题（list_flows / get_flow / get_user_assignments / get_final_summary / 写工具默认禁验证）
    4. 用 browser-harness (Playwright) 或 macOS screencapture 截图存到 docs/screenshots/mcp-claude-desktop-{a..e}.png
    5. 写 docs/e2e-test-report-phase-8-mcp-2026-05-17.md（含每个问题截图 + tool 调用日志 + 返回结构 + 结论）
    6. 更新 README §MCP 集成段（3 个场景 + 配置示例 + 安全提示）
  </action>
  <verify>
    <automated>test -f /Users/admin/ai/resume/interview/liuxin/hr/docs/mcp-claude-desktop-config.example.json &amp;&amp; test -f /Users/admin/ai/resume/interview/liuxin/hr/docs/e2e-test-report-phase-8-mcp-2026-05-17.md &amp;&amp; grep -c "MCP" /Users/admin/ai/resume/interview/liuxin/hr/README.md</automated>
  </verify>
  <done>
    - docs/mcp-claude-desktop-config.example.json 含 offboarding-flow mcpServers 完整配置
    - docs/e2e-test-report-phase-8-mcp-2026-05-17.md 完成（含 5 个问题 + 5 张截图 + tool 调用日志 + PASS/FAIL 结论）
    - 5 张截图存到 docs/screenshots/mcp-claude-desktop-*.png
    - README §MCP 集成段已写（含三个场景 / 三个配置 / 安全提示 / 报告链接）
    - 测试 e（写工具默认禁）真证明 Claude 看不到 write tools（不只是描述）
    - 人工 checkpoint：用户回复 approved
  </done>
</task>

</tasks>

<verification>
端到端验证（Plan 07 整体）：

1. 单测：`cd backend && uv run pytest tests/unit/mcp/ -v` — 23 个用例（5+8+10）全 PASS
2. 集成：`cd backend && uv run pytest tests/integration/test_mcp_http.py -v` — 5 用例 PASS
3. 全量回归：`cd backend && uv run pytest -x` — Plan 02 / 05 后基线 + 28 新增，0 回归
4. mypy + ruff：`mcp/` 全模块 0 error
5. stdio smoke：`cd backend && uv run python -m offboarding_flow.mcp.runner stdio` → 启动后用 `npx @modelcontextprotocol/inspector` 连接验证 7 tools 可见
6. http smoke：`MCP_HTTP_MOUNTED=true uv run uvicorn offboarding_flow.main:app --port 8000` → `curl -X POST localhost:8000/mcp/` 返回 401 + Bearer 提示（鉴权生效）
7. E2E（checkpoint Task 3）：Claude Desktop 5 个问题全 PASS + 5 张截图 + 写工具默认禁验证

must_haves 反向校验：
- Truth 1：`grep -n "fastmcp" backend/pyproject.toml` 命中
- Truth 2：`grep -n "FastMCP\|add_middleware" backend/src/offboarding_flow/mcp/server.py` 命中
- Truth 3：`grep -n "@mcp.tool" backend/src/offboarding_flow/mcp/tools_read.py | wc -l` ≥ 7
- Truth 4：`grep -n "mcp_allow_write\|_maybe_register_write_tools" backend/src/offboarding_flow/mcp/server.py` 命中
- Truth 5：`grep -n "decode_token\|get_http_headers" backend/src/offboarding_flow/mcp/auth.py` 命中
- Truth 6：`grep -n "verify_actor_can_handle" backend/src/offboarding_flow/mcp/tools_write.py | wc -l` ≥ 4
- Truth 7：`grep -n "transport=.streamable-http" backend/src/offboarding_flow/mcp/runner.py` 命中
- Truth 8：`grep -n "mcp.http_app\|mount.*mcp" backend/src/offboarding_flow/main.py` 命中
- Truth 9：`grep -rn "checkpoint\|langgraph" backend/src/offboarding_flow/mcp/` 应为空（不依赖 LangGraph 私有数据）
- Truth 10：`docs/mcp-claude-desktop-config.example.json` 存在 + 含 `offboarding-flow` key
- Truth 11：E2E 报告含 5 张截图
</verification>

<success_criteria>
- FastMCP 3.3.1+ 完整 MCP server 实现，7 read tools 默认开 + 4 write tools 默认禁
- Bearer JWT 鉴权复用 magic-link JWT（C-2 满足，无第二 token 体系）
- write tools 三层保护：注册闸门 + middleware 鉴权 + verify_actor_can_handle 角色闸门
- stdio + http (streamable-http) 双 mode 工作；http 通过 FastAPI mount 不增容器
- MCP tools 只读业务 DB，不直接访问 LangGraph checkpoint (C-3)
- Claude Desktop E2E 5 个问题全 PASS（browser-harness 截图）
- README 含 MCP 集成段
- 28 个新测试 + 0 回归
- 所有 docstring / commit message / 报告中文
</success_criteria>

<output>
完成后写 `.planning/phases/08-huly-abstraction/08-07-SUMMARY.md` 含：
- mcp/ 模块 6 个新文件清单 + 关键导出符号 + 11 个 tool 名（7 read + 4 write）
- pyproject.toml diff（fastmcp 新增）
- config.py 新增 3 个 settings 字段
- main.py 改动点（mount 行）
- 鉴权链路图（HTTP request → Authorization Bearer → MagicLinkAuthMiddleware.on_call_tool → decode_token → set_state user_sub/user_role → tool 函数 ctx.get_state → verify_actor_can_handle (write only) → 业务 service）
- E2E 报告路径 + 5 张截图清单
- 关键决策记录：
  - 为什么用 FastMCP 3.x 而不是 mcp 官方 SDK（决策见 RESEARCH §Alternatives）
  - 为什么 mount 到 FastAPI 而不是独立容器（决策见 RESEARCH §Open Q #6）
  - 为什么 stdio mode 跳鉴权（信任本地 subprocess context；适合 Claude Desktop 单机场景）
  - 为什么 write tools 默认禁（PRD §15.3 AI 边界红线 + Pitfall #8）
- 未实现项 / Phase 9 候选：
  - HTTP mode 加 rate-limit（防 LLM 暴调用）
  - 更细粒度审计（per-tool 调用计数 → Grafana）
  - prompts / resources（FastMCP 还支持 `@mcp.prompt` / `@mcp.resource`，本 phase 仅 tools）
- 后续可复用的契约：READ_TOOLS / WRITE_TOOLS 常量在 mcp/registry.py（Plan 08 可加新 tool 时直接 append）
</output>
