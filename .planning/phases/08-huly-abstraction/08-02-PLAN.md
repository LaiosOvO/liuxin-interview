---
phase: 08-huly-abstraction
plan: 02
type: execute
wave: 0
depends_on:
  - "08-01"
files_modified:
  - backend/src/offboarding_flow/providers/base.py
  - backend/src/offboarding_flow/providers/outline_provider.py
  - backend/src/offboarding_flow/providers/lark_provider.py
  - backend/src/offboarding_flow/providers/wecom_provider.py
  - backend/src/offboarding_flow/providers/dingtalk_provider.py
  - backend/src/offboarding_flow/providers/mattermost_provider.py
  - backend/src/offboarding_flow/im/handler_registry.py
  - backend/src/offboarding_flow/services/bot_service.py
  - backend/tests/unit/providers/test_doc_provider_lifecycle.py
  - backend/tests/unit/im/test_handler_registry.py
  - backend/tests/integration/test_bot_service_dispatch_registry.py
autonomous: true
requirements:
  - ABS-03
  - ABS-04
  - ABS-05

must_haves:
  truths:
    - "DocProvider Protocol 含 delete_collection / list_documents_in_collection / delete_document 三个方法"
    - "IMProvider Protocol 含 register_command_listener(handler) 方法"
    - "5 个 doc provider（outline/lark/wecom/dingtalk/mattermost-doc-stub）+ 1 个 im provider（mattermost）全部实现新方法（stub OK）"
    - "HandlerRegistry 类在 backend/src/offboarding_flow/im/handler_registry.py，支持 register/get/all"
    - "BotService.dispatch 内部 11+ if/elif 全部替换为 _REGISTRY.get + spec.handler 调用，行数从 ~45 → ≤ 10"
    - "所有 11 个 bot 命令（help/start/status/report/suggest/list/simulate-timeout/simulate-evidence-missing/users-sync/meeting-ingest/meeting-list）行为不变"
    - "现有所有 bot_service 测试 0 回归"
  artifacts:
    - path: "backend/src/offboarding_flow/providers/base.py"
      provides: "DocProvider + IMProvider Protocol 扩展"
      contains: "async def delete_document"
    - path: "backend/src/offboarding_flow/im/handler_registry.py"
      provides: "HandlerSpec dataclass + HandlerRegistry 类"
      min_lines: 50
    - path: "backend/src/offboarding_flow/services/bot_service.py"
      provides: "BotService.dispatch 重构为 registry-driven"
      contains: "_REGISTRY"
  key_links:
    - from: "backend/src/offboarding_flow/services/bot_service.py"
      to: "backend/src/offboarding_flow/im/handler_registry.py"
      via: "_REGISTRY.register(HandlerSpec(...)) + spec.handler 调用"
      pattern: "_REGISTRY\\.(register|get)"
    - from: "backend/src/offboarding_flow/providers/outline_provider.py"
      to: "backend/src/offboarding_flow/providers/base.py"
      via: "实现 DocProvider 三个新生命周期方法"
      pattern: "async def (delete_collection|delete_document|list_documents_in_collection)"
---

<objective>
完成 8A 抽象重构剩余 3 个 REQ：
- ABS-03：`DocProvider` 加 `delete_collection / list_documents_in_collection / delete_document`（Huly 需要文档全生命周期）
- ABS-04：`IMProvider` 加 `register_command_listener(handler)` — 与 Plan 01 的 `IMListener.register_command_listener` 对称
- ABS-05：`bot_service.dispatch` 11 个 if/elif 分支重构为 `HandlerRegistry` 动态查表

Purpose: 完成 8A 全部抽象层；Plan 06 的 HulyDocProvider / HulyIMProvider 直接实现新 Protocol；
HandlerRegistry 让新 IM 平台（Huly）以及 MCP `submit_handover_doc` write tool 都能复用同一套命令分发。

Output:
- DocProvider / IMProvider Protocol 扩展 + 5 个 provider 实现（其中 lark/wecom/dingtalk 可 stub raise NotImplementedError）
- HandlerRegistry 模块 + BotService.dispatch 重构
- 测试：新方法覆盖 + dispatch 重构不破坏 11 个命令行为
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-huly-abstraction/08-CONTEXT.md
@.planning/phases/08-huly-abstraction/08-RESEARCH.md
@.planning/phases/08-huly-abstraction/08-01-SUMMARY.md

# 关键源码
@backend/src/offboarding_flow/providers/base.py
@backend/src/offboarding_flow/providers/outline_provider.py
@backend/src/offboarding_flow/providers/mattermost_provider.py
@backend/src/offboarding_flow/services/bot_service.py

<interfaces>
<!-- 当前 DocProvider / IMProvider Protocol（待扩展）-->

From backend/src/offboarding_flow/providers/base.py（现有）:
```python
@runtime_checkable
class DocProvider(Protocol):
    name: str
    async def create_document(self, *, title, markdown, owner_usernames=None) -> DocInfo: ...
    async def update_document(self, *, doc_id, markdown, title=None) -> None: ...
    async def list_documents(self, *, query=None, limit=10) -> list[DocInfo]: ...
    async def ensure_users(self, users: list[dict[str, str]]) -> dict[str, list[str]]: ...
    async def get_document(self, doc_id: str) -> DocInfo | None: ...
    # 🆕 ABS-03 加：
    # async def delete_collection(self, collection_id: str) -> None: ...
    # async def list_documents_in_collection(self, collection_id: str) -> list[DocInfo]: ...
    # async def delete_document(self, doc_id: str) -> None: ...

@runtime_checkable
class IMProvider(Protocol):
    name: str
    async def post_to_channel(self, channel_id, markdown) -> None: ...
    async def send_dm(self, username, markdown) -> None: ...
    async def ensure_user_in_channel(self, channel_id, username) -> None: ...
    async def list_team_users(self) -> list[UserInfo]: ...
    async def resolve_username(self, username: str) -> UserInfo | None: ...
    # 🆕 ABS-04 加：
    # def register_command_listener(self, handler: Callable[..., Awaitable[None]]) -> None: ...
```

From backend/src/offboarding_flow/services/bot_service.py（当前 dispatch 结构）:
```python
class BotService:
    async def dispatch(self, cmd: BotCommand, ctx: BotInvocationContext) -> str:
        if cmd.name == "help": return self.handle_help()
        elif cmd.name == "start": return await self.handle_start(...)
        elif cmd.name == "status": return await self.handle_status(...)
        # ... 11 个分支
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 DocProvider + IMProvider Protocol + 5 个 provider 实现新方法</name>
  <files>
    backend/src/offboarding_flow/providers/base.py
    backend/src/offboarding_flow/providers/outline_provider.py
    backend/src/offboarding_flow/providers/lark_provider.py
    backend/src/offboarding_flow/providers/wecom_provider.py
    backend/src/offboarding_flow/providers/dingtalk_provider.py
    backend/src/offboarding_flow/providers/mattermost_provider.py
    backend/tests/unit/providers/test_doc_provider_lifecycle.py
  </files>
  <action>
    1. 改造 `backend/src/offboarding_flow/providers/base.py`：
       - DocProvider Protocol 加 3 个方法（中文 docstring）：
         ```python
         async def delete_collection(self, collection_id: str) -> None:
             """删除一个文档集合 / Teamspace / Wiki Space（含其下所有文档）。Huly 需要。"""
             ...

         async def list_documents_in_collection(
             self, collection_id: str, *, limit: int = 50
         ) -> list[DocInfo]:
             """列出集合内所有文档（按 rank 排序）。"""
             ...

         async def delete_document(self, doc_id: str) -> None:
             """删除单篇文档。"""
             ...
         ```
       - IMProvider Protocol 加：
         ```python
         from typing import Awaitable, Callable
         CommandHandlerFn = Callable[..., Awaitable[None]]

         def register_command_listener(self, handler: CommandHandlerFn) -> None:
             """ABS-04：让 provider 反向订阅命令 dispatch（与 IMListener 对称）。
             实现：内部存 self._handler = handler；listener 模式的 provider（Mattermost / Huly）
             收到命令时调用 handler。Webhook-only / send-only provider 可直接 pass。
             """
             ...
         ```
       - **顶部 import** 加 `from typing import Awaitable, Callable`

    2. 实现新方法（5 个 provider）：

       **OutlineProvider** (`outline_provider.py`)：
       - `delete_collection`：调 Outline REST `POST /api/collections.delete {id, permanent: true}`（参考已有 outline/client.py 模式）
       - `delete_document`：调 `POST /api/documents.delete {id, permanent: true}`
       - `list_documents_in_collection`：调 `POST /api/documents.list {collectionId, limit}`，转 list[DocInfo]
       - `register_command_listener`：pass + log info（Outline 是 doc 不是 IM，但 base.py 是 DocProvider 不 IMProvider — 跳过此 IM 方法）

       **LarkDocsProvider / WeComDocProvider / DingTalkDocProvider**：
       - 3 个新方法 stub：`raise NotImplementedError(f"{self.name}: {method_name} 不在 v1 支持范围")` + 中文 log
       - Lark IM 已有 register 接口的 — 加 `register_command_listener` 实现（v1 stub OK）

       **MattermostProvider** (`mattermost_provider.py`)：
       - `register_command_listener(handler)`：内部 `self._handler = handler` + log（Mattermost listener 是独立 worker，provider 本身不订阅；本方法用于 IMProvider Protocol 兼容）

    3. 创建测试 `backend/tests/unit/providers/test_doc_provider_lifecycle.py`：
       - 测试 1：OutlineProvider 三个方法可调用（mock httpx response，不真打 Outline）
       - 测试 2：LarkDocsProvider.delete_collection raises NotImplementedError 含中文 msg
       - 测试 3：MattermostProvider.register_command_listener 后 self._handler 不为 None
       - 测试 4：runtime_checkable — `isinstance(OutlineProvider(...), DocProvider)` True（验证 Protocol 满足）

    全局规则：所有 stub 用 NotImplementedError 而不是 pass（防 silent fail）；中文 log 含 provider 名称。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/providers/test_doc_provider_lifecycle.py -x -v</automated>
  </verify>
  <done>
    - DocProvider Protocol 含 3 个生命周期方法（grep 命中）
    - IMProvider Protocol 含 register_command_listener（grep 命中）
    - 5 个 provider 实现新方法（含 stub）
    - 4 个新测试 PASS
    - `mypy backend/src/offboarding_flow/providers/` 0 error（含 Protocol checking）
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 HandlerRegistry + HandlerSpec dataclass + 单测</name>
  <files>
    backend/src/offboarding_flow/im/handler_registry.py
    backend/tests/unit/im/test_handler_registry.py
  </files>
  <action>
    1. 创建 `backend/src/offboarding_flow/im/handler_registry.py`（参考 RESEARCH §Code Examples 8A）：
       - 顶部 imports：
         ```python
         from __future__ import annotations
         from dataclasses import dataclass, field
         from typing import Awaitable, Callable, Any, TYPE_CHECKING
         if TYPE_CHECKING:
             from offboarding_flow.services.bot_command_parser import BotCommand
             from offboarding_flow.services.bot_service import BotInvocationContext, BotService
         ```
       - 类型别名：`HandlerFn = Callable[["BotCommand", "BotInvocationContext", "BotService"], Awaitable[str]]`
       - `@dataclass(frozen=True) class HandlerSpec`：
         - `name: str` — 命令名（"help" / "start" 等）
         - `handler: HandlerFn`
         - `description: str` — 用于 help 输出
         - `allowed_roles: frozenset[str] = field(default_factory=frozenset)` — 空 = 所有人；非空 = 白名单
       - `class HandlerRegistry`：
         - `__init__`：`self._handlers: dict[str, HandlerSpec] = {}`
         - `register(spec: HandlerSpec) -> None`：去重检查（已存在抛 `ValueError(f"重复注册命令: {spec.name}")`）
         - `get(name: str) -> HandlerSpec | None`
         - `all() -> list[HandlerSpec]`
         - `__contains__(name: str) -> bool`
         - `__len__() -> int`
       - 顶部 module docstring 中文说明 "ABS-05 替代 bot_service.dispatch 11 个 if/elif"

    2. 创建 `backend/tests/unit/im/test_handler_registry.py`：
       - 测试 1：注册 + get 命中 + 不命中返回 None
       - 测试 2：重复注册抛 ValueError 含中文 msg
       - 测试 3：`all()` 返回 list 长度正确
       - 测试 4：`HandlerSpec` 是 frozen（不可变）
       - 测试 5：`allowed_roles` 默认空 frozenset；显式传入后正确
       - 测试 6：`__contains__` / `__len__` magic methods 工作

    全局规则：HandlerSpec 用 frozen dataclass（immutability rule）；handlers dict 不暴露（封装在 _handlers）。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/im/test_handler_registry.py -x -v</automated>
  </verify>
  <done>
    - `HandlerRegistry` + `HandlerSpec` 类定义完成
    - 6 个单测全 PASS
    - `mypy backend/src/offboarding_flow/im/handler_registry.py` 0 error
    - `python -c "from offboarding_flow.im.handler_registry import HandlerRegistry, HandlerSpec"` 0 error
  </done>
</task>

<task type="auto">
  <name>Task 3: 重构 BotService.dispatch 为 registry-driven + 集成回归测试</name>
  <files>
    backend/src/offboarding_flow/services/bot_service.py
    backend/tests/integration/test_bot_service_dispatch_registry.py
  </files>
  <action>
    1. 改造 `backend/src/offboarding_flow/services/bot_service.py`（参考 RESEARCH §Code Examples ABS-05）：
       - 文件顶部加：`from offboarding_flow.im.handler_registry import HandlerRegistry, HandlerSpec`
       - 在 `BotService` 类**外**、文件末尾或顶部，定义 module-level `_REGISTRY = HandlerRegistry()`
       - 定义 module-level adapter 函数（每个 handler 包一层标准签名 `(cmd, ctx, bot) -> str`），共 11 个：
         ```python
         async def _handle_help(cmd, ctx, bot): return bot.handle_help()
         async def _handle_start(cmd, ctx, bot):
             # 解析 cmd.args 拿 employee_id 等参数（按 BotCommand schema）
             return await bot.handle_start(cmd.args.get("employee_id"), ctx)
         async def _handle_status(cmd, ctx, bot):
             return await bot.handle_status(cmd.args.get("flow_id"))
         # ... 11 个
         ```
       - 定义 `_setup_registry()` 函数注册所有 11 个 HandlerSpec（含 allowed_roles：start 限 `{"hr","hr_admin","admin"}`，meeting-ingest 限 `{"hr","admin"}` 等按现状）
       - 模块加载时调用 `_setup_registry()`（在所有 _handle_* 定义后）
       - **重写 `BotService.dispatch`**：
         ```python
         async def dispatch(self, cmd: BotCommand, ctx: BotInvocationContext) -> str:
             """ABS-05：通过 HandlerRegistry 分发，替代 11 个 if/elif。"""
             spec = _REGISTRY.get(cmd.name)
             if spec is None:
                 raise ValueError(f"未知命令：{cmd.name}（可用：{', '.join(s.name for s in _REGISTRY.all())}）")
             if spec.allowed_roles and ctx.user_role not in spec.allowed_roles:
                 raise BotPermissionError(
                     f"权限不足：'{cmd.name}' 仅 {sorted(spec.allowed_roles)} 角色可用（当前 role={ctx.user_role}）"
                 )
             return await spec.handler(cmd, ctx, self)
         ```
       - 公开 `_REGISTRY` 给测试（或加 `def get_registry() -> HandlerRegistry: return _REGISTRY`）
       - 保留所有 11 个 `handle_*` 方法（_handle_* adapter 内部调用它们，业务逻辑 0 变化）

    2. 创建 `backend/tests/integration/test_bot_service_dispatch_registry.py`（pytest.mark.integration）：
       - Fixture：用现有 conftest.py 的 db_session + real settings + mock GLM
       - 测试 1：dispatch("help") → 返回 help 文本（与旧实现 byte-by-byte 一致 — 用 snapshot）
       - 测试 2：dispatch 未知命令 → ValueError msg 含可用列表
       - 测试 3：非 HR 用户 dispatch("start") → BotPermissionError 含 role 信息
       - 测试 4：HR 用户 dispatch("start", employee_id="zhang.san") → 正常返回（与旧实现一致）
       - 测试 5：dispatch("status", flow_id=X) → 返回 status 文本（用 seed 的真实 flow）
       - 测试 6：_REGISTRY 含 11 个命令（assert len == 11，所有命令名 in [...]）
       - 测试 7：所有 11 个 handler 通过 dispatch 路径调用 — 用 monkeypatch 替换 _handle_* 验证被调用

    3. 全量回归：`cd backend && uv run pytest tests/test_bot_service*.py tests/test_bot_command*.py -v` 必须全 PASS

    全局规则：保留所有 `handle_*` 方法签名；adapter 只是包装；commit message 用 `refactor: bot_service.dispatch 重构为 HandlerRegistry（ABS-05）`。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/integration/test_bot_service_dispatch_registry.py tests/test_bot_service.py tests/test_bot_command_parser.py -x -v 2>&amp;1 | tail -50</automated>
  </verify>
  <done>
    - BotService.dispatch 重构完成（行数从 ~45 → ≤ 10，grep `if cmd\.name ==` 在 dispatch 内 0 命中）
    - _REGISTRY 注册 11 个 handler
    - 7 个新集成测试全 PASS
    - 所有现有 bot_service 测试 0 回归
    - `cd backend && uv run pytest -x` 全量 0 回归
    - `ruff check backend/src/offboarding_flow/services/bot_service.py` 0 error
  </done>
</task>

</tasks>

<verification>
端到端验证（Plan 02 整体）：
1. `cd backend && uv run pytest tests/unit/providers/test_doc_provider_lifecycle.py tests/unit/im/test_handler_registry.py tests/integration/test_bot_service_dispatch_registry.py -v` — 17 个新测试 PASS
2. `cd backend && uv run pytest -x` — 全量 0 回归
3. `grep -c "elif cmd\.name" backend/src/offboarding_flow/services/bot_service.py` — 应为 0（dispatch 内已无 if/elif）
4. `grep -c "_REGISTRY\.register" backend/src/offboarding_flow/services/bot_service.py` — 应为 11（11 个命令注册）
5. `mypy backend/src/offboarding_flow/providers/ backend/src/offboarding_flow/im/ backend/src/offboarding_flow/services/bot_service.py` — 0 error

must_haves 反向校验：
- ✅ Truth 1：grep `delete_collection|list_documents_in_collection|delete_document` in base.py 3 命中
- ✅ Truth 2：grep `def register_command_listener` in base.py（IMProvider 段）1 命中
- ✅ Truth 3：每个 provider 文件 grep 新方法
- ✅ Truth 4：handler_registry.py 存在
- ✅ Truth 5：BotService.dispatch 行数缩到 ≤ 10
- ✅ Truth 6：11 个命令测试全 PASS
- ✅ Truth 7：全量 pytest 0 回归
</verification>

<success_criteria>
- 8A 抽象重构全部完成（Plan 01 + Plan 02 共覆盖 ABS-01..05）
- 5 个 doc provider + Mattermost IM provider 全实现新方法
- HandlerRegistry 替代 dispatch 11 个 if/elif
- 现有所有测试 0 回归 + 新增 17 个测试 PASS
- Wave 0 完成，Wave 1（Plan 03/04/07）可启动
</success_criteria>

<output>
完成后写 `.planning/phases/08-huly-abstraction/08-02-SUMMARY.md` 含：
- 5 个 provider 新方法实现状态（哪些是真实现 / 哪些是 stub）
- HandlerRegistry 11 个注册命令清单（含 allowed_roles）
- BotService.dispatch 重构前后 LOC 对比
- 17 个新测试名称
- 后续 plan 复用契约：HandlerRegistry API（register/get/all）+ DocProvider 3 新方法 + IMProvider register_command_listener
</output>
