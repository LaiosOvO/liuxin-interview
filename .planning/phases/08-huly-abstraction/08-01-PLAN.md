---
phase: 08-huly-abstraction
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/offboarding_flow/im/__init__.py
  - backend/src/offboarding_flow/im/protocol.py
  - backend/src/offboarding_flow/im/dispatcher.py
  - backend/src/offboarding_flow/im/context.py
  - backend/src/offboarding_flow/workers/mattermost_listener.py
  - backend/src/offboarding_flow/main.py
  - backend/tests/unit/im/__init__.py
  - backend/tests/unit/im/test_protocol.py
  - backend/tests/unit/im/test_dispatcher.py
  - backend/tests/integration/test_mattermost_listener_dispatch.py
autonomous: true
requirements:
  - ABS-01
  - ABS-02

must_haves:
  truths:
    - "IMListener Protocol 在 backend/src/offboarding_flow/im/protocol.py 定义，含 start/stop/register_command_listener 三方法"
    - "dispatch_message 通用函数在 backend/src/offboarding_flow/im/dispatcher.py，接收 sender_username/channel_id/channel_type/message/im_helpers 等参数"
    - "MattermostListener 实现 IMListener Protocol（isinstance(l, IMListener) is True）"
    - "MattermostListener._handle_event 行 175+ 的 dispatch 逻辑改为调用 dispatch_message"
    - "main.py lifespan 通过 listener.register_command_listener(dispatch_message) 注册"
    - "现有 332 个测试 0 回归"
  artifacts:
    - path: "backend/src/offboarding_flow/im/protocol.py"
      provides: "IMListener Protocol + DispatchFn 类型"
      min_lines: 25
    - path: "backend/src/offboarding_flow/im/dispatcher.py"
      provides: "dispatch_message 函数 + IMHelpers dataclass"
      min_lines: 80
    - path: "backend/src/offboarding_flow/im/context.py"
      provides: "BotInvocationContext 扩展（im_helpers 字段）"
      min_lines: 10
    - path: "backend/src/offboarding_flow/workers/mattermost_listener.py"
      provides: "实现 IMListener Protocol 的 MattermostListener"
      contains: "name = \"mattermost\""
  key_links:
    - from: "backend/src/offboarding_flow/workers/mattermost_listener.py"
      to: "backend/src/offboarding_flow/im/dispatcher.py"
      via: "调用 self._dispatch(...) 转交消息"
      pattern: "self\\._dispatch\\s*\\("
    - from: "backend/src/offboarding_flow/main.py"
      to: "backend/src/offboarding_flow/im/dispatcher.py"
      via: "listener.register_command_listener(dispatch_message)"
      pattern: "register_command_listener\\s*\\(\\s*dispatch_message"
---

<objective>
建立 IM 抽象层基座：定义 `IMListener` Protocol 与通用 `dispatch_message` 函数，并把现有
`MattermostListener._handle_event` 行 175+ 的 dispatch 逻辑（parse_command → intent_router →
sender_role 解析 → BotService.dispatch → 回写）提取为平台无关的纯函数。

Purpose: 8A 的核心抽象，是 8B Huly Listener 与 8C MCP 复用业务逻辑的前置依赖。本 plan
**只做抽象重构，零业务行为变化**，必须保证现有 332 测试 0 回归。

Output:
- `backend/src/offboarding_flow/im/` 新模块（protocol/dispatcher/context）
- `MattermostListener` 实现 Protocol 并改用 dispatch_message
- main.py lifespan 注册改造
- 单测 + 集成测试覆盖 Protocol 行为 + dispatcher 分支
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-huly-abstraction/08-CONTEXT.md
@.planning/phases/08-huly-abstraction/08-RESEARCH.md

# 关键源码（dispatch 逻辑当前位置）
@backend/src/offboarding_flow/workers/mattermost_listener.py
@backend/src/offboarding_flow/services/bot_service.py
@backend/src/offboarding_flow/services/bot_command_parser.py
@backend/src/offboarding_flow/services/bot_intent_router.py
@backend/src/offboarding_flow/main.py

<interfaces>
<!-- 当前 mattermost_listener.py 的关键内部方法 — dispatcher 提取时直接复用 -->

From backend/src/offboarding_flow/workers/mattermost_listener.py（现有，待重构）:
```python
class MattermostListener:
    # 已有但需补 name 属性
    async def start(self) -> None: ...
    async def stop(self) -> None: ...

    # 内部 helpers（dispatcher 化后由 IMHelpers 注入）
    async def _post_reply(self, channel_id: str, markdown: str) -> None: ...
    async def _send_dm_by_username(self, username: str, markdown: str) -> None: ...
    async def _ensure_user_in_channel(self, channel_id: str, username: str) -> None: ...

    # 待提取的行 175+ 逻辑（_handle_event 内）
    async def _handle_event(self, event_str: str) -> None:
        # 行 129-173：WS 解码 + 触发条件检查（保留）
        # 行 175+：parse → intent → role → ctx → BotService.dispatch → 回复（提取出来）
```

From backend/src/offboarding_flow/services/bot_service.py:
```python
@dataclass
class BotInvocationContext:
    user_sub: str
    user_role: str
    channel_id: str
    channel_type: str
    sender_username: str
    raw_message: str
    settings: Any
    session_factory: Callable[[], AsyncContextManager[AsyncSession]]
    # 🆕 新增字段（context.py 扩展）: im_helpers: IMHelpers

class BotService:
    async def dispatch(self, cmd: BotCommand, ctx: BotInvocationContext) -> str: ...
```

From backend/src/offboarding_flow/services/bot_command_parser.py:
```python
def parse_command(message: str) -> BotCommand  # 失败抛 BotCommandParseError
```

From backend/src/offboarding_flow/services/bot_intent_router.py:
```python
class BotIntentRouter:
    async def route(self, message: str, sender_role: str) -> BotCommand | None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 IMListener Protocol + IMHelpers dataclass + 扩展 BotInvocationContext</name>
  <files>
    backend/src/offboarding_flow/im/__init__.py
    backend/src/offboarding_flow/im/protocol.py
    backend/src/offboarding_flow/im/context.py
    backend/tests/unit/im/__init__.py
    backend/tests/unit/im/test_protocol.py
  </files>
  <action>
    1. 创建 `backend/src/offboarding_flow/im/__init__.py`（空文件 + 模块 docstring）
    2. 创建 `backend/src/offboarding_flow/im/protocol.py`：
       - `from __future__ import annotations`
       - `from typing import Protocol, runtime_checkable, Awaitable, Callable`
       - 定义 `DispatchFn = Callable[..., Awaitable[None]]` 类型别名
       - `@runtime_checkable class IMListener(Protocol)` 含三方法：
         - `name: str`（class attribute）
         - `async def start(self) -> None`
         - `async def stop(self) -> None`
         - `def register_command_listener(self, dispatch: DispatchFn) -> None`
       - 所有 docstring 用中文，说明 listener 生命周期与反向订阅 hook 用途
    3. 创建 `backend/src/offboarding_flow/im/context.py`：
       - `@dataclass(frozen=True) class IMHelpers`（参考 RESEARCH §Pattern 3）
       - 字段：`post_channel: Callable[[str, str], Awaitable[None]]`、`send_dm: Callable[[str, str], Awaitable[None]]`、`ensure_in_channel: Callable[[str, str], Awaitable[None]] | None = None`
       - 同文件加 `BotInvocationContext` 的迁移注释（注：BotInvocationContext 仍在 bot_service.py，本 plan 不动；Plan 02 ABS-05 重构时再加 im_helpers 字段）
    4. 创建测试 `backend/tests/unit/im/__init__.py`（空）
    5. 创建测试 `backend/tests/unit/im/test_protocol.py`：
       - 测试 1：定义一个最小 MockListener 实现三方法 → `isinstance(MockListener(), IMListener) is True`
       - 测试 2：缺 `register_command_listener` 的类 → `isinstance(...) is False`（runtime_checkable 检查）
       - 测试 3：DispatchFn 类型别名导入可用
       - 测试 4：IMHelpers 是 frozen dataclass（不可变性）— `with pytest.raises(FrozenInstanceError): helpers.post_channel = None`

    全局规则：本 plan 注释 / docstring 全部中文；类型注解严格；不引入 dict-based 数据结构（用 dataclass）。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/im/test_protocol.py -x -v</automated>
  </verify>
  <done>
    - `im/protocol.py` 含 `IMListener` Protocol + `DispatchFn` 类型别名
    - `im/context.py` 含 `IMHelpers` frozen dataclass
    - 4 个单测全部 PASS
    - `python -c "from offboarding_flow.im.protocol import IMListener, DispatchFn; from offboarding_flow.im.context import IMHelpers"` 0 error
  </done>
</task>

<task type="auto">
  <name>Task 2: 提取 dispatch_message 通用函数 + 单测覆盖所有分支</name>
  <files>
    backend/src/offboarding_flow/im/dispatcher.py
    backend/tests/unit/im/test_dispatcher.py
  </files>
  <action>
    1. 创建 `backend/src/offboarding_flow/im/dispatcher.py`：
       - 函数签名（严格按 RESEARCH §Pattern 3 + §Code Examples 8A）：
         ```python
         async def dispatch_message(
             *,
             sender_username: str,
             user_id: str,
             channel_id: str,
             channel_type: str,    # 'D' | 'O' | 'P'
             message: str,
             im_helpers: IMHelpers,
             settings: Any,
             session_factory: Callable[[], AsyncContextManager[AsyncSession]],
         ) -> None:
         ```
       - 内部实现 6 步（**完全照搬** mattermost_listener.py 行 175-244 的逻辑）：
         1. `cmd = parse_command(message)` — 失败抛 BotCommandParseError 时走 LLM intent router fallback
         2. fallback：`router = BotIntentRouter(settings); cmd = await router.route(message, sender_role)`（sender_role 在第 4 步前要解析）
         3. 进 session：`async with session_factory() as session` → 查 User by username → 取 role
         4. 构造 `ctx = BotInvocationContext(...)` — 把 im_helpers 通过 ctx 字段或后续 Plan 02 加上；本 plan 暂用 closure 方式或临时 attribute
         5. `bot = BotService(...); reply = await bot.dispatch(cmd, ctx)`
         6. 失败处理：BotFlowNotFoundError / BotPermissionError / 其他 Exception → 友好 markdown reply
         7. `await im_helpers.post_channel(channel_id, reply)` 回写
       - 异常 import：`from offboarding_flow.services.bot_service import BotPermissionError, BotFlowNotFoundError, BotInvocationContext, BotService`
       - 中文 docstring 完整说明每一步

    2. 创建 `backend/tests/unit/im/test_dispatcher.py` — pytest-asyncio + AsyncMock：
       - Fixture：`im_helpers` 用 AsyncMock 实现 post_channel/send_dm；`settings` mock；`session_factory` 用 fakeredis-style asynccontextmanager
       - 测试 1：合法命令（"help"） → `parse_command` 成功 → `bot.dispatch` 返回 reply → `im_helpers.post_channel.assert_awaited_once_with(channel_id, reply)`
       - 测试 2：parse 失败 → intent_router 返回命令 → `bot.dispatch` 成功 → 同测试 1
       - 测试 3：parse 失败 + intent_router 返回 None → 用户友好提示（"未识别命令"）回写
       - 测试 4：BotFlowNotFoundError → 友好 markdown reply 回写（含 `:warning:`）
       - 测试 5：BotPermissionError → 拒绝提示回写
       - 测试 6：未知 Exception → 通用错误提示 + log（用 caplog 验证）

    全局规则：mock 用 `unittest.mock.AsyncMock`；不 mock DB（用 session_factory 注入测试 session）；测试函数中文 docstring 描述场景。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/im/test_dispatcher.py -x -v --tb=short</automated>
  </verify>
  <done>
    - `dispatch_message` 函数实现完成
    - 6 个测试场景全部 PASS（含 happy path 与 4 种错误分支）
    - `ruff check backend/src/offboarding_flow/im/dispatcher.py` 0 error
    - `mypy backend/src/offboarding_flow/im/dispatcher.py` 0 error
  </done>
</task>

<task type="auto">
  <name>Task 3: MattermostListener 实现 Protocol + main.py 注册改造 + 集成回归测试</name>
  <files>
    backend/src/offboarding_flow/workers/mattermost_listener.py
    backend/src/offboarding_flow/main.py
    backend/tests/integration/test_mattermost_listener_dispatch.py
  </files>
  <action>
    1. 改造 `backend/src/offboarding_flow/workers/mattermost_listener.py`：
       - 顶部加 `from offboarding_flow.im.protocol import IMListener, DispatchFn`
       - 顶部加 `from offboarding_flow.im.context import IMHelpers`
       - 类内加 class-level `name: str = "mattermost"` 满足 Protocol
       - 加 `__init__` 时 `self._dispatch: DispatchFn | None = None`
       - 加 `def register_command_listener(self, dispatch: DispatchFn) -> None` — 设置 `self._dispatch = dispatch`（含中文 docstring "ABS-04 反向订阅 hook"）
       - **改造 `_handle_event` 行 175 以后**：原 dispatch 逻辑全删，替换为：
         ```python
         helpers = IMHelpers(
             post_channel=self._post_reply,
             send_dm=self._send_dm_by_username,
             ensure_in_channel=self._ensure_user_in_channel,
         )
         if self._dispatch is None:
             logger.warning("[mattermost_listener] dispatch 未注册，丢弃消息：%s", message[:80])
             return
         await self._dispatch(
             sender_username=sender_name,
             user_id=post_user_id,
             channel_id=channel_id,
             channel_type=channel_type,
             message=message,
             im_helpers=helpers,
             settings=self.settings,
             session_factory=new_session,
         )
         ```
       - **保留** httpx monkey-patch（行 32-40，Pitfall #3）— 不动
       - **保留** WS 解码 + 触发条件检查（行 129-173）— 不动

    2. 改造 `backend/src/offboarding_flow/main.py`（lifespan 函数行 75-83）：
       - import: `from offboarding_flow.im.protocol import IMListener; from offboarding_flow.im.dispatcher import dispatch_message`
       - 注释更新：'# IM listener — 实现 IMListener Protocol；ABS-04 注册统一 dispatch'
       - 在 `mm_listener = MattermostListener(settings)` 后立即：
         ```python
         mm_listener.register_command_listener(dispatch_message)
         ```
       - 仅在 `settings.mattermost_bot_token` 存在时启动（已有逻辑保留）

    3. 创建集成测试 `backend/tests/integration/test_mattermost_listener_dispatch.py`（pytest.mark.integration）：
       - 测试 1：`MattermostListener` 满足 `isinstance(listener, IMListener)`（runtime_checkable）
       - 测试 2：未 register_command_listener 时收到事件 → log warning + 不抛
       - 测试 3：register_command_listener 后 → `_handle_event(fake_ws_event)` → dispatch_message mock 被调用，参数正确（sender_username / channel_id / message）
       - 测试 4：MattermostListener.name == "mattermost"
       - **注意**：测试 3 用 fakeredis + 真 PostgreSQL（项目 CLAUDE.md §2.3 集成测试禁止 mock DB）；用现有 conftest 的 `db_session` fixture

    4. 全量回归：`cd backend && uv run pytest -x` 必须保持 332 PASS / 27 skipped（与 Phase 7 后基线一致）

    全局规则：保留所有现有中文注释；不改 mattermost_listener.py 原有的 monkeypatch 与 WS 部分。
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/integration/test_mattermost_listener_dispatch.py tests/ -x --tb=short -q 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - `MattermostListener` 满足 `IMListener` Protocol（isinstance 测试 PASS）
    - main.py lifespan 通过 `mm_listener.register_command_listener(dispatch_message)` 注册
    - 集成测试 4 个用例全 PASS
    - `cd backend && uv run pytest -x` 全量 332 PASS / 27 skipped 0 回归（含新增 4 个 + 6 个 + 4 个 = 14 个新测试 → 总数应 ≥ 346）
    - `ruff check backend/src/offboarding_flow/workers/mattermost_listener.py backend/src/offboarding_flow/main.py` 0 error
    - `mypy backend/src/offboarding_flow/im/` 0 error
  </done>
</task>

</tasks>

<verification>
端到端验证（Plan 01 整体）：
1. `cd backend && uv run pytest tests/unit/im/ tests/integration/test_mattermost_listener_dispatch.py -v` — 14 个新测试全 PASS
2. `cd backend && uv run pytest -x` — 332+ 全量 PASS / 0 回归
3. `cd backend && uv run mypy src/offboarding_flow/im/ src/offboarding_flow/workers/mattermost_listener.py` — 0 error
4. `python -c "from offboarding_flow.im.protocol import IMListener; from offboarding_flow.workers.mattermost_listener import MattermostListener; print(isinstance.__self__ if False else 'ok')"` — import 0 error
5. 手动 smoke（可选）：docker compose up backend → mm bot 收 `@offboarding-bot help` → log 显示 dispatch_message 被调用 → 收到 help 回复

must_haves 反向校验：
- ✅ Truth 1：`grep -n "IMListener" backend/src/offboarding_flow/im/protocol.py` 命中
- ✅ Truth 2：`grep -n "dispatch_message" backend/src/offboarding_flow/im/dispatcher.py` 命中
- ✅ Truth 3：unit test 1 验证 isinstance
- ✅ Truth 4：`grep -n "self\._dispatch\s*(" backend/src/offboarding_flow/workers/mattermost_listener.py` 命中
- ✅ Truth 5：`grep -n "register_command_listener\s*(\s*dispatch_message" backend/src/offboarding_flow/main.py` 命中
- ✅ Truth 6：全量 pytest PASS
</verification>

<success_criteria>
- IMListener Protocol + IMHelpers + dispatch_message 三个新模块文件已创建
- MattermostListener 在不破坏现有行为前提下实现 Protocol
- main.py lifespan 显式调用 register_command_listener
- 现有 332 测试 0 回归（新增 14 个单测 + 集成测试全 PASS）
- ruff / mypy 0 error
- 所有 commit message / docstring 中文（继承 CLAUDE.md §6）
</success_criteria>

<output>
完成后写 `.planning/phases/08-huly-abstraction/08-01-SUMMARY.md` 含：
- 新建的 im/ 模块 3 个文件路径与导出符号
- MattermostListener 改造点（哪几行替换为 dispatch_message 调用）
- 14 个新测试名称清单
- 关键决策：BotInvocationContext 是否在本 plan 加 im_helpers 字段（决定 Plan 02 ABS-05 是否需要再次改动）
- 后续 plan 可复用的契约：`IMListener` Protocol + `dispatch_message` 签名 + `IMHelpers` 字段
</output>
