---
phase: 03-auth-deeplink
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/offboarding_flow/auth/jti_service.py
  - backend/src/offboarding_flow/auth/redis_client.py
  - backend/src/offboarding_flow/api/health.py
  - backend/tests/auth/test_jti_service.py
  - backend/tests/auth/test_jti_service_concurrency.py
  - backend/tests/conftest.py
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "auth/redis_client.py 暴露 get_redis() / dispose_redis() 单例工厂；用 redis.asyncio.Redis from_url(REDIS_URL, decode_responses=True)"
    - "auth/jti_service.py 暴露 consume_jti(redis, jti, ttl_seconds) -> bool（True=首次消费，False=已消费）— 用 SET key value NX EX 原子操作"
    - "auth/jti_service.py 暴露 register_node_token(redis, node_id, jti, ttl_seconds) — SADD + EXPIRE 防止 SET 永不过期"
    - "auth/jti_service.py 暴露 invalidate_node_tokens(redis, node_id) -> int — SMEMBERS → DEL 每个 jti:{value} → DEL node:jti:{id}，返回失效数量"
    - "auth/jti_service.py 暴露 is_jti_consumed(redis, jti) -> bool — 仅检查不消费（debug 用）"
    - "api/health.py 接入 Redis ping：components['redis'] 从 'not_checked' 改为真 ping 结果"
    - "tests/conftest.py 新增 redis_client fixture（function scope，autoflushdb，连 REDIS_TEST_URL 或默认 redis://localhost:6380/1）"
    - "test_jti_service.py 单元测试 ≥ 8 个用例覆盖：首次消费/重放/不同 jti/register 后 SADD/invalidate 清理/SET 自动过期/key 命名/ttl 生效"
    - "test_jti_service_concurrency.py ≥ 2 用例：asyncio.gather 并发同 jti 仅 1 个 True；asyncio.gather 并发不同 jti 全 True"
    - "覆盖率 ≥ 80%（jti_service.py + redis_client.py）"
  artifacts:
    - backend/src/offboarding_flow/auth/redis_client.py
    - backend/src/offboarding_flow/auth/jti_service.py
    - backend/tests/auth/test_jti_service.py
    - backend/tests/auth/test_jti_service_concurrency.py
  key_links:
    - "consume_jti 调 redis.set(key, '1', nx=True, ex=ttl_seconds) — 返回 True/None；本方法返回 True/False"
    - "redis_key 命名约定：consume key = 'jti:{value}'；node SET key = 'node:jti:{node_id}'"
    - "register_node_token 用 pipeline (SADD + EXPIRE) 保证 SET 不会因 key 永驻泄漏"
    - "invalidate_node_tokens 调 SMEMBERS → 批量 DEL → DEL SET 本身；返回 int 删除数量"
    - "health.py 用 try-except 抓 redis ping，失败 components['redis']='fail: {type}'，不阻断 health 200 返回"
    - "tests/conftest.py 用 pytest_asyncio.fixture loop_scope='session' 确保 redis client 复用 event loop（PITFALLS #23）"
---

<objective>
落地 jti 一次性消费的 Redis 层 — 包括 Redis async client 单例、consume_jti 原子操作、node:jti:{id} SET 管理（用于节点状态变更时批量失效）。本 plan 是 Wave 1 两个并行 plan 之一，与 Plan 01 完全独立（无文件冲突，无类型依赖 — 接口只用 str/UUID）。

Purpose: 为 Plan 03（POST /api/auth/exchange）提供 jti 黑名单后端能力；为 Plan 04（node_service hook）提供节点 token 批量失效能力。本 plan 是 Phase 3 防双击 race 的核心保证（PITFALLS #6）。

Output: 可独立集成测试的 jti 服务（含并发 race 测试），≥ 10 个测试全部通过。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-auth-deeplink/03-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@PRD.md
@backend/src/offboarding_flow/config.py
@backend/src/offboarding_flow/api/health.py
@backend/tests/conftest.py
@docker-compose.yml

<interfaces>
<!-- Redis 关键 key 命名（CONTEXT §D5） -->

| Key                    | 类型 | TTL              | 用途                  |
|------------------------|------|------------------|-----------------------|
| jti:{jti_value}        | str  | token_expiry     | 一次性消费标记         |
| node:jti:{node_id}     | SET  | token_expiry     | 节点未消费 token 集合 |

<!-- jti_service 函数签名（本 plan 落地） -->

async def consume_jti(redis: Redis, jti: str, *, ttl_seconds: int) -> bool:
    """SET NX EX 原子操作。True = 首次消费成功；False = 已存在（重放攻击）。"""

async def is_jti_consumed(redis: Redis, jti: str) -> bool:
    """仅检查不消费（debug / 测试用）。"""

async def register_node_token(redis: Redis, node_id: str | UUID, jti: str, *, ttl_seconds: int) -> None:
    """把 jti 加入 node:jti:{node_id} SET；EXPIRE 防止 SET 永驻。pipeline 串两条命令。"""

async def invalidate_node_tokens(redis: Redis, node_id: str | UUID) -> int:
    """节点状态变更时批量失效该节点所有未消费 token。返回删除的 jti 数量。"""

<!-- Redis client 工厂 -->

async def get_redis() -> Redis:  # 单例
async def dispose_redis() -> None:  # lifespan shutdown

<!-- 现有 health.py 第 50-52 行待替换：
components["redis"] = "not_checked"  # 改成真 ping -->

<!-- pytest fixture: redis_client（function scope autoflush） -->
@pytest_asyncio.fixture
async def redis_client(): ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写 redis_client + jti_service 两个模块 + health.py 接 Redis ping</name>
  <files>
    backend/src/offboarding_flow/auth/redis_client.py
    backend/src/offboarding_flow/auth/jti_service.py
    backend/src/offboarding_flow/api/health.py
  </files>
  <action>
  1. `backend/src/offboarding_flow/auth/redis_client.py`：
     ```python
     """Redis async client 单例 — Phase 3 jti 黑名单 + Phase 4 outbox 用。

     约定：
     - 全局单例 _redis，FastAPI lifespan 不显式 init（懒加载首次 get_redis）
     - decode_responses=True 让所有 GET 返回 str 而非 bytes（简化 jti 比较）
     - dispose_redis 在 lifespan shutdown 调，关连接池
     """
     from __future__ import annotations
     import logging
     from redis.asyncio import Redis, ConnectionPool
     from offboarding_flow.config import get_settings

     logger = logging.getLogger(__name__)
     _redis: Redis | None = None
     _pool: ConnectionPool | None = None

     async def get_redis() -> Redis:
         """获取 Redis 单例（懒加载）。"""
         global _redis, _pool
         if _redis is None:
             settings = get_settings()
             _pool = ConnectionPool.from_url(
                 settings.redis_url,
                 decode_responses=True,
                 max_connections=10,
             )
             _redis = Redis(connection_pool=_pool)
             logger.info("[redis_client] initialized: %s", settings.redis_url)
         return _redis

     async def dispose_redis() -> None:
         """lifespan shutdown 调用 — 关闭连接池。"""
         global _redis, _pool
         if _redis is not None:
             await _redis.aclose()
             _redis = None
         if _pool is not None:
             await _pool.aclose()
             _pool = None
         logger.info("[redis_client] disposed")
     ```

  2. `backend/src/offboarding_flow/auth/jti_service.py`：
     ```python
     """jti 一次性消费 + 节点 token 批量失效。

     核心：consume_jti 用 Redis SET NX EX 原子操作（PITFALLS #6 防双击 race）。
     """
     from __future__ import annotations
     import logging
     from uuid import UUID
     from redis.asyncio import Redis

     logger = logging.getLogger(__name__)

     def _jti_key(jti: str) -> str:
         return f"jti:{jti}"

     def _node_set_key(node_id: str | UUID) -> str:
         return f"node:jti:{node_id}"

     async def consume_jti(redis: Redis, jti: str, *, ttl_seconds: int) -> bool:
         """SET NX EX 原子消费。

         Returns:
             True — 首次消费（token 有效）
             False — 已被消费（重放 / 双击 race 失败）
         """
         result = await redis.set(_jti_key(jti), "1", nx=True, ex=ttl_seconds)
         return result is True or result == "OK"  # redis-py 5.x set NX returns True 或 None

     async def is_jti_consumed(redis: Redis, jti: str) -> bool:
         """仅检查不消费 — 用于 debug / 测试。"""
         return await redis.exists(_jti_key(jti)) > 0

     async def register_node_token(
         redis: Redis,
         node_id: str | UUID,
         jti: str,
         *,
         ttl_seconds: int,
     ) -> None:
         """把 jti 注册到 node SET — 节点状态变更时方便批量失效。

         用 pipeline 防止 SET 加成员后忘记 EXPIRE（永驻泄漏）。
         """
         key = _node_set_key(node_id)
         async with redis.pipeline(transaction=False) as pipe:
             await pipe.sadd(key, jti)
             await pipe.expire(key, ttl_seconds)
             await pipe.execute()

     async def invalidate_node_tokens(
         redis: Redis,
         node_id: str | UUID,
     ) -> int:
         """节点状态变更时批量失效该节点所有未消费 token。

         Returns:
             删除的 jti 数量
         """
         set_key = _node_set_key(node_id)
         jtis = await redis.smembers(set_key)
         if not jtis:
             return 0
         keys_to_delete = [_jti_key(j) for j in jtis] + [set_key]
         deleted = await redis.delete(*keys_to_delete)
         logger.info(
             "[jti_service] invalidated %d tokens for node=%s",
             len(jtis),
             node_id,
         )
         return len(jtis)
     ```

  3. `backend/src/offboarding_flow/api/health.py`：把 redis check 从 `not_checked` 改成真 ping
     - 替换第 50-52 行：
       ```python
       # Redis 检查（Phase 3 接入真 ping）
       try:
           from offboarding_flow.auth.redis_client import get_redis
           redis = await get_redis()
           await redis.ping()
           components["redis"] = "ok"
       except Exception as e:
           components["redis"] = f"fail: {type(e).__name__}"
           logger.warning("[health] redis check failed: %s", e)
       ```
  </action>
  <verify>
  ```bash
  cd backend && uv run python -c "
  import asyncio
  from offboarding_flow.auth.redis_client import get_redis, dispose_redis
  from offboarding_flow.auth.jti_service import consume_jti, is_jti_consumed, register_node_token, invalidate_node_tokens

  async def main():
      r = await get_redis()
      ok = await r.ping()
      print('redis ping:', ok)
      await dispose_redis()

  asyncio.run(main())
  " 2>&1 | tail -5
  ```
  期望最后一行 `redis ping: True`（前提：本地或 docker offboarding-redis:6380 运行）。
  如未运行 redis，跳过此命令，仅 import 验证：
  ```bash
  cd backend && uv run python -c "
  from offboarding_flow.auth.jti_service import consume_jti, register_node_token, invalidate_node_tokens, is_jti_consumed
  from offboarding_flow.auth.redis_client import get_redis, dispose_redis
  print('imports ok')
  "
  ```
  </verify>
  <done>
  - redis_client.py + jti_service.py 两个文件创建，函数签名匹配 interfaces 块
  - api/health.py 第 50-52 行替换为真 ping
  - 4 个 jti_service 公共函数全部可 import
  </done>
</task>

<task type="auto">
  <name>Task 2: 加 redis_client fixture + 写 10+ 集成测试（含 asyncio.gather 并发测试）</name>
  <files>
    backend/tests/conftest.py
    backend/tests/auth/test_jti_service.py
    backend/tests/auth/test_jti_service_concurrency.py
  </files>
  <action>
  1. `backend/tests/conftest.py`：扩展（在现有 fixture 后追加，不删除已有）
     ```python
     # ===== Phase 3 新增 redis fixture =====
     import os
     import pytest_asyncio
     from redis.asyncio import Redis

     def _redis_test_url() -> str:
         """优先用 REDIS_TEST_URL 环境变量，否则本地 6380 + db 1（避免污染 db 0）。"""
         return os.environ.get("REDIS_TEST_URL", "redis://localhost:6380/1")

     @pytest_asyncio.fixture
     async def redis_client():
         """function scope — 每个测试干净的 db；用前 flushdb 用后断开。"""
         client = Redis.from_url(_redis_test_url(), decode_responses=True)
         try:
             await client.flushdb()
             yield client
         finally:
             await client.flushdb()
             await client.aclose()
     ```
     注意：保留原有 event_loop_policy / anyio_backend fixture 不动。

  2. `backend/tests/auth/test_jti_service.py`：8+ 用例
     ```python
     """jti_service 集成测试 — 用真 Redis（不 mock）。

     约定：REDIS_TEST_URL=redis://localhost:6380/1 或 CI 注入；本地 docker compose -f docker-compose.dev.yml up redis -d。
     """
     import asyncio
     import uuid
     import pytest
     from offboarding_flow.auth.jti_service import (
         consume_jti, is_jti_consumed,
         register_node_token, invalidate_node_tokens,
     )

     # 标记：本文件 = 集成测试（需真 Redis），不带 e2e marker 但用 redis_client fixture
     pytestmark = pytest.mark.asyncio

     async def test_consume_jti_first_time_returns_true(redis_client):
         jti = uuid.uuid4().hex
         ok = await consume_jti(redis_client, jti, ttl_seconds=60)
         assert ok is True

     async def test_consume_jti_replay_returns_false(redis_client):
         jti = uuid.uuid4().hex
         first = await consume_jti(redis_client, jti, ttl_seconds=60)
         second = await consume_jti(redis_client, jti, ttl_seconds=60)
         assert first is True
         assert second is False

     async def test_is_jti_consumed_reports_state(redis_client):
         jti = uuid.uuid4().hex
         assert (await is_jti_consumed(redis_client, jti)) is False
         await consume_jti(redis_client, jti, ttl_seconds=60)
         assert (await is_jti_consumed(redis_client, jti)) is True

     async def test_different_jti_independent(redis_client):
         a = uuid.uuid4().hex
         b = uuid.uuid4().hex
         await consume_jti(redis_client, a, ttl_seconds=60)
         result_b = await consume_jti(redis_client, b, ttl_seconds=60)
         assert result_b is True  # b 不受 a 影响

     async def test_consume_jti_ttl_applied(redis_client):
         jti = uuid.uuid4().hex
         await consume_jti(redis_client, jti, ttl_seconds=2)
         await asyncio.sleep(2.5)
         # 过期后再消费应当 True（key 已不存在）
         ok = await consume_jti(redis_client, jti, ttl_seconds=60)
         assert ok is True

     async def test_register_node_token_adds_to_set(redis_client):
         node_id = uuid.uuid4()
         jti = uuid.uuid4().hex
         await register_node_token(redis_client, node_id, jti, ttl_seconds=60)
         members = await redis_client.smembers(f"node:jti:{node_id}")
         assert jti in members

     async def test_register_node_token_sets_ttl(redis_client):
         node_id = uuid.uuid4()
         jti = uuid.uuid4().hex
         await register_node_token(redis_client, node_id, jti, ttl_seconds=60)
         ttl = await redis_client.ttl(f"node:jti:{node_id}")
         assert 0 < ttl <= 60

     async def test_invalidate_node_tokens_clears_all(redis_client):
         node_id = uuid.uuid4()
         jtis = [uuid.uuid4().hex for _ in range(3)]
         for j in jtis:
             await consume_jti(redis_client, j, ttl_seconds=60)
             await register_node_token(redis_client, node_id, j, ttl_seconds=60)
         deleted = await invalidate_node_tokens(redis_client, node_id)
         assert deleted == 3
         # 节点 SET 不存在
         assert (await redis_client.exists(f"node:jti:{node_id}")) == 0
         # 全部 jti key 删除
         for j in jtis:
             assert (await redis_client.exists(f"jti:{j}")) == 0
         # 失效后再消费这些 jti 全部 True（key 已不存在）
         for j in jtis:
             ok = await consume_jti(redis_client, j, ttl_seconds=60)
             assert ok is True

     async def test_invalidate_node_tokens_empty_set_returns_zero(redis_client):
         node_id = uuid.uuid4()
         deleted = await invalidate_node_tokens(redis_client, node_id)
         assert deleted == 0

     async def test_invalidate_isolates_to_node(redis_client):
         node_a = uuid.uuid4()
         node_b = uuid.uuid4()
         jti_a = uuid.uuid4().hex
         jti_b = uuid.uuid4().hex
         await consume_jti(redis_client, jti_a, ttl_seconds=60)
         await register_node_token(redis_client, node_a, jti_a, ttl_seconds=60)
         await consume_jti(redis_client, jti_b, ttl_seconds=60)
         await register_node_token(redis_client, node_b, jti_b, ttl_seconds=60)
         await invalidate_node_tokens(redis_client, node_a)
         # node_b 的 jti 不受影响
         assert (await redis_client.exists(f"jti:{jti_b}")) == 1
     ```

  3. `backend/tests/auth/test_jti_service_concurrency.py`：并发竞争（这是 PITFALLS #6 的关键保证）
     ```python
     """asyncio.gather 并发 race condition 测试 — PITFALLS #6 核心保证。"""
     import asyncio
     import uuid
     import pytest
     from offboarding_flow.auth.jti_service import consume_jti

     pytestmark = pytest.mark.asyncio

     async def test_concurrent_consume_same_jti_only_one_wins(redis_client):
         """SET NX EX 原子保证：N 并发同一 jti 只有 1 个 True，N-1 个 False。"""
         jti = uuid.uuid4().hex
         tasks = [consume_jti(redis_client, jti, ttl_seconds=60) for _ in range(20)]
         results = await asyncio.gather(*tasks)
         winners = sum(1 for r in results if r is True)
         losers = sum(1 for r in results if r is False)
         assert winners == 1, f"应当只有 1 个胜出，实际 {winners}"
         assert losers == 19, f"应当 19 个失败，实际 {losers}"

     async def test_concurrent_consume_different_jtis_all_win(redis_client):
         """N 个不同 jti 并发消费全部应 True。"""
         jtis = [uuid.uuid4().hex for _ in range(20)]
         tasks = [consume_jti(redis_client, j, ttl_seconds=60) for j in jtis]
         results = await asyncio.gather(*tasks)
         assert all(r is True for r in results), "全部不同 jti 应当都成功"
     ```
  </action>
  <verify>
  ```bash
  # 前置：本地启 Redis
  docker compose -f docker-compose.dev.yml up offboarding-redis -d || docker run -d --name offboarding-redis-test -p 6380:6379 redis:7-alpine

  cd backend && REDIS_TEST_URL=redis://localhost:6380/1 uv run pytest tests/auth/test_jti_service.py tests/auth/test_jti_service_concurrency.py -v
  ```
  期望：10+ 用例全 PASS。特别注意 test_concurrent_consume_same_jti_only_one_wins — 这是本 plan 的核心断言。

  ```bash
  cd backend && REDIS_TEST_URL=redis://localhost:6380/1 uv run pytest tests/auth/test_jti_service.py tests/auth/test_jti_service_concurrency.py --cov=src/offboarding_flow/auth/jti_service --cov=src/offboarding_flow/auth/redis_client --cov-report=term-missing
  ```
  期望覆盖率 ≥ 80%。
  </verify>
  <done>
  - 10+ 集成测试全 PASS（含 20 并发同 jti 仅 1 胜出）
  - jti_service.py + redis_client.py 覆盖率 ≥ 80%
  - api/health.py 真 ping 生效（uvicorn 起来后 GET /api/health components.redis = 'ok'）
  - 测试用 REDIS_TEST_URL=...:6380/1 隔离生产 db 0
  </done>
</task>

</tasks>

<verification>
- `cd backend && uv run pytest tests/auth/test_jti_service.py tests/auth/test_jti_service_concurrency.py -v` 全 PASS
- `cd backend && uv run ruff check src/offboarding_flow/auth/jti_service.py src/offboarding_flow/auth/redis_client.py` 无 error
- `cd backend && uv run mypy src/offboarding_flow/auth/jti_service.py src/offboarding_flow/auth/redis_client.py` 无 error
- `grep -r "fakeredis\|mock_redis\|MagicMock" backend/tests/auth/` 无任何匹配（禁止 mock Redis 约束）
- 启动 uvicorn 后 `curl http://localhost:8000/api/health` 返回 `components.redis = 'ok'`（前提 redis 跑着）
</verification>

<success_criteria>
- AUTH-03 部分覆盖：jti 一次性消费（Redis SET NX EX）+ 节点 token 批量失效 已就绪（exchange 端点由 Plan 03 完成，node_service hook 由 Plan 04 完成）
- Wave 1 并行不冲突 Plan 01（无共享文件，无类型依赖）
- 集成测试用真 Redis 不 mock（CLAUDE.md §2.3 + 用户 memory）
- 中文 commit + 中文注释
</success_criteria>

<output>
完成后创建 `.planning/phases/03-auth-deeplink/03-02-SUMMARY.md`。
</output>
