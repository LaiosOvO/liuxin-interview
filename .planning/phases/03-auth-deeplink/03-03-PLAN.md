---
phase: 03-auth-deeplink
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - backend/src/offboarding_flow/auth/cookie.py
  - backend/src/offboarding_flow/auth/role_router.py
  - backend/src/offboarding_flow/auth/session_service.py
  - backend/src/offboarding_flow/auth/deps.py
  - backend/src/offboarding_flow/api/auth.py
  - backend/src/offboarding_flow/api/deps.py
  - backend/src/offboarding_flow/api/errors.py
  - backend/src/offboarding_flow/main.py
  - backend/src/offboarding_flow/state_store/repositories.py
  - backend/tests/auth/test_cookie.py
  - backend/tests/auth/test_role_router.py
  - backend/tests/auth/test_exchange_endpoint.py
autonomous: true
requirements:
  - AUTH-02
  - AUTH-04
must_haves:
  truths:
    - "auth/cookie.py 暴露 set_session_cookie(response, token, settings) / clear_session_cookie(response, settings)"
    - "set_session_cookie 调用：httponly=True / samesite='lax' / secure=(app_mode=='prod' and https_enabled) / max_age=session_expiry_hours*3600 / path='/'"
    - "auth/role_router.py 暴露 resolve_redirect(role, flow_id, node_id) 返回字符串路径；6 角色全覆盖"
    - "auth/session_service.py 暴露 exchange_token(token, redis, session) -> ExchangeResult；执行 7 步校验链"
    - "session_service 在校验失败时统一抛 AuthError（不暴露具体原因给用户）；server log 写完整 reason"
    - "auth/deps.py 暴露 get_redis_dep / get_current_user(request) -> SessionPayload / require_role(*roles)"
    - "POST /api/auth/exchange 接收 {token}，返回 {success, data: {redirect_to, role, name}}，副作用：set HttpOnly cookie"
    - "POST /api/auth/logout 清 cookie 返回 200"
    - "状态机：跨 flow_id token / 已 done 节点 token / 角色不匹配 / sub 不匹配 → 全部 401，envelope.error='鉴权失败'"
    - "main.py lifespan 在 shutdown 调 dispose_redis()"
    - "state_store/repositories.py UserRepository 新增 get_by_username(username) 方法（如不存在）"
    - "api/errors.py 注册 AuthError 全局 handler 返回 envelope 401"
    - "test_exchange_endpoint.py ≥ 8 用例覆盖：正常 200 / jti 重放 401 / 过期 token 401 / 跨 flow_id 401 / 跨 role 401 / sub 与 node.assignee 不匹配 401 / 节点已 done 401 / 篡改签名 401"
    - "test_cookie.py + test_role_router.py 共 ≥ 8 单元测试"
    - "覆盖率 ≥ 80%"
  artifacts:
    - backend/src/offboarding_flow/auth/cookie.py
    - backend/src/offboarding_flow/auth/role_router.py
    - backend/src/offboarding_flow/auth/session_service.py
    - backend/src/offboarding_flow/auth/deps.py
    - backend/src/offboarding_flow/api/auth.py
    - backend/tests/auth/test_cookie.py
    - backend/tests/auth/test_role_router.py
    - backend/tests/auth/test_exchange_endpoint.py
  key_links:
    - "session_service.exchange_token 调 jwt_service.decode → jti_service.consume_jti → node_repo.get → user_repo.get_by_username → jti_service.register_node_token → jwt_service.encode_session → role_router.resolve_redirect"
    - "api/auth.py POST /api/auth/exchange 用 Depends 注入 redis + session；catch AuthError → 让全局 handler 返回 401 envelope"
    - "api/errors.py 注册 register_exception_handlers 时加 AuthError handler：return JSONResponse(401, content=err('鉴权失败'))"
    - "main.py 修改 lifespan shutdown 顺序：dispose_graph → dispose_checkpointer → dispose_engine → dispose_redis"
    - "api/deps.py 新增 get_redis_dep() 用 from offboarding_flow.auth.redis_client import get_redis 实现"
    - "auth/deps.py get_current_user 从 request.cookies[SESSION_COOKIE_NAME] 读取；失败抛 AuthError"
---

<objective>
落地 POST /api/auth/exchange 完整端点 — 串起 Plan 01 的 JWT 编解码 + Plan 02 的 jti 一次性消费，加上 cookie 签发 + role 校验 + 三元组绑定 + FastAPI Depends 体系。

Purpose: 这是 Phase 3 主线 — 邮件链接点击后实际换 cookie 的端点。Plan 04 会基于本 plan 加 E2E race condition / role isolation / cross-flow rejection 测试 + node_service hook。

Output: 可独立 curl 调用的 /api/auth/exchange + /api/auth/logout 端点，8+ 集成测试 + 8+ 单元测试全部 PASS。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-auth-deeplink/03-CONTEXT.md
@.planning/phases/03-auth-deeplink/03-01-PLAN.md
@.planning/phases/03-auth-deeplink/03-02-PLAN.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@PRD.md
@backend/src/offboarding_flow/main.py
@backend/src/offboarding_flow/api/deps.py
@backend/src/offboarding_flow/api/errors.py
@backend/src/offboarding_flow/state_store/repositories.py
@backend/src/offboarding_flow/state_store/models.py
@backend/src/offboarding_flow/state_store/enums.py

<interfaces>
<!-- Plan 01 已交付：JWTPayload, SessionPayload, encode, decode, encode_session, decode_session, build_deep_link, ExpiredTokenError, InvalidTokenError, TokenAlreadyConsumedError, AuthError -->

<!-- Plan 02 已交付：get_redis(), dispose_redis(), consume_jti, register_node_token, invalidate_node_tokens, is_jti_consumed -->

<!-- 本 plan 新增的接口 -->

# auth/cookie.py
def set_session_cookie(response: Response, session_token: str, settings: Settings) -> None: ...
def clear_session_cookie(response: Response, settings: Settings) -> None: ...

# auth/role_router.py
def resolve_redirect(role: str, flow_id: UUID, node_id: UUID) -> str: ...

# auth/session_service.py
@dataclass(frozen=True)
class ExchangeResult:
    session_token: str   # JWT for cookie
    redirect_to: str     # 角色对应前端路径
    role: str
    name: str            # user.display_name 或 username

async def exchange_token(token: str, redis: Redis, session: AsyncSession) -> ExchangeResult: ...

# auth/deps.py
async def get_redis_dep() -> Redis: ...  # FastAPI Depends 用
async def get_current_user(request: Request, settings: Settings = Depends(get_settings)) -> SessionPayload: ...
def require_role(*roles: str) -> Callable: ...  # 返回 Depends 工厂

# api/auth.py
@router.post("/auth/exchange")
async def exchange(body: ExchangeRequest, response: Response, redis = Depends(get_redis_dep), session = Depends(get_db_session)) -> dict: ...

@router.post("/auth/logout")
async def logout(response: Response) -> dict: ...

class ExchangeRequest(BaseModel):
    token: str

<!-- 现有 NodeStatus -->
class NodeStatus(StrEnum):
    PENDING = "pending"
    WAITING_HUMAN = "waiting_human"
    IN_REVIEW = "in_review"
    DONE = "done"
    REJECTED = "rejected"
    RETURNED = "returned"

<!-- 现有 Role -->
class Role(StrEnum):
    APPLICANT = "applicant"
    MANAGER = "manager"
    HR = "hr"
    IT_ADMIN = "it_admin"
    FINANCE = "finance"
    LEGAL = "legal"

<!-- node_state 关键字段（已有，本 plan 读不写） -->
node_states.assignee: str  # 节点责任人 username
node_states.status: str    # NodeStatus value
node_states.flow_id: UUID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写 cookie + role_router + session_service + auth/deps 四个 lib 模块</name>
  <files>
    backend/src/offboarding_flow/auth/cookie.py
    backend/src/offboarding_flow/auth/role_router.py
    backend/src/offboarding_flow/auth/session_service.py
    backend/src/offboarding_flow/auth/deps.py
    backend/src/offboarding_flow/state_store/repositories.py
    backend/src/offboarding_flow/auth/__init__.py
  </files>
  <action>
  1. `backend/src/offboarding_flow/auth/cookie.py`：
     ```python
     """Session cookie 签发/清理 — PITFALLS #12 锁定参数。"""
     from __future__ import annotations
     from fastapi import Response
     from offboarding_flow.config import Settings

     def set_session_cookie(response: Response, session_token: str, settings: Settings) -> None:
         """签发 HttpOnly + SameSite=Lax cookie。

         约定（PITFALLS #12）：
         - SameSite=Lax 不能 Strict（邮件跨站跳会丢 cookie）
         - secure 仅生产 HTTPS 才开（内网 HTTP 部署关）
         """
         response.set_cookie(
             key=settings.session_cookie_name,
             value=session_token,
             httponly=True,
             secure=(settings.app_mode == "prod" and settings.https_enabled),
             samesite="lax",
             max_age=settings.session_expiry_hours * 3600,
             path="/",
         )

     def clear_session_cookie(response: Response, settings: Settings) -> None:
         """清除 cookie — 同 key/path 参数。"""
         response.delete_cookie(
             key=settings.session_cookie_name,
             path="/",
         )
     ```

  2. `backend/src/offboarding_flow/auth/role_router.py`：
     ```python
     """角色 → 前端 redirect 路径解析（CONTEXT §D9）。

     Phase 3 仅返回 URL 字符串；Phase 5 落地真实前端页面。
     """
     from __future__ import annotations
     from uuid import UUID

     _PATHS: dict[str, str] = {
         "applicant": "/flow/{flow_id}/applicant",
         "manager":   "/flow/{flow_id}/node/{node_id}/manager",
         "hr":        "/flow/{flow_id}/node/{node_id}/hr",
         "it_admin":  "/flow/{flow_id}/node/{node_id}/it",
         "finance":   "/flow/{flow_id}/node/{node_id}/finance",
         "legal":     "/flow/{flow_id}/node/{node_id}/legal",
     }

     def resolve_redirect(role: str, flow_id: UUID, node_id: UUID) -> str:
         """根据 role 返回对应前端路径。未知 role 用默认 applicant 视图。"""
         template = _PATHS.get(role, _PATHS["applicant"])
         return template.format(flow_id=flow_id, node_id=node_id)
     ```

  3. `backend/src/offboarding_flow/state_store/repositories.py`：扩展 UserRepository 加 get_by_username
     - 用 Read 工具查现有 UserRepository 类的位置
     - 在 UserRepository 内新增：
       ```python
       async def get_by_username(self, username: str) -> User | None:
           """按 username 查 user。"""
           stmt = select(User).where(User.username == username)
           result = await self.session.execute(stmt)
           return result.scalar_one_or_none()
       ```
     如果已存在 get_by_username 则跳过此步。

  4. `backend/src/offboarding_flow/auth/session_service.py`：核心 exchange_token
     ```python
     """exchange_token 7 步校验链 — PRD §6.2.2 锁定。"""
     from __future__ import annotations
     import logging
     from dataclasses import dataclass
     from redis.asyncio import Redis
     from sqlalchemy.ext.asyncio import AsyncSession
     from offboarding_flow.config import get_settings
     from offboarding_flow.state_store.enums import NodeStatus
     from offboarding_flow.state_store.repositories import NodeRepository, UserRepository
     from . import jwt_service
     from . import jti_service
     from . import role_router
     from .errors import AuthError, TokenAlreadyConsumedError, InvalidTokenError
     from .schemas import JWTPayload

     logger = logging.getLogger(__name__)

     @dataclass(frozen=True)
     class ExchangeResult:
         session_token: str
         redirect_to: str
         role: str
         name: str

     async def exchange_token(
         token: str,
         redis: Redis,
         session: AsyncSession,
     ) -> ExchangeResult:
         """7 步校验链 — 任一失败抛 AuthError（reason 字段区分细节）。

         全部失败用统一 detail='鉴权失败' 不暴露具体原因；log 写 reason 便于排查。
         """
         settings = get_settings()

         # 1. JWT decode + 验签 + exp 校验（pyjwt 内置；jwt_service 转 AuthError）
         payload: JWTPayload = jwt_service.decode(token)
         jti_prefix = payload.jti[:8]
         logger.info(
             "[auth.exchange] start jti=%s... sub=%s flow=%s node=%s",
             jti_prefix, payload.sub, payload.flow_id, payload.node_id,
         )

         # 2. SET NX EX 原子消费 jti（必须放在 node 校验之前 — 防双击 race）
         consumed = await jti_service.consume_jti(
             redis, payload.jti, ttl_seconds=settings.token_expiry_hours * 3600,
         )
         if not consumed:
             logger.warning("[auth.exchange] reject reason=jti_replay jti=%s...", jti_prefix)
             raise TokenAlreadyConsumedError()

         # 3. node 校验：存在 + 同 flow_id + status=waiting_human + assignee == sub
         node_repo = NodeRepository(session)
         node = await node_repo.get(payload.node_id)
         if node is None:
             logger.warning("[auth.exchange] reject reason=node_not_found jti=%s...", jti_prefix)
             raise AuthError(reason="node_not_found")
         if str(node.flow_id) != str(payload.flow_id):
             logger.warning(
                 "[auth.exchange] reject reason=cross_flow jti=%s... payload.flow=%s node.flow=%s",
                 jti_prefix, payload.flow_id, node.flow_id,
             )
             raise AuthError(reason="cross_flow")
         if node.status != NodeStatus.WAITING_HUMAN.value:
             logger.warning(
                 "[auth.exchange] reject reason=node_not_waiting jti=%s... status=%s",
                 jti_prefix, node.status,
             )
             raise AuthError(reason="node_not_waiting")
         if node.assignee != payload.sub:
             logger.warning(
                 "[auth.exchange] reject reason=sub_mismatch jti=%s... payload.sub=%s assignee=%s",
                 jti_prefix, payload.sub, node.assignee,
             )
             raise AuthError(reason="sub_mismatch")

         # 4. user 校验：存在 + role 一致
         user_repo = UserRepository(session)
         user = await user_repo.get_by_username(payload.sub)
         if user is None:
             logger.warning("[auth.exchange] reject reason=user_not_found jti=%s...", jti_prefix)
             raise AuthError(reason="user_not_found")
         user_role = user.role.value if hasattr(user.role, "value") else str(user.role)
         if user_role != payload.role:
             logger.warning(
                 "[auth.exchange] reject reason=role_mismatch jti=%s... payload.role=%s user.role=%s",
                 jti_prefix, payload.role, user_role,
             )
             raise AuthError(reason="role_mismatch")

         # 5. 注册 node:jti:{id} SET 便于后续失效
         await jti_service.register_node_token(
             redis, payload.node_id, payload.jti,
             ttl_seconds=settings.token_expiry_hours * 3600,
         )

         # 6. 签 session cookie payload
         session_token = jwt_service.encode_session(payload)

         # 7. 返回 redirect_to
         redirect_to = role_router.resolve_redirect(
             payload.role, payload.flow_id, payload.node_id,
         )
         display_name = (
             user.display_name if hasattr(user, "display_name") and user.display_name
             else user.username
         )

         logger.info(
             "[auth.exchange] success jti=%s... sub=%s redirect=%s",
             jti_prefix, payload.sub, redirect_to,
         )
         return ExchangeResult(
             session_token=session_token,
             redirect_to=redirect_to,
             role=payload.role,
             name=display_name,
         )
     ```

  5. `backend/src/offboarding_flow/auth/deps.py`：FastAPI Depends
     ```python
     """FastAPI Depends 体系 — get_current_user + require_role。"""
     from __future__ import annotations
     import logging
     from collections.abc import Callable
     from fastapi import Depends, Request
     from redis.asyncio import Redis
     from offboarding_flow.config import Settings, get_settings
     from . import jwt_service
     from .errors import AuthError
     from .redis_client import get_redis
     from .schemas import SessionPayload

     logger = logging.getLogger(__name__)

     async def get_redis_dep() -> Redis:
         """FastAPI Depends — 复用 redis_client 单例。"""
         return await get_redis()

     async def get_current_user(
         request: Request,
         settings: Settings = Depends(get_settings),
     ) -> SessionPayload:
         """从 cookie 取 session token → decode → 返回 payload。

         失败抛 AuthError（401 统一 envelope）。
         """
         cookie_value = request.cookies.get(settings.session_cookie_name)
         if not cookie_value:
             raise AuthError(reason="no_cookie")
         payload = jwt_service.decode_session(cookie_value)
         return payload

     def require_role(*roles: str) -> Callable:
         """工厂：返回一个 Depends，校验当前 user 角色在 roles 中。"""
         async def _check(user: SessionPayload = Depends(get_current_user)) -> SessionPayload:
             if user.role not in roles:
                 logger.warning(
                     "[auth.deps] role check fail required=%s actual=%s sub=%s",
                     roles, user.role, user.sub,
                 )
                 raise AuthError(reason="role_not_allowed")
             return user
         return _check
     ```

  6. `backend/src/offboarding_flow/auth/__init__.py`：扩展 export
     ```python
     # 已有的 export 之外加：
     from .cookie import set_session_cookie, clear_session_cookie
     from .role_router import resolve_redirect
     from .session_service import ExchangeResult, exchange_token
     from .deps import get_redis_dep, get_current_user, require_role

     __all__ = [
         # ...已有...
         "set_session_cookie", "clear_session_cookie",
         "resolve_redirect",
         "ExchangeResult", "exchange_token",
         "get_redis_dep", "get_current_user", "require_role",
     ]
     ```
  </action>
  <verify>
  ```bash
  cd backend && uv run python -c "
  from offboarding_flow.auth import (
      set_session_cookie, clear_session_cookie,
      resolve_redirect, ExchangeResult, exchange_token,
      get_redis_dep, get_current_user, require_role,
  )
  print('imports ok')
  print('resolve_redirect manager:', resolve_redirect('manager', __import__('uuid').uuid4(), __import__('uuid').uuid4()))
  "
  ```
  </verify>
  <done>
  - 4 个 lib 模块创建 + UserRepository.get_by_username 存在
  - exchange_token 全签名匹配 interfaces 块
  - role_router.resolve_redirect 6 角色全有 path
  - auth/__init__.py export 全部新增符号
  </done>
</task>

<task type="auto">
  <name>Task 2: 写 POST /api/auth/exchange + /api/auth/logout 端点 + 全局 AuthError handler + lifespan dispose_redis</name>
  <files>
    backend/src/offboarding_flow/api/auth.py
    backend/src/offboarding_flow/api/__init__.py
    backend/src/offboarding_flow/api/errors.py
    backend/src/offboarding_flow/api/deps.py
    backend/src/offboarding_flow/main.py
  </files>
  <action>
  1. `backend/src/offboarding_flow/api/auth.py`（新文件）：
     ```python
     """POST /api/auth/exchange — 邮件深链 token 换 HttpOnly cookie。

     PRD §6.2.2 锁定 7 步流程；CONTEXT §D8 详细实现。
     """
     from __future__ import annotations
     import logging
     from typing import Annotated
     from fastapi import APIRouter, Depends, Response
     from pydantic import BaseModel, Field
     from redis.asyncio import Redis
     from sqlalchemy.ext.asyncio import AsyncSession
     from offboarding_flow.api.deps import SessionDep
     from offboarding_flow.api.envelope import ok
     from offboarding_flow.auth import cookie as cookie_helper
     from offboarding_flow.auth import session_service
     from offboarding_flow.auth.deps import get_redis_dep
     from offboarding_flow.config import get_settings

     logger = logging.getLogger(__name__)

     router = APIRouter(prefix="/api/auth", tags=["auth"])

     class ExchangeRequest(BaseModel):
         token: str = Field(..., min_length=20)  # JWT 最小长度

     @router.post("/exchange")
     async def exchange(
         body: ExchangeRequest,
         response: Response,
         redis: Annotated[Redis, Depends(get_redis_dep)],
         session: SessionDep,
     ) -> dict:
         """换 cookie。任何失败统一 401 envelope。"""
         settings = get_settings()
         result = await session_service.exchange_token(body.token, redis, session)
         cookie_helper.set_session_cookie(response, result.session_token, settings)
         return ok({
             "redirect_to": result.redirect_to,
             "role": result.role,
             "name": result.name,
         })

     @router.post("/logout")
     async def logout(response: Response) -> dict:
         """清 cookie — 不需要鉴权，前端调即可清空 session。"""
         settings = get_settings()
         cookie_helper.clear_session_cookie(response, settings)
         return ok({"message": "logged out"})
     ```

  2. `backend/src/offboarding_flow/api/__init__.py`：扩展
     ```python
     from .auth import router as auth_router
     from .flows import router as flows_router
     from .health import router as health_router
     from .nodes import router as nodes_router

     __all__ = ["auth_router", "flows_router", "health_router", "nodes_router"]
     ```

  3. `backend/src/offboarding_flow/api/errors.py`：注册 AuthError handler
     - 用 Read 工具查现有 register_exception_handlers 函数
     - 在其内追加：
       ```python
       from offboarding_flow.auth.errors import AuthError

       @app.exception_handler(AuthError)
       async def auth_error_handler(request, exc: AuthError) -> JSONResponse:
           # log 详细 reason，对外统一 detail
           logger.warning(
               "[auth_error] reason=%s path=%s",
               exc.reason, request.url.path,
           )
           return JSONResponse(
               status_code=exc.status_code,
               content={"success": False, "data": None, "error": exc.detail, "meta": None},
           )
       ```
     注意：把 `from fastapi.responses import JSONResponse` 加 import（如果没有）；`logger = logging.getLogger(__name__)` 已经存在。

  4. `backend/src/offboarding_flow/api/deps.py`：新增 get_redis_dep 转发（保持 api 层简洁）
     - 文件底部加：
       ```python
       # Phase 3 新增 — redis 在 auth/deps.py 实现，api/deps.py 转发
       from offboarding_flow.auth.deps import get_redis_dep
       ```
     注意：实际转发即 import；保持 api/deps.py 作为 DI 单一入口的约定。

  5. `backend/src/offboarding_flow/main.py`：
     a. lifespan shutdown 加 dispose_redis
        - 在 `dispose_engine` 之后加：
          ```python
          try:
              from offboarding_flow.auth.redis_client import dispose_redis
              await dispose_redis()
          except Exception as e:
              logger.warning("[lifespan] dispose_redis error: %s", e)
          ```
     b. create_app 加 auth_router 挂载
        - 在现有的 flows_router / nodes_router try-except 块之外（或之内）加：
          ```python
          try:
              from offboarding_flow.api.auth import router as auth_router
              app.include_router(auth_router)
              logger.info("[create_app] mounted auth_router")
          except ImportError as e:
              logger.info("[create_app] auth router not yet implemented: %s", e)
          ```
     c. lifespan startup 不显式 init redis（懒加载首次 get_redis 触发）— 不改 startup 段
  </action>
  <verify>
  ```bash
  cd backend && uv run python -c "
  from offboarding_flow.main import create_app
  app = create_app()
  routes = [r.path for r in app.routes if hasattr(r, 'path')]
  print('routes:')
  for r in sorted(routes):
      print(' ', r)
  assert '/api/auth/exchange' in routes
  assert '/api/auth/logout' in routes
  print('auth routes mounted OK')
  "
  ```
  期望输出含 `/api/auth/exchange` 和 `/api/auth/logout`。
  </verify>
  <done>
  - POST /api/auth/exchange 路由可注册
  - POST /api/auth/logout 路由可注册
  - AuthError 全局 handler 注册（不依赖 endpoint 显式 catch）
  - main.py lifespan shutdown 多一段 dispose_redis
  </done>
</task>

<task type="auto">
  <name>Task 3: 写 cookie + role_router 单元测试 + exchange 端点集成测试（含 8+ 用例）</name>
  <files>
    backend/tests/auth/test_cookie.py
    backend/tests/auth/test_role_router.py
    backend/tests/auth/test_exchange_endpoint.py
  </files>
  <action>
  1. `backend/tests/auth/test_cookie.py`：≥ 4 用例
     - test_set_session_cookie_demo_mode：app_mode=demo → secure=False
     - test_set_session_cookie_prod_https_enabled：app_mode=prod + https_enabled=True → secure=True
     - test_set_session_cookie_samesite_lax：set-cookie header 含 SameSite=Lax（不能 Strict）
     - test_clear_session_cookie_emits_delete：用 fastapi Response 验证 set-cookie expires=...

     用 fastapi.Response 直接调本函数，从 response.raw_headers 中找 'set-cookie' 串验证。

  2. `backend/tests/auth/test_role_router.py`：≥ 4 用例
     - test_resolve_redirect_manager / hr / it_admin / applicant 4 个角色
     - test_resolve_redirect_unknown_role_fallback_to_applicant
     - 验证返回 URL 含 flow_id / node_id UUID 串

  3. `backend/tests/auth/test_exchange_endpoint.py`：≥ 8 用例（用 httpx.AsyncClient + asgi-lifespan + 真 DB + 真 Redis）
     ```python
     """POST /api/auth/exchange 集成测试 — 真 PG + 真 Redis。

     约定（CLAUDE.md §2.3 + 用户 memory）：
     - 不 mock Redis / DB
     - 用 polyfactory 造数据
     - 每个测试 truncate + flushdb
     """
     import asyncio
     import time
     import uuid
     import httpx
     import pytest
     import pytest_asyncio
     from asgi_lifespan import LifespanManager
     from offboarding_flow.auth import encode, JWTPayload
     from offboarding_flow.state_store.enums import NodeStatus, Role

     pytestmark = pytest.mark.asyncio

     # === fixtures ===
     # 复用 Plan 02 的 redis_client；本文件新增 db_session + http_client + sample_node fixture

     @pytest_asyncio.fixture
     async def http_client():
         from offboarding_flow.main import create_app
         app = create_app()
         async with LifespanManager(app):
             async with httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://test") as client:
                 yield client

     @pytest_asyncio.fixture
     async def sample_node_and_user(db_session):
         """造一个 manager 角色 + 对应 user + waiting_human node。"""
         # 用 FlowRepository / NodeRepository / UserRepository 造
         # 简化：直接 SQLAlchemy INSERT
         from offboarding_flow.state_store.models import FlowInstance, NodeState, User
         flow_id = uuid.uuid4()
         user = User(
             id=uuid.uuid4(),
             username="li.si",
             email="li.si@demo.local",
             display_name="李四",
             role=Role.MANAGER.value,
         )
         flow = FlowInstance(id=flow_id, employee_id="zhang.san", status="in_progress")
         node = NodeState(
             id=uuid.uuid4(),
             flow_id=flow_id,
             node_name="manager_review",
             status=NodeStatus.WAITING_HUMAN.value,
             assignee="li.si",
         )
         db_session.add_all([user, flow, node])
         await db_session.commit()
         try:
             yield {"flow": flow, "node": node, "user": user}
         finally:
             await db_session.delete(node)
             await db_session.delete(flow)
             await db_session.delete(user)
             await db_session.commit()

     def _make_token(*, sub, role, flow_id, node_id, jti=None, exp_in=3600, node_name="manager_review"):
         payload = JWTPayload(
             sub=sub, email=f"{sub}@demo.local", role=role,
             flow_id=flow_id, node_id=node_id, node_name=node_name,
             allowed_actions=["advance", "return", "reject"],
             iat=int(time.time()),
             exp=int(time.time()) + exp_in,
             jti=jti or uuid.uuid4().hex,
         )
         return encode(payload), payload

     # === 8 用例 ===

     async def test_exchange_success_sets_cookie_and_returns_200(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="li.si", role="manager",
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
         )
         resp = await http_client.post("/api/auth/exchange", json={"token": token})
         assert resp.status_code == 200
         body = resp.json()
         assert body["success"] is True
         assert body["data"]["role"] == "manager"
         assert body["data"]["redirect_to"].startswith("/flow/")
         # cookie 应当被设置
         assert "offboarding_session" in resp.cookies or any(
             "offboarding_session" in (h[1].decode() if isinstance(h[1], bytes) else h[1])
             for h in resp.headers.raw if h[0].lower() == b"set-cookie"
         )

     async def test_exchange_jti_replay_returns_401(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="li.si", role="manager",
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
         )
         r1 = await http_client.post("/api/auth/exchange", json={"token": token})
         r2 = await http_client.post("/api/auth/exchange", json={"token": token})
         assert r1.status_code == 200
         assert r2.status_code == 401

     async def test_exchange_expired_token_returns_401(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="li.si", role="manager",
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
             exp_in=-60,  # 已过期 1 分钟
         )
         resp = await http_client.post("/api/auth/exchange", json={"token": token})
         assert resp.status_code == 401

     async def test_exchange_cross_flow_id_returns_401(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="li.si", role="manager",
             flow_id=uuid.uuid4(),  # 错的 flow_id
             node_id=sample_node_and_user["node"].id,
         )
         resp = await http_client.post("/api/auth/exchange", json={"token": token})
         assert resp.status_code == 401

     async def test_exchange_cross_role_returns_401(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="li.si", role="hr",  # 错的 role（user 是 manager）
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
         )
         resp = await http_client.post("/api/auth/exchange", json={"token": token})
         assert resp.status_code == 401

     async def test_exchange_sub_mismatch_returns_401(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="wang.wu", role="manager",   # 错的 sub（node assignee 是 li.si）
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
         )
         resp = await http_client.post("/api/auth/exchange", json={"token": token})
         assert resp.status_code == 401

     async def test_exchange_node_already_done_returns_401(http_client, sample_node_and_user, redis_client, db_session):
         # 把 node 改成 done
         sample_node_and_user["node"].status = NodeStatus.DONE.value
         await db_session.commit()
         token, _ = _make_token(
             sub="li.si", role="manager",
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
         )
         resp = await http_client.post("/api/auth/exchange", json={"token": token})
         assert resp.status_code == 401

     async def test_exchange_tampered_signature_returns_401(http_client, sample_node_and_user, redis_client):
         token, _ = _make_token(
             sub="li.si", role="manager",
             flow_id=sample_node_and_user["flow"].id,
             node_id=sample_node_and_user["node"].id,
         )
         # 改最后一个字符破坏签名
         tampered = token[:-1] + ("A" if token[-1] != "A" else "B")
         resp = await http_client.post("/api/auth/exchange", json={"token": tampered})
         assert resp.status_code == 401

     async def test_exchange_logout_clears_cookie(http_client):
         resp = await http_client.post("/api/auth/logout")
         assert resp.status_code == 200
         # set-cookie 应当有 expires=过去时间（清除）
         set_cookie_headers = [
             h[1].decode() if isinstance(h[1], bytes) else h[1]
             for h in resp.headers.raw if h[0].lower() == b"set-cookie"
         ]
         assert any("offboarding_session" in sc for sc in set_cookie_headers)
     ```

  注意：需要 db_session fixture 配 truncate；如 backend/tests/conftest.py 未提供，本 task 必须扩展 conftest 加一个 function-scope db_session（用 init_db + 截表后 yield）。检查现有 conftest.py 和 test_state_store.py 看 db_session 命名约定，复用即可。
  </action>
  <verify>
  ```bash
  # 前置：启 PG + Redis
  docker compose -f docker-compose.dev.yml up offboarding-postgres offboarding-redis -d

  cd backend && REDIS_TEST_URL=redis://localhost:6380/1 \
    POSTGRES_DSN=postgresql+asyncpg://flow:devpassword@localhost:5433/offboarding \
    uv run pytest tests/auth/test_cookie.py tests/auth/test_role_router.py tests/auth/test_exchange_endpoint.py -v
  ```
  期望 ≥ 16 用例全 PASS。

  ```bash
  cd backend && uv run pytest tests/auth/ --cov=src/offboarding_flow/auth --cov=src/offboarding_flow/api/auth --cov-report=term-missing
  ```
  期望覆盖率 ≥ 80%。
  </verify>
  <done>
  - cookie test 4+ PASS / role_router test 4+ PASS / exchange test 8+ PASS
  - AUTH-02 全覆盖（exchange 端点 + cookie 签发）
  - AUTH-04 全覆盖（三元组绑定 + role 校验 + sub 不匹配 + cross_flow）
  - 集成测试用真 PG + 真 Redis 不 mock
  - 覆盖率 ≥ 80%
  </done>
</task>

</tasks>

<verification>
- `cd backend && uv run pytest tests/auth/ -v` 全 PASS
- `cd backend && uv run ruff check src/offboarding_flow/auth/ src/offboarding_flow/api/auth.py` 无 error
- `cd backend && uv run mypy src/offboarding_flow/auth/ src/offboarding_flow/api/auth.py` 无 error（仅本 plan 范围）
- 启动 uvicorn 后 `curl http://localhost:8000/openapi.json | jq '.paths | keys'` 含 `/api/auth/exchange` 与 `/api/auth/logout`
- `grep -r "mock_redis\|fakeredis\|MagicMock" backend/tests/auth/` 无匹配
- `grep -r "JWT_SECRET=" backend/` 仅 .env.example（占位）
</verification>

<success_criteria>
- AUTH-02 完成：POST /api/auth/exchange + HttpOnly cookie + role 路由
- AUTH-04 完成：(flow_id, node_id, sub) 三元组绑定 + role 校验 + cross_flow 拒绝
- 8 个 exchange 端点测试覆盖全部失败路径（jti 重放 / exp / cross flow / cross role / sub 不匹配 / node done / 签名篡改）
- 中文 commit / 注释 / log（log 写 reason 但用户面向 detail 中文）
</success_criteria>

<output>
完成后创建 `.planning/phases/03-auth-deeplink/03-03-SUMMARY.md`。
</output>
