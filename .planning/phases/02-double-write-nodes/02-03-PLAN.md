---
phase: 02-double-write-nodes
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/src/offboarding_flow/flow_engine/nodes/device_return.py
  - backend/src/offboarding_flow/flow_engine/nodes/access_revoke.py
  - backend/src/offboarding_flow/flow_engine/nodes/knowledge_handover.py
  - backend/src/offboarding_flow/flow_engine/nodes/finance_settle.py
  - backend/src/offboarding_flow/flow_engine/nodes/legal_sign.py
  - backend/src/offboarding_flow/flow_engine/nodes/_human_node_factory.py
  - backend/src/offboarding_flow/flow_engine/nodes/__init__.py
  - backend/tests/test_nodes_parallel.py
  - CHANGELOG.md
autonomous: true
requirements:
  - FLOW-01
  - FLOW-04
must_haves:
  truths:
    - "5 个并行节点全部落地：device_return / access_revoke / knowledge_handover / finance_settle / legal_sign"
    - "_human_node_factory 提供统一的人工节点工厂避免重复（每节点 8 行模板）"
    - "PARALLEL_NODES_META 字典暴露每个节点的常量（name / title / description / role）供 graph 拓扑装配使用"
    - "5 节点 interrupt + Command(resume) 单测全 pass"
  artifacts:
    - 5 个节点文件
    - _human_node_factory.py（节点工厂）
    - tests/test_nodes_parallel.py（节点 interrupt 行为单测）
    - CHANGELOG.md（Plan 03 段）
  key_links:
    - "PRD §4.1 5 并行节点"
    - "02-CONTEXT §1 节点函数模板"
---

<objective>
新增 5 个**并行人工节点**，全部走同一套通用节点结构（PRD §4.2）。技术上每个节点函数只有 8 行模板差异（节点名 / 标题 / 说明 / 默认角色不同），因此抽出 `_human_node_factory`：
- device_return（IT 设备归还）
- access_revoke（权限回收）
- knowledge_handover（知识 / 文档交接）
- finance_settle（财务结算）
- legal_sign（法务签字）

Purpose: 完成 10 节点拼图的并行段。每个节点函数 interrupt + Command(resume) 行为与 hr_initial 完全一致，靠 _human_node_factory 复用模板，每节点文件仅 10 行。

Output: 5 节点 + 1 工厂 + 5 节点单测。本 plan **不动 graph.py 拓扑**（拓扑变更在 Plan 05 总装）。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-double-write-nodes/02-CONTEXT.md
@backend/src/offboarding_flow/flow_engine/nodes/manager_review.py
@backend/src/offboarding_flow/flow_engine/nodes/__init__.py
@backend/src/offboarding_flow/flow_engine/state.py

<interfaces>
<!-- _human_node_factory 签名 -->
def make_human_node(
    node_name: str,
    node_title: str,
    node_description: str,
) -> Callable[[OffboardingState], Awaitable[dict]]:
    """生成一个标准的人工节点函数（interrupt + decision 解析 + node_results 追加）。"""

<!-- 5 节点常量 -->
DEVICE_RETURN_NODE_NAME = "device_return"
DEVICE_RETURN_NODE_TITLE = "设备归还"
ACCESS_REVOKE_NODE_NAME = "access_revoke"
ACCESS_REVOKE_NODE_TITLE = "权限回收"
KNOWLEDGE_HANDOVER_NODE_NAME = "knowledge_handover"
KNOWLEDGE_HANDOVER_NODE_TITLE = "知识 / 文档交接"
FINANCE_SETTLE_NODE_NAME = "finance_settle"
FINANCE_SETTLE_NODE_TITLE = "财务结算"
LEGAL_SIGN_NODE_NAME = "legal_sign"
LEGAL_SIGN_NODE_TITLE = "法务签字"

<!-- PARALLEL_NODES_META 字典 -->
PARALLEL_NODES_META: list[tuple[str, str, str, Callable]] = [
    (DEVICE_RETURN_NODE_NAME, DEVICE_RETURN_NODE_TITLE, "请清点离职员工归还的所有设备并填写明细。", device_return_node),
    (ACCESS_REVOKE_NODE_NAME, ACCESS_REVOKE_NODE_TITLE, "请回收离职员工所有系统权限并填写清单。", access_revoke_node),
    (KNOWLEDGE_HANDOVER_NODE_NAME, KNOWLEDGE_HANDOVER_NODE_TITLE, "请确认知识 / 文档已交接完成并填写交接路径。", knowledge_handover_node),
    (FINANCE_SETTLE_NODE_NAME, FINANCE_SETTLE_NODE_TITLE, "请完成离职员工的薪资 / 福利结算并填写明细。", finance_settle_node),
    (LEGAL_SIGN_NODE_NAME, LEGAL_SIGN_NODE_TITLE, "请确认保密 / 竞业 / 离职协议已签署。", legal_sign_node),
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 _human_node_factory + 5 并行节点文件</name>
  <files>backend/src/offboarding_flow/flow_engine/nodes/_human_node_factory.py, backend/src/offboarding_flow/flow_engine/nodes/device_return.py, backend/src/offboarding_flow/flow_engine/nodes/access_revoke.py, backend/src/offboarding_flow/flow_engine/nodes/knowledge_handover.py, backend/src/offboarding_flow/flow_engine/nodes/finance_settle.py, backend/src/offboarding_flow/flow_engine/nodes/legal_sign.py</files>
  <action>
    1. **_human_node_factory.py**：
       ```python
       """人工节点工厂 — 把"interrupt + decision 解析 + node_results 追加"模板复用化。

       生成的节点函数与 manager_review_node / hr_initial_node 行为一致。
       """
       from __future__ import annotations
       import logging
       from collections.abc import Awaitable, Callable
       from datetime import UTC, datetime
       from typing import Any
       from langgraph.types import interrupt
       from offboarding_flow.flow_engine.state import OffboardingState

       logger = logging.getLogger(__name__)

       def make_human_node(
           node_name: str,
           node_title: str,
           node_description: str,
       ) -> Callable[[OffboardingState], Awaitable[dict[str, Any]]]:
           async def _node(state: OffboardingState) -> dict[str, Any]:
               logger.info("[%s] flow=%s interrupt for human decision", node_name, state.get("flow_id"))
               decision = interrupt({
                   "node_name": node_name,
                   "node_title": node_title,
                   "node_description": node_description,
                   "flow_id": state.get("flow_id"),
                   "employee_id": state.get("employee_id"),
               })
               if not isinstance(decision, dict):
                   decision = {"action": "advance", "result_text": str(decision), "actor": "unknown"}
               return {
                   "current_action": decision["action"],
                   "node_results": [{
                       "node_name": node_name,
                       "node_title": node_title,
                       "result_text": decision.get("result_text", ""),
                       "actor": decision.get("actor", "unknown"),
                       "completed_at": decision.get("completed_at", datetime.now(UTC).isoformat()),
                   }],
               }
           _node.__name__ = f"{node_name}_node"
           return _node
       ```
    2. **device_return.py**（其他 4 节点同样模板）：
       ```python
       """device_return 节点：设备归还（并行人工节点之一）。"""
       from offboarding_flow.flow_engine.nodes._human_node_factory import make_human_node

       DEVICE_RETURN_NODE_NAME = "device_return"
       DEVICE_RETURN_NODE_TITLE = "设备归还"
       DEVICE_RETURN_NODE_DESCRIPTION = "请清点离职员工归还的所有设备（笔记本、门禁卡、SIM 卡等）并填写明细。"

       device_return_node = make_human_node(
           DEVICE_RETURN_NODE_NAME,
           DEVICE_RETURN_NODE_TITLE,
           DEVICE_RETURN_NODE_DESCRIPTION,
       )
       ```
    3. **access_revoke.py**：常量 ACCESS_REVOKE_*；description "请回收离职员工的所有系统权限（邮箱、VPN、GitLab、Confluence 等）并填写清单。"
    4. **knowledge_handover.py**：常量 KNOWLEDGE_HANDOVER_*；description "请确认知识 / 文档已交接给后续接手人并填写交接路径。"
    5. **finance_settle.py**：常量 FINANCE_SETTLE_*；description "请完成离职员工的薪资 / 福利 / 公积金结算并填写明细。"
    6. **legal_sign.py**：常量 LEGAL_SIGN_*；description "请确认保密协议 / 竞业限制 / 离职证明签署完成。"
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from offboarding_flow.flow_engine.nodes.device_return import device_return_node; from offboarding_flow.flow_engine.nodes.access_revoke import access_revoke_node; from offboarding_flow.flow_engine.nodes.knowledge_handover import knowledge_handover_node; from offboarding_flow.flow_engine.nodes.finance_settle import finance_settle_node; from offboarding_flow.flow_engine.nodes.legal_sign import legal_sign_node; print('ok')"</automated>
  </verify>
  <done>5 节点 + 工厂可 import；每节点文件 < 20 行</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 nodes/__init__.py export 5 并行节点 + PARALLEL_NODES_META</name>
  <files>backend/src/offboarding_flow/flow_engine/nodes/__init__.py</files>
  <action>
    在现有 import 基础上追加：
    ```python
    from .device_return import (
        DEVICE_RETURN_NODE_NAME, DEVICE_RETURN_NODE_TITLE,
        DEVICE_RETURN_NODE_DESCRIPTION, device_return_node,
    )
    from .access_revoke import (
        ACCESS_REVOKE_NODE_NAME, ACCESS_REVOKE_NODE_TITLE,
        ACCESS_REVOKE_NODE_DESCRIPTION, access_revoke_node,
    )
    from .knowledge_handover import (
        KNOWLEDGE_HANDOVER_NODE_NAME, KNOWLEDGE_HANDOVER_NODE_TITLE,
        KNOWLEDGE_HANDOVER_NODE_DESCRIPTION, knowledge_handover_node,
    )
    from .finance_settle import (
        FINANCE_SETTLE_NODE_NAME, FINANCE_SETTLE_NODE_TITLE,
        FINANCE_SETTLE_NODE_DESCRIPTION, finance_settle_node,
    )
    from .legal_sign import (
        LEGAL_SIGN_NODE_NAME, LEGAL_SIGN_NODE_TITLE,
        LEGAL_SIGN_NODE_DESCRIPTION, legal_sign_node,
    )

    # 5 并行节点元数据（供 graph.py 总装 fan-out 时迭代用）
    PARALLEL_NODES_META: list[tuple[str, str, str, object]] = [
        (DEVICE_RETURN_NODE_NAME, DEVICE_RETURN_NODE_TITLE, DEVICE_RETURN_NODE_DESCRIPTION, device_return_node),
        (ACCESS_REVOKE_NODE_NAME, ACCESS_REVOKE_NODE_TITLE, ACCESS_REVOKE_NODE_DESCRIPTION, access_revoke_node),
        (KNOWLEDGE_HANDOVER_NODE_NAME, KNOWLEDGE_HANDOVER_NODE_TITLE, KNOWLEDGE_HANDOVER_NODE_DESCRIPTION, knowledge_handover_node),
        (FINANCE_SETTLE_NODE_NAME, FINANCE_SETTLE_NODE_TITLE, FINANCE_SETTLE_NODE_DESCRIPTION, finance_settle_node),
        (LEGAL_SIGN_NODE_NAME, LEGAL_SIGN_NODE_TITLE, LEGAL_SIGN_NODE_DESCRIPTION, legal_sign_node),
    ]

    __all__ = [
        # ... 既有 ...
        "DEVICE_RETURN_NODE_NAME", "DEVICE_RETURN_NODE_TITLE", "device_return_node",
        "ACCESS_REVOKE_NODE_NAME", "ACCESS_REVOKE_NODE_TITLE", "access_revoke_node",
        "KNOWLEDGE_HANDOVER_NODE_NAME", "KNOWLEDGE_HANDOVER_NODE_TITLE", "knowledge_handover_node",
        "FINANCE_SETTLE_NODE_NAME", "FINANCE_SETTLE_NODE_TITLE", "finance_settle_node",
        "LEGAL_SIGN_NODE_NAME", "LEGAL_SIGN_NODE_TITLE", "legal_sign_node",
        "PARALLEL_NODES_META",
    ]
    ```
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from offboarding_flow.flow_engine.nodes import PARALLEL_NODES_META; assert len(PARALLEL_NODES_META) == 5; print([n[0] for n in PARALLEL_NODES_META])"</automated>
  </verify>
  <done>PARALLEL_NODES_META 长度 5；5 个节点函数均可从 nodes 包顶级 import</done>
</task>

<task type="auto">
  <name>Task 3: 写 test_nodes_parallel 5 节点 interrupt 行为单测</name>
  <files>backend/tests/test_nodes_parallel.py</files>
  <action>
    ```python
    """test_nodes_parallel.py — 5 个并行人工节点的 interrupt + Command(resume) 行为。

    用最小 StateGraph（START → node → END）+ InMemorySaver 验证 _human_node_factory 生成的节点。
    """
    from __future__ import annotations
    import uuid
    import pytest
    from langgraph.checkpoint.memory import InMemorySaver
    from langgraph.graph import END, START, StateGraph
    from langgraph.types import Command

    from offboarding_flow.flow_engine.nodes import PARALLEL_NODES_META
    from offboarding_flow.flow_engine.state import OffboardingState

    @pytest.fixture(params=PARALLEL_NODES_META, ids=[m[0] for m in PARALLEL_NODES_META])
    def parallel_node_graph(request):
        name, title, desc, fn = request.param
        b = StateGraph(OffboardingState)
        b.add_node(name, fn)
        b.add_edge(START, name)
        b.add_edge(name, END)
        return name, b.compile(checkpointer=InMemorySaver())

    def _state():
        return {"flow_id": str(uuid.uuid4()), "employee_id": "zhang.san",
                "current_action": None, "node_results": [], "context": {}}

    async def test_parallel_node_interrupts(parallel_node_graph):
        name, graph = parallel_node_graph
        s = _state()
        cfg = {"configurable": {"thread_id": s["flow_id"]}}
        await graph.ainvoke(s, config=cfg)
        snap = await graph.aget_state(config=cfg)
        assert snap.next == (name,)  # interrupt 后 next 仍是该节点

    async def test_parallel_node_advance_resumes(parallel_node_graph):
        name, graph = parallel_node_graph
        s = _state()
        cfg = {"configurable": {"thread_id": s["flow_id"]}}
        await graph.ainvoke(s, config=cfg)
        final = await graph.ainvoke(
            Command(resume={"action": "advance", "result_text": f"{name} done", "actor": "tester"}),
            config=cfg,
        )
        assert final["current_action"] == "advance"
        assert any(r["node_name"] == name for r in final["node_results"])

    async def test_parallel_node_return_resumes(parallel_node_graph):
        name, graph = parallel_node_graph
        s = _state()
        cfg = {"configurable": {"thread_id": s["flow_id"]}}
        await graph.ainvoke(s, config=cfg)
        final = await graph.ainvoke(
            Command(resume={"action": "return", "result_text": "need rework", "actor": "tester"}),
            config=cfg,
        )
        assert final["current_action"] == "return"

    async def test_parallel_node_factory_preserves_dunder_name():
        """make_human_node 设置 _node.__name__ 便于调试 / log。"""
        for name, _title, _desc, fn in PARALLEL_NODES_META:
            assert fn.__name__ == f"{name}_node", f"{fn.__name__} != {name}_node"

    def test_parallel_node_count_is_5():
        assert len(PARALLEL_NODES_META) == 5

    def test_parallel_node_names_are_unique():
        names = [m[0] for m in PARALLEL_NODES_META]
        assert len(set(names)) == 5
    ```
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_nodes_parallel.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>5 节点 × 3 行为测试 = 15+ 单测全 pass</done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG [Unreleased] 段追加 Plan 03 记录</name>
  <files>CHANGELOG.md</files>
  <action>
    追加：
    ```
    - 2026-05-16 — Phase 2 Plan 03：5 并行节点 + _human_node_factory 工厂
      - flow_engine/nodes/_human_node_factory.py：人工节点通用模板（interrupt + decision + node_results）
      - 5 节点：device_return / access_revoke / knowledge_handover / finance_settle / legal_sign
      - flow_engine/nodes/__init__.py 暴露 PARALLEL_NODES_META 给 graph 总装
      - 17+ 节点 interrupt 行为单测
    ```
  </action>
  <verify>
    <automated>grep -c "Phase 2 Plan 03" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG 含 Plan 03 记录</done>
</task>

</tasks>

<verification>
1. 5 节点 + 工厂均可 import
2. PARALLEL_NODES_META 长度 == 5
3. 节点函数 __name__ 正确设置（device_return_node / access_revoke_node / ...）
4. `uv run pytest backend/tests/test_nodes_parallel.py -v` 全 pass
5. Phase 1 既有测试不破
6. CHANGELOG 含 Plan 03 段
</verification>

<success_criteria>
- 5 并行节点均按统一工厂模板生成（避免重复代码）
- PARALLEL_NODES_META 元数据可供 Plan 05 graph 拓扑总装使用
- 单测覆盖每个节点的 3 态决策
- Phase 1 + Plan 01/02 测试零回归
</success_criteria>

<output>
完成后写 `.planning/phases/02-double-write-nodes/02-03-SUMMARY.md`
</output>
