---
phase: 04.5-autonode-demo
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mock-archive-service/main.py
  - mock-archive-service/Dockerfile
  - mock-archive-service/requirements.txt
  - mock-archive-service/.dockerignore
  - docker-compose.yml
  - .env.example
  - backend/tests/integration/test_auto_archive_to_storage.py
  - backend/tests/e2e/test_full_flow_with_auto_archive.py
autonomous: true
requirements:
  - AUTO-02
must_haves:
  truths:
    - "mock-archive-service/ 独立目录：FastAPI 极简服务，接收 POST /archive 写文件到 /data/archive/{flow_id}.json"
    - "docker-compose.yml 追加 mock-archive-service service（端口 5050:5000，挂载 /data/offboarding/archive volume）"
    - ".env.example 追加 ARCHIVE_SERVICE_URL 配置项（带默认值注释）"
    - "integration test 用 pytest fixture 启 mock-archive-service subprocess（端口 15000 防冲突）+ 真 PG 验证完整双写"
    - "E2E test 跑 full flow 到 auto_archive_to_storage → 校验文件存在 + node_states 含 auto_archive done + action_logs 含 system:auto"
    - "integration test 包含失败场景：subprocess 关掉 → 节点 raise ArchiveServiceError + action_log.failed"
  artifacts:
    - mock-archive-service/main.py (< 30 行)
    - mock-archive-service/Dockerfile + requirements.txt + .dockerignore
    - docker-compose.yml 追加 service 段
    - .env.example 追加 ARCHIVE_SERVICE_URL 段
    - integration test 文件 (test_auto_archive_to_storage.py) — 真 mock service + 真 PG
    - E2E test 文件 (test_full_flow_with_auto_archive.py) — 跑 full flow
  key_links:
    - "PRD §18.2 mock 服务 设计"
    - "ROADMAP §Phase 4.5 验收"
    - "CLAUDE.md §2.1 全流程 E2E 必跑 / §2.3 集成测试禁 mock DB"
    - "REQ AUTO-02 (mock-archive-service 容器)"
---

<objective>
落地 mock-archive-service container + integration + E2E 测试，证明 AutoNode 端到端可工作。

- **mock-archive-service**：FastAPI 10 行接收 POST /archive 写文件。极简 PoC（不做鉴权 / TLS / 重排）。
- **docker-compose service**：让其他服务通过 `mock-archive-service:5000` 访问，端口暴露 5050:5000 方便 host 调试。
- **integration test**：起一个 FastAPI subprocess（端口 15000）+ 真 PG，验证节点函数完整链路（POST → 文件落地 → node_states done + action_logs success）。
- **E2E test**：跑 full flow 到 auto_archive_to_storage，校验文件 + 业务表所有写入。

Output: 1 mock service 目录 + docker-compose 新 service + .env.example + 2 个测试文件。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04.5-autonode-demo/04.5-CONTEXT.md
@.planning/phases/04.5-autonode-demo/04.5-01-PLAN.md
@PRD.md
@docker-compose.yml
@.env.example
@backend/tests/conftest.py
@backend/tests/integration/test_flow_full_chain.py
@backend/tests/e2e/conftest.py
@backend/tests/e2e/test_full_10_nodes_flow.py

<test_helper_pattern>
# integration test fixture：起 mock-archive-service 在 subprocess（解耦 docker），用 uvicorn programmatic API
# E2E test fixture：复用 app_client + integration mock_archive_service fixture
</test_helper_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写 mock-archive-service/main.py + requirements.txt + Dockerfile + .dockerignore</name>
  <files>mock-archive-service/main.py, mock-archive-service/requirements.txt, mock-archive-service/Dockerfile, mock-archive-service/.dockerignore</files>
  <action>
    1. **mock-archive-service/main.py**（极简 FastAPI）：
       ```python
       """mock-archive-service — Phase 4.5 演示用 mock 外部归档服务（PRD §18.2）。

       行为：接收 POST /archive 把 body 写到 /data/archive/{flow_id}.json 文件。
       不做鉴权 / 持久化策略 / 重排 — 仅为演示 AutoNode 调外部 HTTP API 模式。
       """

       from __future__ import annotations

       import json
       import os
       import pathlib

       from fastapi import FastAPI, Request

       DATA_DIR = pathlib.Path(os.environ.get("ARCHIVE_DATA_DIR", "/data/archive"))
       DATA_DIR.mkdir(parents=True, exist_ok=True)

       app = FastAPI(title="mock-archive-service", version="0.1.0")


       @app.post("/archive")
       async def archive(req: Request) -> dict:
           body = await req.json()
           flow_id = body.get("flow_id") or "unknown"
           path = DATA_DIR / f"{flow_id}.json"
           path.write_text(json.dumps(body, ensure_ascii=False, indent=2), encoding="utf-8")
           return {"archived": True, "path": str(path), "size": path.stat().st_size}


       @app.get("/health")
       async def health() -> dict:
           return {"ok": True, "data_dir": str(DATA_DIR)}
       ```

    2. **mock-archive-service/requirements.txt**：
       ```
       fastapi>=0.115,<0.140
       uvicorn[standard]>=0.32,<0.40
       ```

    3. **mock-archive-service/Dockerfile**：
       ```dockerfile
       # Phase 4.5 mock 归档服务（PRD §18.2）— 演示 AutoNode 调外部 HTTP API
       FROM python:3.12-slim-bookworm

       WORKDIR /app

       COPY requirements.txt /app/requirements.txt
       RUN pip install --no-cache-dir -r /app/requirements.txt

       COPY main.py /app/main.py

       # 数据目录（docker-compose volume 挂载到此路径）
       RUN mkdir -p /data/archive
       VOLUME ["/data/archive"]

       EXPOSE 5000

       CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "5000"]
       ```

    4. **mock-archive-service/.dockerignore**：
       ```
       __pycache__
       *.pyc
       .pytest_cache
       .venv
       .env
       ```
  </action>
  <verify>
    <automated>ls -la mock-archive-service/ && head -5 mock-archive-service/main.py && cat mock-archive-service/requirements.txt</automated>
  </verify>
  <done>4 个文件在位；main.py < 30 行；Dockerfile 基于 python:3.12-slim</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml 追加 mock-archive-service service</name>
  <files>docker-compose.yml</files>
  <action>
    在 services 段 flow-api 之后、networks 之前追加：
    ```yaml
      mock-archive-service:
        build:
          context: ./mock-archive-service
        image: offboarding-mock-archive:latest
        container_name: offboarding-mock-archive
        ports:
          - "5050:5000"
        environment:
          ARCHIVE_DATA_DIR: /data/archive
        volumes:
          - /data/offboarding/archive:/data/archive
        restart: unless-stopped
        healthcheck:
          test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/health').read()"]
          interval: 10s
          timeout: 3s
          retries: 5
          start_period: 10s
        networks:
          - offboarding-net
    ```
    同时**给 flow-api service 加 depends_on**（让 flow-api 起来时 mock 服务已 healthy）：
    在 flow-api.depends_on 下追加：
    ```yaml
          mock-archive-service:
            condition: service_healthy
    ```
    同时 flow-api environment 段追加：
    ```yaml
          ARCHIVE_SERVICE_URL: ${ARCHIVE_SERVICE_URL:-http://mock-archive-service:5000/archive}
    ```
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('docker-compose.yml')); print('services:', list(d['services'].keys())); assert 'mock-archive-service' in d['services']; print('mock-archive-service:', d['services']['mock-archive-service'].get('container_name')); print('flow-api depends_on:', list(d['services']['flow-api']['depends_on'].keys()))"</automated>
  </verify>
  <done>docker-compose.yml 含 mock-archive-service service；flow-api depends_on 含 mock-archive-service</done>
</task>

<task type="auto">
  <name>Task 3: .env.example 追加 ARCHIVE_SERVICE_URL</name>
  <files>.env.example</files>
  <action>
    在 .env.example 末尾追加（带分组注释）：
    ```
    # ============================================================================
    # Phase 4.5 — AutoNode 外部归档服务（PRD §18 加分项）
    # ============================================================================
    # mock-archive-service 容器内地址（docker-compose 默认）；host 调试时用 http://localhost:5050/archive
    ARCHIVE_SERVICE_URL=http://mock-archive-service:5000/archive
    ARCHIVE_SERVICE_TIMEOUT_SECONDS=8.0
    ARCHIVE_SERVICE_MAX_RETRIES=3
    ```
  </action>
  <verify>
    <automated>grep -c "ARCHIVE_SERVICE_URL" .env.example</automated>
  </verify>
  <done>.env.example 含 ARCHIVE_SERVICE_URL 段</done>
</task>

<task type="auto">
  <name>Task 4: 写 integration test — test_auto_archive_to_storage.py（真 mock service + 真 PG）</name>
  <files>backend/tests/integration/test_auto_archive_to_storage.py</files>
  <action>
    ```python
    """test_auto_archive_to_storage.py — Phase 4.5 集成测试（CLAUDE.md §2.3 禁 mock DB）。

    起一个 mock-archive-service subprocess（端口 15050）+ 用真 PG，跑节点函数：
    1. 成功路径：节点函数返回 advance + 文件落地 + 业务表三处写入对
    2. 失败路径：subprocess 关掉 → 节点 raise ArchiveServiceError + action_log.failed
    """

    from __future__ import annotations

    import asyncio
    import json
    import os
    import pathlib
    import socket
    import subprocess
    import sys
    import tempfile
    import time
    import uuid

    import httpx
    import pytest
    from sqlalchemy import select

    pytestmark = [
        pytest.mark.integration,
        pytest.mark.skipif(
            not os.environ.get("TEST_DATABASE_URL"),
            reason="需要 TEST_DATABASE_URL 指向 offboarding_test DB",
        ),
    ]


    def _free_port() -> int:
        """找一个空闲 TCP 端口（用于 mock-archive-service subprocess）。"""
        s = socket.socket()
        s.bind(("127.0.0.1", 0))
        port = s.getsockname()[1]
        s.close()
        return port


    @pytest.fixture
    def archive_data_dir():
        """提供 mock service 写文件目录 — 临时目录用完删。"""
        with tempfile.TemporaryDirectory(prefix="auto-archive-") as d:
            yield pathlib.Path(d)


    @pytest.fixture
    async def mock_archive_subprocess(archive_data_dir, monkeypatch):
        """在 subprocess 启 mock-archive-service。

        把 ARCHIVE_SERVICE_URL 指向 subprocess 端口（解耦 docker — CI 也能跑）。
        """
        port = _free_port()
        env = os.environ.copy()
        env["ARCHIVE_DATA_DIR"] = str(archive_data_dir)
        # mock-archive-service 目录 = repo 根 / mock-archive-service
        mock_dir = pathlib.Path(__file__).resolve().parents[3] / "mock-archive-service"
        assert mock_dir.exists(), f"mock-archive-service 目录缺失: {mock_dir}"
        proc = subprocess.Popen(
            [
                sys.executable, "-m", "uvicorn", "main:app",
                "--host", "127.0.0.1", "--port", str(port),
            ],
            cwd=str(mock_dir),
            env=env,
            stdout=subprocess.DEVNULL,
            stderr=subprocess.DEVNULL,
        )
        # poll /health 等到起来（最多 10s）
        url = f"http://127.0.0.1:{port}"
        deadline = time.time() + 10
        ok = False
        while time.time() < deadline:
            try:
                async with httpx.AsyncClient(timeout=1.0) as c:
                    r = await c.get(f"{url}/health")
                    if r.status_code == 200:
                        ok = True
                        break
            except Exception:
                await asyncio.sleep(0.2)
        if not ok:
            proc.terminate()
            pytest.skip(f"mock-archive-service subprocess 启不来 (port={port})")
        # patch settings
        from offboarding_flow.config import get_settings
        s = get_settings()
        monkeypatch.setattr(s, "ARCHIVE_SERVICE_URL", f"{url}/archive", raising=False)
        try:
            yield {"port": port, "url": url, "data_dir": archive_data_dir}
        finally:
            proc.terminate()
            try:
                proc.wait(timeout=5)
            except subprocess.TimeoutExpired:
                proc.kill()


    @pytest.fixture
    async def services_and_flow():
        """造真 PG flow + node + apply node done — auto_archive 节点需要 flow_instances + flow.context 存在。"""
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

        from offboarding_flow.state_store.enums import FlowStatus, NodeStatus
        from offboarding_flow.state_store.models import (
            ActionLog,
            Base,
            FlowInstance,
            NodeState,
        )

        dsn = os.environ["TEST_DATABASE_URL"]
        engine = create_async_engine(dsn, echo=False)
        async with engine.begin() as conn:
            await conn.run_sync(Base.metadata.drop_all)
            await conn.run_sync(Base.metadata.create_all)
        maker = async_sessionmaker(engine, expire_on_commit=False)

        flow_id = uuid.uuid4()
        async with maker() as s:
            flow = FlowInstance(
                id=flow_id,
                employee_id="zhang.san",
                status=FlowStatus.IN_PROGRESS.value,
                context={"node_results": [
                    {"node_name": "manager_review", "node_title": "上级审批",
                     "result_text": "同意", "actor": "li.si",
                     "completed_at": "2026-05-16T10:00:00+00:00"}
                ]},
            )
            s.add(flow)
            await s.commit()
        try:
            yield {"engine": engine, "maker": maker, "flow_id": flow_id, "models": (FlowInstance, NodeState, ActionLog)}
        finally:
            await engine.dispose()


    async def test_auto_archive_success_writes_file_and_business_tables(
        mock_archive_subprocess, services_and_flow, monkeypatch
    ):
        """成功路径：节点函数调成功 → 文件落地 + node_states done + action_logs SYSTEM SUCCESS + flow.context 追加。"""
        from offboarding_flow.flow_engine.nodes.auto_archive_to_storage import (
            AUTO_ARCHIVE_TO_STORAGE_NODE_NAME,
            auto_archive_to_storage_node,
        )

        # patch 默认 session_factory 走 services_and_flow.maker
        from contextlib import asynccontextmanager

        @asynccontextmanager
        async def sf():
            async with services_and_flow["maker"]() as s:
                yield s

        from offboarding_flow.services import auto_node_service as mod
        monkeypatch.setattr(mod, "_default_session_factory", sf)

        flow_id = services_and_flow["flow_id"]
        state = {
            "flow_id": str(flow_id),
            "employee_id": "zhang.san",
            "current_action": None,
            "node_results": [
                {"node_name": "manager_review", "node_title": "上级审批",
                 "result_text": "同意", "actor": "li.si",
                 "completed_at": "2026-05-16T10:00:00+00:00"}
            ],
            "context": {},
        }
        result = await auto_archive_to_storage_node(state)
        assert result["current_action"] == "advance"

        # 1) mock 服务文件落地
        archive_file = mock_archive_subprocess["data_dir"] / f"{flow_id}.json"
        assert archive_file.exists(), f"归档文件应存在: {archive_file}"
        archived = json.loads(archive_file.read_text(encoding="utf-8"))
        assert archived["flow_id"] == str(flow_id)
        assert archived["employee_id"] == "zhang.san"
        assert len(archived["node_results"]) == 1

        # 2) node_states 含 auto_archive done
        FlowInstance, NodeState, ActionLog = services_and_flow["models"]
        async with services_and_flow["maker"]() as s:
            row = (await s.execute(
                select(NodeState).where(
                    NodeState.flow_id == flow_id,
                    NodeState.node_name == AUTO_ARCHIVE_TO_STORAGE_NODE_NAME,
                )
            )).scalar_one()
            assert row.status == "done"
            assert row.assignee == "system:auto"

            # 3) action_logs 含 SYSTEM SUCCESS actor=system:auto
            logs = (await s.execute(
                select(ActionLog).where(
                    ActionLog.flow_id == flow_id,
                    ActionLog.action == "system",
                )
            )).scalars().all()
            assert any(l.status == "success" and l.actor == "system:auto" for l in logs)

            # 4) flow.context.node_results 追加（数组长度 +1）
            f = (await s.execute(select(FlowInstance).where(FlowInstance.id == flow_id))).scalar_one()
            assert any(
                r.get("node_name") == AUTO_ARCHIVE_TO_STORAGE_NODE_NAME
                for r in (f.context or {}).get("node_results", [])
            )


    async def test_auto_archive_failure_raises_and_marks_action_log_failed(
        services_and_flow, monkeypatch
    ):
        """失败路径：URL 指向不存在的端口 → 节点 raise + action_log.failed + node_states 不动。"""
        from offboarding_flow.config import get_settings
        from offboarding_flow.flow_engine.nodes.auto_archive_to_storage import (
            ArchiveServiceError,
            auto_archive_to_storage_node,
        )

        s = get_settings()
        # 指向必定 connection refused 的端口
        monkeypatch.setattr(s, "ARCHIVE_SERVICE_URL", "http://127.0.0.1:1/archive", raising=False)
        monkeypatch.setattr(s, "ARCHIVE_SERVICE_MAX_RETRIES", 2, raising=False)
        monkeypatch.setattr(s, "ARCHIVE_SERVICE_TIMEOUT_SECONDS", 1.0, raising=False)

        from contextlib import asynccontextmanager

        @asynccontextmanager
        async def sf():
            async with services_and_flow["maker"]() as ss:
                yield ss

        from offboarding_flow.services import auto_node_service as mod
        monkeypatch.setattr(mod, "_default_session_factory", sf)

        flow_id = services_and_flow["flow_id"]
        state = {
            "flow_id": str(flow_id),
            "employee_id": "zhang.san",
            "current_action": None,
            "node_results": [],
            "context": {},
        }
        with pytest.raises(ArchiveServiceError):
            await auto_archive_to_storage_node(state)

        # action_logs 含 SYSTEM FAILED
        FlowInstance, NodeState, ActionLog = services_and_flow["models"]
        async with services_and_flow["maker"]() as ss:
            logs = (await ss.execute(
                select(ActionLog).where(
                    ActionLog.flow_id == flow_id,
                    ActionLog.action == "system",
                    ActionLog.status == "failed",
                )
            )).scalars().all()
            assert len(logs) >= 1
            assert "connection" in (logs[0].error_message or "").lower() or "refused" in (logs[0].error_message or "").lower() or "127.0.0.1" in (logs[0].error_message or "")

            # node_states 不应有 auto_archive done（失败时不强行标 done）
            rows = (await ss.execute(
                select(NodeState).where(
                    NodeState.flow_id == flow_id,
                    NodeState.node_name == "auto_archive_to_storage",
                )
            )).scalars().all()
            # 可能 0 行或 ≥1 行（取决于 upsert 是否在失败前已调）— 但绝不应 status=done
            for r in rows:
                assert r.status != "done"
    ```
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_auto_archive_to_storage.py -v -m integration 2>&1 | tail -25 || true</automated>
  </verify>
  <done>integration test 文件创建；TEST_DATABASE_URL 设置时可跑（无 env 时 skip 不阻塞 CI）</done>
</task>

<task type="auto">
  <name>Task 5: 写 E2E test — test_full_flow_with_auto_archive.py（跑 full flow 到 auto_archive）</name>
  <files>backend/tests/e2e/test_full_flow_with_auto_archive.py</files>
  <action>
    ```python
    """test_full_flow_with_auto_archive.py — Phase 4.5 E2E（CLAUDE.md §2.1 全流程必跑）。

    跑完 10 节点 happy-path 到 applicant_final_confirm → advance → auto_archive_to_storage → archive → END
    校验：
    1. /data/archive/{flow_id}.json 文件存在且内容含 node_results
    2. node_states 含 auto_archive_to_storage status=done + actor=system:auto
    3. action_logs 含 SYSTEM SUCCESS actor=system:auto
    4. 流程最终 status=completed
    """

    from __future__ import annotations

    import asyncio
    import json
    import os
    import pathlib
    import socket
    import subprocess
    import sys
    import tempfile
    import time

    import httpx
    import pytest

    pytestmark = [pytest.mark.e2e]


    def _free_port() -> int:
        s = socket.socket()
        s.bind(("127.0.0.1", 0))
        port = s.getsockname()[1]
        s.close()
        return port


    @pytest.fixture
    async def archive_data_dir():
        with tempfile.TemporaryDirectory(prefix="e2e-auto-archive-") as d:
            yield pathlib.Path(d)


    @pytest.fixture
    async def mock_archive_subprocess(archive_data_dir, monkeypatch):
        """与 integration test 同样的 subprocess 启动模式。"""
        port = _free_port()
        env = os.environ.copy()
        env["ARCHIVE_DATA_DIR"] = str(archive_data_dir)
        mock_dir = pathlib.Path(__file__).resolve().parents[3] / "mock-archive-service"
        if not mock_dir.exists():
            pytest.skip(f"mock-archive-service 目录缺失: {mock_dir}")
        proc = subprocess.Popen(
            [
                sys.executable, "-m", "uvicorn", "main:app",
                "--host", "127.0.0.1", "--port", str(port),
            ],
            cwd=str(mock_dir),
            env=env,
            stdout=subprocess.DEVNULL,
            stderr=subprocess.DEVNULL,
        )
        deadline = time.time() + 10
        ok = False
        while time.time() < deadline:
            try:
                async with httpx.AsyncClient(timeout=1.0) as c:
                    r = await c.get(f"http://127.0.0.1:{port}/health")
                    if r.status_code == 200:
                        ok = True
                        break
            except Exception:
                await asyncio.sleep(0.2)
        if not ok:
            proc.terminate()
            pytest.skip(f"mock-archive-service subprocess 启不来 (port={port})")
        from offboarding_flow.config import get_settings
        s = get_settings()
        monkeypatch.setattr(
            s, "ARCHIVE_SERVICE_URL", f"http://127.0.0.1:{port}/archive", raising=False
        )
        try:
            yield {"port": port, "data_dir": archive_data_dir}
        finally:
            proc.terminate()
            try:
                proc.wait(timeout=5)
            except subprocess.TimeoutExpired:
                proc.kill()


    async def _advance(client, flow_id, node_id, result_text, actor):
        r = await client.post(
            f"/api/flows/{flow_id}/nodes/{node_id}/actions",
            json={"action": "advance", "result_text": result_text, "actor": actor},
        )
        assert r.status_code == 200, f"advance failed: {r.status_code} {r.text}"
        return r.json()["data"]


    async def _get_waiting_node_by_name(client, flow_id, name):
        r = await client.get(f"/api/flows/{flow_id}/nodes")
        assert r.status_code == 200
        nodes = r.json()["data"]
        waiting = [n for n in nodes if n["status"] == "waiting_human" and n["name"] == name]
        assert waiting, f"node {name} 未 waiting; got {[n['name'] + ':' + n['status'] for n in nodes]}"
        return waiting[0]


    async def test_full_flow_to_auto_archive_writes_file_and_completes(
        app_client, mock_archive_subprocess
    ):
        """跑完 10 节点到 applicant advance → auto_archive_to_storage 成功 → archive → completed。"""
        # Step 1: 起流程
        r = await app_client.post("/api/flows", json={"employee_id": "zhang.san"})
        assert r.status_code == 200, r.text
        flow_id = r.json()["data"]["flow_id"]

        # 2-3: manager + hr_initial advance
        node = await _get_waiting_node_by_name(app_client, flow_id, "manager_review")
        await _advance(app_client, flow_id, node["id"], "同意", "li.si")
        node = await _get_waiting_node_by_name(app_client, flow_id, "hr_initial")
        await _advance(app_client, flow_id, node["id"], "材料齐全", "hr.alice")

        # 4: 5 并行节点全部 advance
        parallel_results = {
            "device_return": ("MacBook 已归还", "it.charlie"),
            "access_revoke": ("权限已回收", "it.charlie"),
            "knowledge_handover": ("文档已交接", "li.si"),
            "finance_settle": ("结算完成", "fin.david"),
            "legal_sign": ("协议已签", "legal.eve"),
        }
        for name, (text, actor) in parallel_results.items():
            node = await _get_waiting_node_by_name(app_client, flow_id, name)
            await _advance(app_client, flow_id, node["id"], text, actor)

        # 5: hr_final
        node = await _get_waiting_node_by_name(app_client, flow_id, "hr_final")
        await _advance(app_client, flow_id, node["id"], "终审通过", "hr.bob")

        # 6: applicant advance → 自动跑 auto_archive_to_storage → archive → END
        node = await _get_waiting_node_by_name(app_client, flow_id, "applicant_final_confirm")
        await _advance(app_client, flow_id, node["id"], "无异议", "zhang.san")

        # 7: 流程应 completed
        r = await app_client.get(f"/api/flows/{flow_id}")
        assert r.json()["data"]["status"] == "completed", f"flow not completed: {r.json()}"

        # 8: 归档文件存在
        archive_file = mock_archive_subprocess["data_dir"] / f"{flow_id}.json"
        assert archive_file.exists(), (
            f"归档文件应存在: {archive_file}; "
            f"现有文件: {list(mock_archive_subprocess['data_dir'].iterdir())}"
        )
        archived = json.loads(archive_file.read_text(encoding="utf-8"))
        assert archived["flow_id"] == flow_id
        assert "node_results" in archived
        assert len(archived["node_results"]) >= 9  # 至少 9 节点结果（不含 archive 自己）

        # 9: node_states 含 auto_archive done + actor=system:auto
        r = await app_client.get(f"/api/flows/{flow_id}/nodes")
        nodes = r.json()["data"]
        auto_archive = [n for n in nodes if n["name"] == "auto_archive_to_storage"]
        assert auto_archive, f"auto_archive_to_storage 应在 node_states; got {[n['name'] for n in nodes]}"
        assert auto_archive[0]["status"] == "done"
        # assignee=system:auto
        assert auto_archive[0].get("assignee") == "system:auto"
    ```
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/e2e/test_full_flow_with_auto_archive.py -v -m e2e 2>&1 | tail -25 || true</automated>
  </verify>
  <done>E2E test 文件创建；OFFBOARDING_E2E_BASE_URL / TEST_DATABASE_URL 设置时可跑</done>
</task>

</tasks>

<verification>
1. mock-archive-service/ 4 文件在位（main.py, requirements.txt, Dockerfile, .dockerignore）
2. docker-compose.yml 含新 mock-archive-service service；flow-api depends_on 含 mock-archive-service
3. .env.example 含 ARCHIVE_SERVICE_URL 段
4. integration test：跑出 subprocess 后写真 PG，验证文件 + 业务表所有写入
5. E2E test：跑 full flow 到 auto_archive → 文件存在 + 流程 completed + node_states auto_archive done
6. 失败路径测试覆盖：subprocess 关掉 → 节点 raise + action_log.failed + node_states 不强行标 done
</verification>

<success_criteria>
- REQ AUTO-02 Complete（mock-archive-service container + docker-compose 集成）
- E2E 验证 PRD §18.2 完整链路：applicant advance → 自动归档 → 文件落地 → 流程完成
- 集成测试满足 CLAUDE.md §2.3（真 PG + 真 mock service subprocess，不 mock httpx）
- 失败补偿对称：mock 服务 down → action_log.failed + 流程卡住等运维介入（验收 §「失败不绕过」）
</success_criteria>

<output>
完成后写 `.planning/phases/04.5-autonode-demo/04.5-02-SUMMARY.md`。
</output>
