---
phase: 01-langgraph-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.dev.yml
  - backend/Dockerfile
  - backend/.dockerignore
  - backend/entrypoint.sh
  - deploy/init-db.sql
  - CHANGELOG.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-05
must_haves:
  truths:
    - "docker-compose.yml 起 3 个服务（offboarding-postgres / offboarding-redis / flow-api），各自 healthcheck 通过；postgres 端口 5433，redis 端口 6380"
    - "docker-compose.dev.yml override 把 backend/src 挂进容器，启动 uvicorn --reload"
    - "Dockerfile 多阶段（uv builder + python:3.12-slim-bookworm runtime），final image < 250MB"
    - "entrypoint.sh 顺序：等待 postgres 健康 → alembic upgrade head → await checkpointer.setup() → uvicorn"
    - "init-db.sql 在 postgres 首次启动时创建 app / langgraph 两个 schema"
  artifacts:
    - docker-compose.yml（生产模板，无 volume mount，restart=unless-stopped）
    - docker-compose.dev.yml（本地开发 override：volume mount + --reload）
    - backend/Dockerfile（多阶段）
    - backend/.dockerignore
    - backend/entrypoint.sh
    - deploy/init-db.sql（创建 app + langgraph + app_test + langgraph_test 4 schemas）
  key_links:
    - "POSTGRES_PASSWORD 通过 .env 注入，不写死任何凭证"
    - "init-db.sql 挂载到 postgres /docker-entrypoint-initdb.d/"
    - "flow-api depends_on postgres + redis healthy（CONTEXT §10）"
    - "本 Plan 不上 nginx、不上 web/Next.js 容器（Phase 6 才完整部署）"
---

<objective>
建立 Docker Compose 编排骨架与容器化基础设施：定义 3 个独立容器（postgres-16 / redis-7 / flow-api）部署到 192.168.2.44 端口 5433/6380/8000；多阶段 Dockerfile（uv builder + slim runtime）；entrypoint 串联 alembic + checkpointer.setup + uvicorn；init-db.sql 创建 app/langgraph schema。

Purpose: 提供 Phase 1 部署到 192.168.2.44 的可运行容器编排。本 Plan 与 Plan 01（工具链）独立，可并行写。本 Plan 不会跑容器 — 跑容器是 Plan 07 全流程冒烟才做。

Output: 完整 docker-compose 编排文件 + Dockerfile + entrypoint + schema 初始化脚本。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-langgraph-schema/01-CONTEXT.md
@.env.example

<interfaces>
<!-- docker-compose.yml 必须遵循 CONTEXT §10 的完整模板 -->

services:
  offboarding-postgres:
    image: postgres:16-alpine
    container_name: offboarding-postgres
    environment:
      POSTGRES_USER: flow
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD 必须设置}
      POSTGRES_DB: offboarding
    ports: ["5433:5432"]
    volumes:
      - /data/offboarding/postgres-data:/var/lib/postgresql/data
      - ./deploy/init-db.sql:/docker-entrypoint-initdb.d/01-init-schemas.sql:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flow -d offboarding"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks: [offboarding-net]

  offboarding-redis:
    image: redis:7-alpine
    container_name: offboarding-redis
    ports: ["6380:6379"]
    volumes: ["/data/offboarding/redis-data:/data"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks: [offboarding-net]

  flow-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: offboarding-flow-api:latest
    container_name: offboarding-flow-api
    ports: ["8000:8000"]
    environment:
      POSTGRES_DSN: postgresql+asyncpg://flow:${POSTGRES_PASSWORD}@offboarding-postgres:5432/offboarding
      LANGGRAPH_PG_CONNINFO: postgres://flow:${POSTGRES_PASSWORD}@offboarding-postgres:5432/offboarding
      REDIS_URL: redis://offboarding-redis:6379/0
      APP_MODE: ${APP_MODE:-demo}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      offboarding-postgres: {condition: service_healthy}
      offboarding-redis: {condition: service_healthy}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    networks: [offboarding-net]

networks:
  offboarding-net:
    driver: bridge

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写 docker-compose.yml（生产） + docker-compose.dev.yml（本地 override） + init-db.sql</name>
  <files>docker-compose.yml, docker-compose.dev.yml, deploy/init-db.sql</files>
  <action>
    1. 写 `docker-compose.yml`：使用上面 [interfaces] 完整模板。注意：
       - `POSTGRES_PASSWORD:?...` 语法表示必须设置否则启动失败
       - volume 路径 `/data/offboarding/postgres-data` 假设部署 44 上预先 `mkdir -p`
       - `init-db.sql` 通过 bind mount 挂到 `/docker-entrypoint-initdb.d/`
       - 不要 expose 5432 (内部用 5432 / 外部映射 5433)
    2. 写 `docker-compose.dev.yml`（override）：
       ```yaml
       services:
         flow-api:
           volumes:
             - ./backend/src:/app/src
             - ./backend/migrations:/app/migrations
             - ./backend/alembic.ini:/app/alembic.ini
           environment:
             APP_MODE: demo
             LOG_LEVEL: DEBUG
           command: uv run uvicorn offboarding_flow.main:app --host 0.0.0.0 --port 8000 --reload
       ```
    3. 写 `deploy/init-db.sql`：
       ```sql
       -- 在 postgres 容器首次启动时执行，创建业务 schema + LangGraph schema
       -- 同时创建测试用 schema（_test 后缀），方便 pytest 隔离
       CREATE SCHEMA IF NOT EXISTS app AUTHORIZATION flow;
       CREATE SCHEMA IF NOT EXISTS langgraph AUTHORIZATION flow;
       CREATE SCHEMA IF NOT EXISTS app_test AUTHORIZATION flow;
       CREATE SCHEMA IF NOT EXISTS langgraph_test AUTHORIZATION flow;

       -- 默认 search_path：app 在最前（业务连接走这个），public 兜底
       ALTER ROLE flow SET search_path TO app, public;

       -- 创建 uuid-ossp 扩展（虽然 PG 16 内置 gen_random_uuid()，留 uuid-ossp 兜底）
       CREATE EXTENSION IF NOT EXISTS "uuid-ossp" SCHEMA public;
       ```
  </action>
  <verify>
    <automated>docker compose config 2>&1 | head -3 && cat /Users/admin/ai/resume/interview/liuxin/hr/deploy/init-db.sql | head -5</automated>
  </verify>
  <done>docker-compose.yml 和 docker-compose.dev.yml 语法通过 `docker compose config` 校验（即使容器没起来 config 校验也能跑）；init-db.sql 含 app/langgraph/app_test/langgraph_test 4 个 schema；本 Plan 不真起容器，留给 Plan 07 全流程冒烟</done>
</task>

<task type="auto">
  <name>Task 2: 写多阶段 Dockerfile + .dockerignore</name>
  <files>backend/Dockerfile, backend/.dockerignore</files>
  <action>
    1. 写 backend/Dockerfile（多阶段）：
       ```dockerfile
       # ---- Stage 1: builder ----
       FROM ghcr.io/astral-sh/uv:0.5-python3.12-bookworm-slim AS builder
       
       ENV UV_COMPILE_BYTECODE=1 \
           UV_LINK_MODE=copy \
           UV_PYTHON_DOWNLOADS=never
       
       WORKDIR /app
       
       # 1. 先 copy pyproject.toml + uv.lock 利用缓存层
       COPY pyproject.toml uv.lock .python-version ./
       RUN --mount=type=cache,target=/root/.cache/uv \
           uv sync --frozen --no-install-project --no-dev
       
       # 2. 再 copy 源码
       COPY src ./src
       RUN --mount=type=cache,target=/root/.cache/uv \
           uv sync --frozen --no-dev
       
       # ---- Stage 2: runtime ----
       FROM python:3.12-slim-bookworm AS runtime
       
       RUN apt-get update && apt-get install -y --no-install-recommends \
               wget \
               && rm -rf /var/lib/apt/lists/*
       
       WORKDIR /app
       
       # 拷贝 builder 阶段的 venv 与源码
       COPY --from=builder /app/.venv /app/.venv
       COPY --from=builder /app/src /app/src
       COPY migrations /app/migrations
       COPY alembic.ini /app/
       COPY entrypoint.sh /app/
       
       RUN chmod +x /app/entrypoint.sh
       
       ENV PATH="/app/.venv/bin:$PATH" \
           PYTHONUNBUFFERED=1 \
           PYTHONDONTWRITEBYTECODE=1 \
           PYTHONPATH=/app/src
       
       EXPOSE 8000
       
       ENTRYPOINT ["/app/entrypoint.sh"]
       CMD ["uvicorn", "offboarding_flow.main:app", "--host", "0.0.0.0", "--port", "8000"]
       ```
    2. 写 backend/.dockerignore：
       ```
       .venv/
       __pycache__/
       *.pyc
       .pytest_cache/
       .mypy_cache/
       .ruff_cache/
       htmlcov/
       .coverage
       tests/
       .git/
       .gitignore
       README*.md
       .env*
       ```
    3. **本 Task 不真 build image**（无法保证 build 环境有 docker）— 留给 Plan 07 冒烟时校验
  </action>
  <verify>
    <automated>test -f /Users/admin/ai/resume/interview/liuxin/hr/backend/Dockerfile && test -f /Users/admin/ai/resume/interview/liuxin/hr/backend/.dockerignore && grep -q "uv:0.5" /Users/admin/ai/resume/interview/liuxin/hr/backend/Dockerfile && grep -q "python:3.12-slim-bookworm" /Users/admin/ai/resume/interview/liuxin/hr/backend/Dockerfile</automated>
  </verify>
  <done>Dockerfile 含 builder + runtime 两阶段 + uv 0.5 + python 3.12-slim-bookworm + entrypoint.sh ENTRYPOINT；.dockerignore 排除 .venv/tests/.env</done>
</task>

<task type="auto">
  <name>Task 3: 写 entrypoint.sh（alembic + saver.setup + uvicorn 串联）+ CHANGELOG</name>
  <files>backend/entrypoint.sh, CHANGELOG.md</files>
  <action>
    1. 写 backend/entrypoint.sh：
       ```bash
       #!/bin/sh
       set -e
       
       echo "[entrypoint] === offboarding-flow-api starting ==="
       echo "[entrypoint] APP_MODE=${APP_MODE:-demo}"
       echo "[entrypoint] POSTGRES_DSN=${POSTGRES_DSN%@*}@***"  # 不打印密码部分
       
       # Step 1: 业务表 migration（alembic）
       echo "[entrypoint] >>> alembic upgrade head"
       alembic upgrade head
       
       # Step 2: LangGraph checkpoint 表（由 AsyncPostgresSaver.setup() 自管）
       echo "[entrypoint] >>> initializing LangGraph checkpointer schema"
       python -m offboarding_flow.flow_engine.checkpointer --setup
       
       # Step 3: 启动 uvicorn（exec 替换 PID 1 让信号正确传递）
       echo "[entrypoint] >>> exec uvicorn"
       exec "$@"
       ```
       说明：
       - 第 2 步通过 `python -m offboarding_flow.flow_engine.checkpointer --setup` 入口运行 `await AsyncPostgresSaver(...).setup()`；该入口在 Plan 04 实现，本 Plan 只占位写命令
       - 用 sh 不用 bash 以兼容 slim image
    2. chmod +x backend/entrypoint.sh（git 提交时保留权限）— 注意：用 `git update-index --chmod=+x backend/entrypoint.sh` 确保 git 记录可执行位
    3. 更新 CHANGELOG.md `[Unreleased] / Added`：
       - "2026-05-16 — Phase 1 Plan 02：Docker 编排（docker-compose.yml + dev override + init-db.sql 创建 app/langgraph 双 schema） + 多阶段 Dockerfile（uv builder + slim 3.12 runtime） + entrypoint.sh（alembic → checkpointer.setup → uvicorn）"
  </action>
  <verify>
    <automated>test -x /Users/admin/ai/resume/interview/liuxin/hr/backend/entrypoint.sh && grep -q "alembic upgrade head" /Users/admin/ai/resume/interview/liuxin/hr/backend/entrypoint.sh && grep -q "checkpointer --setup" /Users/admin/ai/resume/interview/liuxin/hr/backend/entrypoint.sh</automated>
  </verify>
  <done>entrypoint.sh 含 alembic upgrade head + checkpointer setup + exec uvicorn 三步；脚本有可执行权限；CHANGELOG 更新</done>
</task>

</tasks>

<verification>
1. docker compose config 校验 YAML 正确
2. docker-compose.dev.yml override 后 flow-api 服务定义合并正确
3. init-db.sql 含 4 个 schema + ALTER ROLE search_path
4. Dockerfile 多阶段 + 无密码硬编码
5. entrypoint.sh 三步串联（alembic → checkpointer.setup → uvicorn）
6. .dockerignore 排除 tests/.env/.venv
7. CHANGELOG 更新
</verification>

<success_criteria>
- 完整 Docker Compose 编排（postgres 5433 / redis 6380 / flow-api 8000）就绪
- init-db.sql 创建 app + langgraph 双 schema（PITFALLS #1 防 alembic 误删 langgraph 的前置）
- entrypoint.sh 串联 alembic + checkpointer setup + uvicorn（DEPLOY-05）
- 本 Plan 不要求真起容器（留给 Plan 07）— 文件落地即可
</success_criteria>

<output>
完成后写 `.planning/phases/01-langgraph-schema/01-02-SUMMARY.md`
</output>
