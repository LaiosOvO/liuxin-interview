---
phase: 01-langgraph-schema
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/src/offboarding_flow/config.py
  - backend/src/offboarding_flow/main.py
  - backend/src/offboarding_flow/api/__init__.py
  - backend/src/offboarding_flow/api/envelope.py
  - backend/src/offboarding_flow/api/errors.py
  - backend/src/offboarding_flow/api/health.py
  - backend/src/offboarding_flow/utils/__init__.py
  - backend/src/offboarding_flow/utils/logger.py
  - backend/tests/test_api_health.py
  - CHANGELOG.md
autonomous: true
requirements:
  - DEPLOY-01
must_haves:
  truths:
    - "Pydantic Settings 从 .env 读取所有配置（POSTGRES_DSN / LANGGRAPH_PG_CONNINFO / REDIS_URL / APP_MODE / LOG_LEVEL 等）；缺关键变量启动 fail fast"
    - "FastAPI 应用启动时 lifespan：init_db / build_graph / make_checkpointer；shutdown：dispose_engine / dispose_graph / dispose_checkpointer"
    - "GET /api/health 返回 200 + {success: true, data: {status, db, redis, graph, app_mode, version}}"
    - "全局响应 envelope：{success, data, error, meta}（CONTEXT §6 + 全局 patterns.md）"
    - "全局 exception handler：捕获 4xx/5xx 统一返回 envelope；不泄露 stacktrace"
    - "structlog 配置：dev 模式 colorized，prod 模式 JSON"
    - "启动 log 显式打印 APP_MODE（PITFALLS #10 防上线没切回 prod）"
    - "pytest backend/tests/test_api_health.py 通过：httpx.AsyncClient + asgi-lifespan 起 app，GET /api/health 返回 200"
  artifacts:
    - backend/src/offboarding_flow/config.py
    - backend/src/offboarding_flow/main.py（FastAPI app + lifespan）
    - backend/src/offboarding_flow/api/__init__.py
    - backend/src/offboarding_flow/api/envelope.py（响应 envelope helper）
    - backend/src/offboarding_flow/api/errors.py（exception handlers）
    - backend/src/offboarding_flow/api/health.py
    - backend/src/offboarding_flow/utils/logger.py
    - backend/tests/test_api_health.py
  key_links:
    - "main.py 的 lifespan 通过 try/except 捕获 init_db 失败 → log error + raise（PITFALLS #5）"
    - "config.py 用 SettingsConfigDict(env_file='.env', env_prefix='', case_sensitive=False)"
    - "config.py 暴露 postgres_dsn（asyncpg）+ postgres_dsn_sync（用于 alembic offline）"
    - "logger.py 在 main.py 启动时调用 configure_logging() — 先于其他模块 import"
---

<objective>
建立 FastAPI 应用骨架：用 Pydantic Settings 读取所有 .env 配置；FastAPI lifespan 串联 init_db / build_graph / make_checkpointer 的启动顺序；GET /api/health 返回组件状态；全局响应 envelope + exception handlers；structlog 配置 + 启动 log 打印 APP_MODE。

Purpose: 为 Plan 06（业务 API 端点 flows/nodes）提供 app + DI 容器 + 错误处理基础。本 Plan 不接业务逻辑（不导入 state_store / flow_engine 的具体业务方法），只搭框架 + health 检查；Plan 06 才把所有部件接起来。

Output: 可独立运行的最小 FastAPI app（uvicorn 起来 + /api/health 返回 200）。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-langgraph-schema/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.env.example
@backend/pyproject.toml

<interfaces>
<!-- API envelope（CONTEXT §6 + 全局 patterns.md） -->

# backend/src/offboarding_flow/api/envelope.py
from typing import Any, Generic, TypeVar
from pydantic import BaseModel

T = TypeVar("T")

class Envelope(BaseModel, Generic[T]):
    success: bool
    data: T | None = None
    error: str | None = None
    meta: dict[str, Any] | None = None

def ok(data: Any = None, meta: dict | None = None) -> Envelope:
    return Envelope(success=True, data=data, error=None, meta=meta)

def err(message: str, meta: dict | None = None) -> Envelope:
    return Envelope(success=False, data=None, error=message, meta=meta)


# backend/src/offboarding_flow/config.py
class Settings(BaseSettings):
    app_mode: Literal["demo", "prod"] = "demo"
    app_host: str = "0.0.0.0"
    app_port: int = 8000
    log_level: str = "INFO"
    
    postgres_password: str = "changeme_in_real_env"
    postgres_dsn: str = "postgresql+asyncpg://flow:changeme_in_real_env@offboarding-postgres:5432/offboarding"
    langgraph_pg_conninfo: str = "postgres://flow:changeme_in_real_env@offboarding-postgres:5432/offboarding"
    redis_url: str = "redis://offboarding-redis:6379/0"
    
    deeplink_base_url: str = "http://192.168.2.44"  # R1 待 Phase 6 修正
    
    @computed_field
    @property
    def postgres_dsn_sync(self) -> str:
        """Sync DSN 用于 alembic offline 模式。"""
        return self.postgres_dsn.replace("+asyncpg", "")
    
    model_config = SettingsConfigDict(
        env_file=".env", env_file_encoding="utf-8",
        case_sensitive=False, extra="ignore",
    )

# GET /api/health 响应 schema
class HealthResponse(BaseModel):
    status: Literal["ok", "degraded", "fail"]
    app_mode: str
    version: str
    components: dict[str, str]  # {db: "ok", redis: "ok", graph: "ok"}

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.py + utils/logger.py（structlog 配置 + APP_MODE 打印）</name>
  <files>backend/src/offboarding_flow/config.py, backend/src/offboarding_flow/utils/__init__.py, backend/src/offboarding_flow/utils/logger.py</files>
  <action>
    1. 写 `config.py`：使用 [interfaces] 的完整 Settings 类。
       ```python
       """全局配置 — 从 .env 读取。
       
       关键约定：
       - 用 BaseSettings 不要 dotenv 手动 load
       - get_settings() 用 lru_cache 单例（FastAPI Depends 友好）
       - postgres_dsn_sync 是 asyncpg DSN 去掉 +asyncpg → alembic offline 用
       - 不在这里放业务常量（节点名等放 flow_engine.nodes 常量；演示模式邮件相关放 Phase 4）
       """
       from __future__ import annotations
       
       import logging
       from functools import lru_cache
       from typing import Literal
       
       from pydantic import computed_field
       from pydantic_settings import BaseSettings, SettingsConfigDict
       
       
       class Settings(BaseSettings):
           # 应用基础
           app_mode: Literal["demo", "prod"] = "demo"
           app_host: str = "0.0.0.0"
           app_port: int = 8000
           log_level: str = "INFO"
           app_version: str = "0.1.0"
           
           # 数据库
           postgres_password: str = "changeme_in_real_env"
           postgres_dsn: str = "postgresql+asyncpg://flow:changeme_in_real_env@offboarding-postgres:5432/offboarding"
           langgraph_pg_conninfo: str = "postgres://flow:changeme_in_real_env@offboarding-postgres:5432/offboarding"
           
           # Redis（Phase 3 鉴权才真用，Phase 1 仅 health check）
           redis_url: str = "redis://offboarding-redis:6379/0"
           
           # 部署 URL
           deeplink_base_url: str = "http://192.168.2.44"
           
           @computed_field  # type: ignore[misc]
           @property
           def postgres_dsn_sync(self) -> str:
               """sync DSN 用于 alembic offline 模式（去掉 +asyncpg 后缀）。"""
               return self.postgres_dsn.replace("+asyncpg", "")
           
           @computed_field  # type: ignore[misc]
           @property
           def is_demo(self) -> bool:
               return self.app_mode == "demo"
           
           model_config = SettingsConfigDict(
               env_file=".env",
               env_file_encoding="utf-8",
               case_sensitive=False,
               extra="ignore",
           )
       
       
       @lru_cache(maxsize=1)
       def get_settings() -> Settings:
           """单例 Settings — 启动后冻结。"""
           settings = Settings()
           logger = logging.getLogger(__name__)
           # PITFALLS #10: 启动时显式打印 APP_MODE，防上线没切回 prod
           logger.warning("=" * 60)
           logger.warning("APP_MODE = %s", settings.app_mode.upper())
           logger.warning("APP_VERSION = %s", settings.app_version)
           logger.warning("=" * 60)
           return settings
       ```
    2. 写 `utils/__init__.py`：
       ```python
       """工具模块。"""
       from .logger import configure_logging, get_logger
       __all__ = ["configure_logging", "get_logger"]
       ```
    3. 写 `utils/logger.py`：
       ```python
       """structlog 配置 — dev 模式 colorized / prod 模式 JSON。
       
       在 main.py 启动时（lifespan 之前）调用 configure_logging()。
       """
       from __future__ import annotations
       
       import logging
       import sys
       from typing import Any
       
       import structlog
       
       
       def configure_logging(level: str = "INFO", app_mode: str = "demo") -> None:
           """配置 structlog + 标准库 logging。
           
           - dev (APP_MODE=demo) 用 ConsoleRenderer + colorized
           - prod (APP_MODE=prod) 用 JSONRenderer
           """
           log_level = getattr(logging, level.upper(), logging.INFO)
           
           processors: list[Any] = [
               structlog.contextvars.merge_contextvars,
               structlog.stdlib.add_log_level,
               structlog.stdlib.add_logger_name,
               structlog.processors.TimeStamper(fmt="iso"),
               structlog.processors.StackInfoRenderer(),
               structlog.processors.format_exc_info,
           ]
           
           if app_mode == "prod":
               processors.append(structlog.processors.JSONRenderer())
           else:
               processors.append(structlog.dev.ConsoleRenderer(colors=True))
           
           structlog.configure(
               processors=processors,
               logger_factory=structlog.stdlib.LoggerFactory(),
               wrapper_class=structlog.stdlib.BoundLogger,
               cache_logger_on_first_use=True,
           )
           
           # 标准库 logging 也用 structlog 格式
           handler = logging.StreamHandler(sys.stdout)
           handler.setFormatter(logging.Formatter("%(message)s"))
           root = logging.getLogger()
           root.handlers.clear()
           root.addHandler(handler)
           root.setLevel(log_level)
           
           # 减少 noise
           logging.getLogger("uvicorn.access").setLevel(logging.WARNING)
           logging.getLogger("sqlalchemy.engine").setLevel(logging.WARNING)
       
       
       def get_logger(name: str | None = None):
           """获取 structlog logger。"""
           return structlog.get_logger(name)
       ```
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run python -c "from offboarding_flow.config import Settings, get_settings; from offboarding_flow.utils.logger import configure_logging, get_logger; configure_logging('DEBUG', 'demo'); log = get_logger('test'); log.info('config + logger OK'); s = Settings(); assert s.app_mode == 'demo'; assert s.postgres_dsn_sync == s.postgres_dsn.replace('+asyncpg', ''); print('config OK; postgres_dsn_sync:', s.postgres_dsn_sync)"</automated>
  </verify>
  <done>Settings 类含所有 Phase 1 字段；postgres_dsn_sync computed field 工作；structlog 配置好；get_settings() lru_cache 单例；APP_MODE 启动 log 已实现</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI main.py（lifespan + envelope + 全局 exception handler）+ /api/health 路由</name>
  <files>backend/src/offboarding_flow/main.py, backend/src/offboarding_flow/api/__init__.py, backend/src/offboarding_flow/api/envelope.py, backend/src/offboarding_flow/api/errors.py, backend/src/offboarding_flow/api/health.py</files>
  <action>
    1. 写 `api/__init__.py`（暴露 health router 给 main.py）：
       ```python
       """FastAPI 路由模块。"""
       from .health import router as health_router
       __all__ = ["health_router"]
       ```
    2. 写 `api/envelope.py`：使用 [interfaces] 的 Envelope + ok/err helper。
       ```python
       """统一响应 envelope（CONTEXT §6 + 全局 patterns.md）。
       
       所有 API 响应必须包成 {success, data, error, meta}。
       前端通过 success 判断；data 拿数据；error 拿错误消息。
       """
       from __future__ import annotations
       
       from typing import Any, Generic, TypeVar
       
       from pydantic import BaseModel
       
       T = TypeVar("T")
       
       
       class Envelope(BaseModel, Generic[T]):
           success: bool
           data: T | None = None
           error: str | None = None
           meta: dict[str, Any] | None = None
       
       
       def ok(data: Any = None, meta: dict | None = None) -> dict[str, Any]:
           """成功响应。返回 dict 让 FastAPI 直接序列化（避免 Generic 序列化问题）。"""
           return {"success": True, "data": data, "error": None, "meta": meta}
       
       
       def err(message: str, meta: dict | None = None) -> dict[str, Any]:
           """失败响应。"""
           return {"success": False, "data": None, "error": message, "meta": meta}
       ```
    3. 写 `api/errors.py`（全局 exception handler）：
       ```python
       """全局 exception handler — 把所有未捕获异常包成 envelope。
       
       PITFALLS：不能泄露 stacktrace 到前端；要写日志 + 返回 generic message。
       """
       from __future__ import annotations
       
       import logging
       
       from fastapi import FastAPI, HTTPException, Request
       from fastapi.exceptions import RequestValidationError
       from fastapi.responses import JSONResponse
       
       from .envelope import err
       
       logger = logging.getLogger(__name__)
       
       
       def register_exception_handlers(app: FastAPI) -> None:
           """注册全局 exception handler。"""
           
           @app.exception_handler(HTTPException)
           async def http_exc_handler(_: Request, exc: HTTPException) -> JSONResponse:
               return JSONResponse(
                   status_code=exc.status_code,
                   content=err(str(exc.detail), meta={"status_code": exc.status_code}),
               )
           
           @app.exception_handler(RequestValidationError)
           async def validation_handler(_: Request, exc: RequestValidationError) -> JSONResponse:
               return JSONResponse(
                   status_code=422,
                   content=err("请求参数校验失败", meta={"errors": exc.errors()}),
               )
           
           @app.exception_handler(Exception)
           async def generic_handler(req: Request, exc: Exception) -> JSONResponse:
               logger.exception("[unhandled] %s %s — %s", req.method, req.url.path, exc)
               return JSONResponse(
                   status_code=500,
                   content=err("服务器内部错误，请稍后重试"),
               )
       ```
    4. 写 `api/health.py`：
       ```python
       """GET /api/health — 组件状态检查。
       
       Phase 1 检查：db / redis / graph singleton 是否就绪。
       不在乎"100% ok" 还是 "degraded" — 只要 200 即视为活着。
       """
       from __future__ import annotations
       
       import logging
       from typing import Any
       
       from fastapi import APIRouter
       from sqlalchemy import text
       
       from offboarding_flow.config import get_settings
       from offboarding_flow.state_store.session import get_engine
       
       from .envelope import ok
       
       router = APIRouter(prefix="/api", tags=["health"])
       logger = logging.getLogger(__name__)
       
       
       @router.get("/health")
       async def health_check() -> dict[str, Any]:
           settings = get_settings()
           components: dict[str, str] = {}
           
           # DB 检查
           try:
               engine = get_engine()
               async with engine.connect() as conn:
                   await conn.execute(text("SELECT 1"))
               components["db"] = "ok"
           except Exception as e:
               components["db"] = f"fail: {type(e).__name__}"
               logger.warning("[health] db check failed: %s", e)
           
           # Graph 检查（仅校验单例是否已 build）
           try:
               from offboarding_flow.flow_engine.graph import _graph  # noqa: F401
               components["graph"] = "ok" if _graph is not None else "not_built"
           except Exception as e:
               components["graph"] = f"fail: {type(e).__name__}"
           
           # Redis 检查（Phase 1 不接 Redis 业务，仅 ping；可暂时跳过）
           components["redis"] = "not_checked"  # Phase 3 引入 Redis 客户端后再 ping
           
           status = "ok" if components.get("db") == "ok" else "degraded"
           
           return ok({
               "status": status,
               "app_mode": settings.app_mode,
               "version": settings.app_version,
               "components": components,
           })
       ```
    5. 写 `main.py`（FastAPI app + lifespan）：
       ```python
       """FastAPI 应用入口。
       
       lifespan 顺序（启动）：
         configure_logging → get_settings() → init_db → build_graph → make_checkpointer
       lifespan 顺序（关闭）：
         dispose_graph → dispose_engine → dispose_checkpointer
       
       注意：本 Plan 不实现 flows/nodes 路由 — Plan 06 才接。
       """
       from __future__ import annotations
       
       import logging
       from contextlib import asynccontextmanager
       
       from fastapi import FastAPI
       
       from offboarding_flow.api import health_router
       from offboarding_flow.api.errors import register_exception_handlers
       from offboarding_flow.config import get_settings
       from offboarding_flow.utils.logger import configure_logging
       
       logger = logging.getLogger(__name__)
       
       
       @asynccontextmanager
       async def lifespan(app: FastAPI):
           """启动 / 关闭顺序管理。"""
           settings = get_settings()
           configure_logging(level=settings.log_level, app_mode=settings.app_mode)
           
           logger.info("[lifespan] starting — version=%s mode=%s", settings.app_version, settings.app_mode)
           
           # 启动顺序：DB → graph → checkpointer
           # 注意：Phase 1 容错 — DB 不通也能启动（health 会显示 fail），方便开发期
           try:
               from offboarding_flow.state_store.session import init_db
               await init_db()
               logger.info("[lifespan] DB connection OK")
           except Exception as e:
               logger.warning("[lifespan] DB init failed (will continue, /api/health will report): %s", e)
           
           try:
               from offboarding_flow.flow_engine.graph import build_graph
               await build_graph(use_memory_saver=False)
               logger.info("[lifespan] graph built with AsyncPostgresSaver")
           except Exception as e:
               logger.warning("[lifespan] graph build failed (will continue): %s", e)
           
           yield
           
           # 关闭
           logger.info("[lifespan] shutdown")
           try:
               from offboarding_flow.flow_engine.graph import dispose_graph
               from offboarding_flow.flow_engine.checkpointer import dispose_checkpointer
               from offboarding_flow.state_store.session import dispose_engine
               await dispose_graph()
               await dispose_checkpointer()
               await dispose_engine()
           except Exception as e:
               logger.warning("[lifespan] shutdown error: %s", e)
       
       
       def create_app() -> FastAPI:
           settings = get_settings()
           app = FastAPI(
               title="offboarding-flow",
               version=settings.app_version,
               description="AI 驱动的离职流程执行系统（Phase 1: LangGraph 骨架）",
               lifespan=lifespan,
           )
           
           register_exception_handlers(app)
           app.include_router(health_router)
           # Plan 06 will mount flows_router + nodes_router
           
           return app
       
       
       # uvicorn 入口
       app = create_app()
       ```
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run python -c "from offboarding_flow.main import create_app; app = create_app(); routes = [r.path for r in app.routes]; assert '/api/health' in routes, f'/api/health missing in {routes}'; print('app + /api/health route OK')"</automated>
  </verify>
  <done>main.py 含 lifespan + create_app；envelope.py / errors.py / health.py 落地；create_app() 不抛错；/api/health route 注册到 app.routes</done>
</task>

<task type="auto">
  <name>Task 3: /api/health 集成测试（httpx + asgi-lifespan） + CHANGELOG</name>
  <files>backend/tests/test_api_health.py, CHANGELOG.md</files>
  <action>
    1. 写 `backend/tests/test_api_health.py`：
       ```python
       """GET /api/health 集成测试 — 用 asgi-lifespan + httpx.AsyncClient。
       
       不依赖真数据库 — DB 不通时 /api/health 仍应返回 200（status='degraded'）。
       """
       import pytest
       from asgi_lifespan import LifespanManager
       from httpx import ASGITransport, AsyncClient
       
       from offboarding_flow.main import create_app
       
       
       @pytest.fixture
       async def client():
           app = create_app()
           async with LifespanManager(app):
               transport = ASGITransport(app=app)
               async with AsyncClient(transport=transport, base_url="http://test") as ac:
                   yield ac
       
       
       async def test_health_endpoint_returns_200(client):
           """health 端点必须始终返回 200（不论 DB 是否通）。"""
           resp = await client.get("/api/health")
           assert resp.status_code == 200
           body = resp.json()
           assert body["success"] is True
           assert "status" in body["data"]
           assert body["data"]["status"] in ("ok", "degraded")
       
       
       async def test_health_envelope_shape(client):
           """响应必须是 {success, data, error, meta} envelope。"""
           resp = await client.get("/api/health")
           body = resp.json()
           assert set(body.keys()) == {"success", "data", "error", "meta"}
           assert body["success"] is True
           assert body["error"] is None
       
       
       async def test_health_reports_app_mode(client):
           """data 含 app_mode + version + components。"""
           resp = await client.get("/api/health")
           data = resp.json()["data"]
           assert "app_mode" in data
           assert "version" in data
           assert "components" in data
           assert "db" in data["components"]
           assert "graph" in data["components"]
       
       
       async def test_404_returns_envelope(client):
           """未注册路由 404 仍是 envelope。"""
           resp = await client.get("/api/nonexistent")
           assert resp.status_code == 404
           body = resp.json()
           # FastAPI 默认 404 不走我们的 HTTPException handler — 但 405 / 422 会走
           # 这里我们只校验响应是 JSON
           assert isinstance(body, dict)
       
       
       async def test_validation_error_returns_envelope(client):
           """请求体校验错误返回 envelope（422）— 用 POST /api/health 触发 Method Not Allowed。"""
           resp = await client.post("/api/health", json={})
           # GET-only 路由 POST 会返回 405
           assert resp.status_code in (405, 422)
           body = resp.json()
           # 405 走 HTTPException handler
           if resp.status_code == 405:
               assert body["success"] is False
               assert body["error"] is not None
       ```
    2. 更新 CHANGELOG.md `[Unreleased] / Added`：
       - "2026-05-16 — Phase 1 Plan 05：FastAPI 应用骨架（Pydantic Settings 配置中心 + lifespan 串联 DB/graph/checkpointer 启动顺序 + structlog 配置 + 启动 log 打印 APP_MODE PITFALLS #10 + 全局响应 envelope {success,data,error,meta} + 全局 exception handler + GET /api/health 组件状态检查 + httpx/asgi-lifespan 集成测试）"
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run pytest tests/test_api_health.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>tests/test_api_health.py 5 个用例通过；/api/health 返回 envelope；CHANGELOG 更新</done>
</task>

</tasks>

<verification>
1. config.py + utils/logger.py 落地，可 import
2. main.py 含 lifespan，create_app() 可调用且不报错
3. /api/health 注册到 app.routes
4. pytest tests/test_api_health.py 通过
5. 启动 log 打印 APP_MODE（在 get_settings() warning 日志里）
6. CHANGELOG 更新
</verification>

<success_criteria>
- FastAPI app + lifespan 骨架完整
- 启动顺序：configure_logging → init_db → build_graph 都有容错（不阻断启动 — 方便开发期 DB 没起来也能 import）
- /api/health 端点返回 envelope + 组件状态
- 全局 envelope + exception handler 工作正常
- httpx + asgi-lifespan 集成测试通过（不依赖真 DB）
</success_criteria>

<output>
完成后写 `.planning/phases/01-langgraph-schema/01-05-SUMMARY.md`
</output>
