---
phase: 01-langgraph-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/.python-version
  - backend/src/offboarding_flow/__init__.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - .pre-commit-config.yaml
  - .env.example
  - .gitignore
  - CHANGELOG.md
autonomous: true
requirements:
  - DEPLOY-04
must_haves:
  truths:
    - "uv sync 在 backend/ 目录可成功安装所有依赖（含 LangGraph 1.2 / FastAPI 0.136.1 / SQLAlchemy 2 async / asyncpg / psycopg[binary] / aiosmtplib / openai / pyjwt / structlog / tenacity / httpx / polyfactory / APScheduler / pytest / pytest-asyncio / ruff / mypy）"
    - "pre-commit run --all-files 通过（gitleaks + ruff + ruff-format + mypy + check-yaml + end-of-file-fixer + trailing-whitespace + no-commit-to-branch=main）"
    - "pytest backend/tests/ -q 通过（即便没测试也不能报 collection error）"
    - ".env.example 不含任何真值；只用 changeme_in_real_env 等占位符"
  artifacts:
    - backend/pyproject.toml（含 [project] / [tool.uv] / [tool.ruff] / [tool.mypy] / [tool.pytest.ini_options]）
    - backend/uv.lock
    - .pre-commit-config.yaml（gitleaks + ruff + mypy + 系统钩子）
    - .env.example（包含 Phase 1 所有变量 + 部分 Phase 4/3 占位 — 凭证只用占位符）
    - backend/tests/conftest.py（pytest-asyncio mode=auto + loop_scope=session 全局 fixture）
    - CHANGELOG.md（[Unreleased] 段追加 "Added: Phase 1 项目骨架"）
  key_links:
    - "pyproject.toml 中 [tool.uv.workspace] 不需要（单包项目）"
    - "pyproject.toml 中 [tool.ruff] target-version 锁 py312"
    - "conftest.py 中 fixture loop_scope=session 防止 PITFALLS #23 测试 hang"
---

<objective>
建立 Phase 1 的项目骨架与工具链基线：用 uv 初始化 src layout Python 项目；锁定所有 v1 依赖；配置 pre-commit（gitleaks 强制 + ruff/mypy/check-yaml）；扩展 `.env.example` 覆盖 Phase 1 必需变量（不含真值）；建立 pytest 异步测试 fixture 骨架；CHANGELOG 追加。

Purpose: 后续所有 plan（02-07）都依赖此骨架的 pyproject + venv + lint。这是 Phase 1 的"第一个 commit 之前必有"基线（CONTEXT §9 + PITFALLS #8）。

Output: backend/ 目录 + 工具链 + pre-commit + 更新后的根 .env.example / .gitignore / CHANGELOG。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/01-langgraph-schema/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.gitignore
@.env.example

<interfaces>
<!-- backend/pyproject.toml 必备依赖清单（来自 SUMMARY §Recommended Stack + CONTEXT §3） -->

[project] 关键依赖（runtime）：
  - "fastapi>=0.136.1,<0.137"
  - "uvicorn[standard]>=0.32,<0.33"
  - "pydantic>=2.10,<3"
  - "pydantic-settings>=2.6,<3"
  - "sqlalchemy[asyncio]>=2.0.36,<3"
  - "asyncpg>=0.30,<0.31"
  - "alembic>=1.14,<2"
  - "psycopg[binary]>=3.2,<4"  # LangGraph checkpointer 底层
  - "langgraph>=1.2.0,<2"
  - "langgraph-checkpoint-postgres>=3.1.0,<4"
  - "redis>=5.2,<6"
  - "structlog>=24.4,<25"
  - "tenacity>=9,<10"
  - "httpx>=0.28,<0.29"
  - "python-multipart>=0.0.20"  # FastAPI form support（health 不用但日后用）

[project.optional-dependencies] dev 依赖：
  - "pytest>=8.3,<9"
  - "pytest-asyncio>=0.24,<0.25"
  - "pytest-cov>=6,<7"
  - "polyfactory>=2.18,<3"
  - "asgi-lifespan>=2.1,<3"
  - "httpx>=0.28,<0.29"
  - "ruff>=0.8,<0.9"
  - "mypy>=1.13,<2"

[tool.ruff] target-version="py312", line-length=100, src=["src"], select=["E","F","W","I","B","UP","SIM","RUF"], ignore=["E501"]
[tool.mypy] python_version="3.12", strict=false, ignore_missing_imports=true, plugins=["pydantic.mypy"]
[tool.pytest.ini_options] asyncio_mode="auto", asyncio_default_fixture_loop_scope="session", testpaths=["tests"]
[tool.uv] dev-dependencies 复用 optional-dependencies dev

[tool.hatch.build.targets.wheel] packages=["src/offboarding_flow"]  # 或换 setuptools — Claude 自行决定

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 backend/ uv 项目并锁定所有 v1 依赖</name>
  <files>backend/pyproject.toml, backend/.python-version, backend/uv.lock, backend/src/offboarding_flow/__init__.py</files>
  <action>
    1. mkdir -p backend/src/offboarding_flow backend/tests
    2. cd backend && uv init --no-readme --package（如果 uv init 不支持 --package 则手写 pyproject.toml）
    3. 写 backend/pyproject.toml：使用 `[interfaces]` 块的完整依赖清单 + ruff/mypy/pytest 配置 + hatchling 或 setuptools build-backend
    4. 写 backend/.python-version 内容为 `3.12`
    5. 写 backend/src/offboarding_flow/__init__.py：`__version__ = "0.1.0"`
    6. cd backend && uv sync --all-extras 安装 venv + uv.lock 生成
    7. 验证 `uv run python -c "import langgraph; import fastapi; import sqlalchemy; print('ok')"` 正常输出
  </action>
  <verify>
    <automated>cd backend && uv sync --all-extras && uv run python -c "import langgraph, fastapi, sqlalchemy, asyncpg, psycopg, redis, structlog, tenacity, httpx, pydantic; from langgraph_checkpoint_postgres.aio import AsyncPostgresSaver; print('all imports ok')"</automated>
  </verify>
  <done>backend/pyproject.toml 含所有 [interfaces] 列出的依赖；backend/uv.lock 提交；uv sync 0 错误；上面 import 校验通过</done>
</task>

<task type="auto">
  <name>Task 2: 配置 pre-commit hooks（gitleaks 强制） + 扩展根 .gitignore / .env.example / CHANGELOG</name>
  <files>.pre-commit-config.yaml, .gitignore, .env.example, CHANGELOG.md</files>
  <action>
    1. 写 `.pre-commit-config.yaml`：使用 CONTEXT §9 给定的完整模板（gitleaks v8.21.2 + ruff v0.8.0 fix + ruff-format + mypy v1.13.0 + check-yaml + end-of-file-fixer + trailing-whitespace + no-commit-to-branch main）
    2. 更新根 .gitignore：在现有内容基础上追加 `backend/.venv/`、`backend/uv.lock` **不要忽略**（lock 文件必须入 git）、`*.pyc`、`htmlcov/`、`.coverage`、`coverage.xml`、`backend/src/**/__pycache__/`、`backend/data/`
    3. 重写根 .env.example：保留现有内容（演示模式 / Mattermost / SMTP / JWT / MinIO / GLM 占位），新增 Phase 1 段：
       ```
       # =========================================
       # Phase 1: 数据库 / Redis / 日志
       # =========================================
       POSTGRES_PASSWORD=changeme_in_real_env
       POSTGRES_DSN=postgresql+asyncpg://flow:changeme_in_real_env@offboarding-postgres:5432/offboarding
       LANGGRAPH_PG_CONNINFO=postgres://flow:changeme_in_real_env@offboarding-postgres:5432/offboarding
       REDIS_URL=redis://offboarding-redis:6379/0
       LOG_LEVEL=INFO
       APP_HOST=0.0.0.0
       APP_PORT=8000
       ```
       注意：所有真值占位必须是 `changeme_in_real_env` 或 `__placeholder__`，不要写任何真凭证。**保留原有 DEEPLINK_BASE_URL=http://192.168.2.44:3000**（Phase 6 才修正 R1，本 phase 不动）。
    4. 更新 CHANGELOG.md `[Unreleased] / Added` 段追加：
       - "2026-05-16 — Phase 1 Plan 01：初始化 backend/ uv 项目（pyproject.toml + uv.lock + .python-version）+ pre-commit hooks（gitleaks v8.21.2 + ruff + mypy + check-yaml）+ 扩展 .env.example 含 Phase 1 数据库/Redis 占位"
    5. 安装 pre-commit hooks: `cd <project_root> && pre-commit install`（如果 pre-commit CLI 不存在则 pipx install pre-commit）
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr && pre-commit run --all-files</automated>
  </verify>
  <done>.pre-commit-config.yaml 落地；pre-commit run --all-files 通过（可能首次会 auto-fix 一些 trailing whitespace，重跑必须通过）；.env.example 含 Phase 1 段且无任何真凭证；CHANGELOG 已更新</done>
</task>

<task type="auto">
  <name>Task 3: 建立 pytest 异步 fixture 骨架 + tests/__init__</name>
  <files>backend/tests/__init__.py, backend/tests/conftest.py</files>
  <action>
    1. 写 backend/tests/__init__.py（空文件即可）
    2. 写 backend/tests/conftest.py：
       ```python
       """Pytest 全局 fixture 与配置。

       - asyncio mode=auto + loop_scope=session（防 PITFALLS #23 测试 hang）
       - 后续 Plan 03/04/06 会扩充 db / graph fixture
       """
       import asyncio
       import pytest
       
       @pytest.fixture(scope="session")
       def event_loop_policy():
           """显式声明事件循环策略，避免不同平台行为差异。"""
           return asyncio.DefaultEventLoopPolicy()
       
       @pytest.fixture(scope="session")
       def anyio_backend() -> str:
           """如果有 anyio 测试，固定为 asyncio。"""
           return "asyncio"
       ```
    3. 跑 `cd backend && uv run pytest -q` 验证 collection 不报错（0 个测试也 OK）
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>backend/tests/conftest.py 落地；pytest --collect-only 不报 collection error；不报 fixture loop_scope warning</done>
</task>

</tasks>

<verification>
1. backend/pyproject.toml + uv.lock 已 git tracked
2. uv sync --all-extras 成功
3. 所有关键依赖可 import（langgraph / fastapi / sqlalchemy / asyncpg / psycopg / redis / structlog / pydantic / pyjwt 已暂不需要 — JWT Phase 3 才加；本 Plan 暂不锁 pyjwt）
4. pre-commit run --all-files 0 error
5. .env.example 含 Phase 1 段 + 凭证均用占位
6. CHANGELOG `[Unreleased] / Added` 更新
7. pytest --collect-only 不报错
</verification>

<success_criteria>
- backend/ 目录 src layout 就绪
- uv lock 后所有依赖可 import
- pre-commit gitleaks 装上且能拦截真凭证
- .env.example 不含任何真值，模板覆盖 Phase 1 必需变量
- pytest 异步 fixture 就绪（不抛 fixture-loop-scope warning）
- CHANGELOG 更新到 [Unreleased]
</success_criteria>

<output>
完成后写 `.planning/phases/01-langgraph-schema/01-01-SUMMARY.md`
</output>
