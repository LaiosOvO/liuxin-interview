---
phase: 01-langgraph-schema
plan: 07
type: execute
wave: 3
depends_on: [02, 06]
files_modified:
  - backend/tests/e2e/__init__.py
  - backend/tests/e2e/test_full_flow.py
  - backend/tests/e2e/README.md
  - frontend/tests/e2e/README.md
  - frontend/tests/e2e/.gitkeep
  - scripts/smoke_test.sh
  - scripts/dev_up.sh
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "backend/tests/e2e/test_full_flow.py 提供 Phase 1 全流程冒烟测试骨架（curl-based 通过 httpx 跑真后端容器） — 可被 pytest -m e2e 跳过 / 显式运行"
    - "scripts/smoke_test.sh 给人手敲运行：起 docker compose → 等 health → curl POST /api/flows → curl POST advance → 校验响应"
    - "scripts/dev_up.sh 简化本地开发：docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build"
    - "frontend/tests/e2e/ 目录创建占位 + README 说明 Phase 5 才落 Playwright spec"
    - "backend/tests/e2e/ 目录创建 + README 说明 Phase 1 范围 + 后续 phase 扩展点（含'演示模式默认场景'/'演示模式异常场景'多个用例占位 — CLAUDE.md §2.1）"
    - "CHANGELOG 标 Phase 1 完成 + 下一步指向 Phase 2"
  artifacts:
    - backend/tests/e2e/__init__.py
    - backend/tests/e2e/test_full_flow.py（Phase 1 冒烟）
    - backend/tests/e2e/README.md
    - frontend/tests/e2e/.gitkeep + README.md
    - scripts/smoke_test.sh
    - scripts/dev_up.sh
    - CHANGELOG.md（Phase 1 完成段）
  key_links:
    - "test_full_flow.py 默认 skip（除非 OFFBOARDING_E2E_BASE_URL 环境变量设置）— 防 CI 误跑"
    - "Phase 5 才填 frontend/tests/e2e Playwright spec；Phase 1 仅占位"
    - "Phase 2 全流程会扩展 backend/tests/e2e/test_full_flow.py 用例（zhang.san 全 10 节点）"
---

<objective>
建立 Phase 1 E2E 测试骨架 + 给手动冒烟提供 shell 脚本 + 给 Phase 5（前端）预留 Playwright 测试目录 + 更新 CHANGELOG 标 Phase 1 完成。

Purpose: CLAUDE.md §2.1/§2.2 要求"每个 phase 完成后必须跑全流程 E2E"+ "E2E 用 browser-harness（webapp-testing）"，但 Phase 1 无 UI，所以本 Plan：
- 后端 E2E：用 httpx 跑 docker compose 起来的真后端容器（curl 等价），覆盖 Phase 1 验收 5 项
- 前端 E2E：仅创建目录骨架 + README 指引（Phase 5 才填）
- 手动冒烟脚本：scripts/smoke_test.sh 给人复跑

Output: Phase 1 验收可一键脚本化跑通 + 测试结构为后续 phase 留好扩展点。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-langgraph-schema/01-CONTEXT.md
@.planning/ROADMAP.md
@docker-compose.yml
@backend/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/tests/e2e/ 全流程冒烟测试 + 占位 README</name>
  <files>backend/tests/e2e/__init__.py, backend/tests/e2e/test_full_flow.py, backend/tests/e2e/README.md</files>
  <action>
    1. 写 `backend/tests/e2e/__init__.py`（空）
    2. 写 `backend/tests/e2e/README.md`：
       ```markdown
       # 后端 E2E 测试
       
       ## 范围
       
       这里放**真后端容器**（docker compose 起来）的端到端测试，不用 mock。
       - Phase 1 (本目录)：`test_full_flow.py` 覆盖最小 2 节点 + docker restart 恢复
       - Phase 2：扩展到 10 节点 + 并行 fan-out + 申请人最终确认
       - Phase 4：邮件 / Mattermost / GLM 摘要降级 / 演示模式收件箱
       - Phase 4.5：自动节点 + mock-archive-service
       - Phase 5：浏览器 E2E（请见 `frontend/tests/e2e/`）
       - Phase 6：超时扫描 + 整体演示 runbook
       
       ## 怎么跑
       
       **手动冒烟**（推荐）：
       ```bash
       # 1. 起容器
       bash scripts/dev_up.sh
       
       # 2. 跑冒烟脚本（curl-based，给人看的）
       bash scripts/smoke_test.sh
       
       # 3. 跑 pytest E2E（python httpx，更精确）
       export OFFBOARDING_E2E_BASE_URL=http://localhost:8000
       cd backend && uv run pytest tests/e2e/ -v -m e2e
       ```
       
       **CI 不跑 E2E**：默认 pytest 排除 -m e2e（pyproject.toml `[tool.pytest.ini_options]` 已加 `markers = ["e2e: 需要真后端容器，默认 skip"]` 和 `addopts = "-m 'not e2e'"`）
       
       ## 演示模式场景清单（CLAUDE.md §2.1）
       
       下列场景会在对应 phase 加用例：
       
       | 场景 | Phase | 用例文件 |
       |------|-------|---------|
       | 起流程 → 10 节点全部 advance 走完 | 2 | test_full_flow.py::test_happy_path |
       | result_text 校验 + node_results 聚合 | 2 | test_full_flow.py::test_aggregation |
       | 申请人邮件聚合 + GLM 摘要 | 4 | test_full_flow.py::test_applicant_summary |
       | 任意节点 return（回退到上游） | 2 | test_full_flow.py::test_return_path |
       | 任意关键节点 reject（终止） | 2 | test_full_flow.py::test_reject_path |
       | 超时模拟（timeout_scan 触发） | 6 | test_full_flow.py::test_timeout_simulate |
       | 证据缺失检测 | 4 | test_full_flow.py::test_evidence_missing |
       | GLM API down（降级） | 4 | test_full_flow.py::test_glm_degradation |
       | QQ SMTP down（outbox 重试） | 4 | test_full_flow.py::test_smtp_outbox_retry |
       | 进程崩溃 docker restart 恢复 | **1（本 phase）** | test_full_flow.py::test_docker_restart_recovers |
       
       ## 浏览器 E2E（browser-harness）
       
       走 `webapp-testing` skill（Playwright 后端），spec 在 `frontend/tests/e2e/` — Phase 5 才落地。
       本目录只放"curl-based 后端 E2E"。
       ```
    3. 写 `backend/tests/e2e/test_full_flow.py`：
       ```python
       """Phase 1 后端全流程冒烟测试 — 用 httpx 打真后端容器（无 mock）。
       
       默认 skip — 通过 OFFBOARDING_E2E_BASE_URL 环境变量启用：
         export OFFBOARDING_E2E_BASE_URL=http://localhost:8000
         uv run pytest tests/e2e/ -v -m e2e
       
       Phase 2+ 会在本文件追加更多场景（见 README.md）。
       """
       from __future__ import annotations
       
       import os
       import time
       
       import httpx
       import pytest
       
       _BASE_URL = os.environ.get("OFFBOARDING_E2E_BASE_URL")
       
       pytestmark = [
           pytest.mark.e2e,
           pytest.mark.skipif(
               not _BASE_URL,
               reason="需要 OFFBOARDING_E2E_BASE_URL 环境变量指向运行中的后端容器",
           ),
       ]
       
       
       @pytest.fixture
       async def client():
           async with httpx.AsyncClient(base_url=_BASE_URL, timeout=10.0) as ac:
               yield ac
       
       
       async def test_health_endpoint_returns_200(client):
           """Phase 1 验收 #1: uvicorn 起来 /api/health 返回 200."""
           resp = await client.get("/api/health")
           assert resp.status_code == 200
           body = resp.json()
           assert body["success"] is True
           assert body["data"]["status"] in ("ok", "degraded")
       
       
       async def test_create_flow_and_advance_full_loop(client):
           """Phase 1 验收 #2+#3+#5: 起流程 + advance + 业务表与 checkpoint 一致."""
           # Step 1: 起流程
           resp = await client.post("/api/flows", json={"employee_id": "zhang.san"})
           assert resp.status_code == 200, resp.text
           data = resp.json()["data"]
           flow_id = data["flow_id"]
           node_id = data["current_node"]["id"]
           assert data["current_node"]["name"] == "manager_review"
           assert data["current_node"]["status"] == "waiting_human"
           
           # Step 2: GET /api/flows/{id}/nodes 校验 apply + manager_review 都在
           resp = await client.get(f"/api/flows/{flow_id}/nodes")
           assert resp.status_code == 200
           names = sorted(n["name"] for n in resp.json()["data"])
           assert names == ["apply", "manager_review"]
           
           # Step 3: advance manager_review
           resp = await client.post(
               f"/api/flows/{flow_id}/nodes/{node_id}/actions",
               json={"action": "advance", "result_text": "同意离职", "actor": "li.si"},
           )
           assert resp.status_code == 200, resp.text
           assert resp.json()["data"]["new_status"] == "done"
           
           # Step 4: 重读 — 节点 done 流程 completed
           resp = await client.get(f"/api/flows/{flow_id}")
           assert resp.json()["data"]["status"] == "completed"
       
       
       @pytest.mark.skip(reason="Phase 1 不强制 — 需手动 docker compose restart flow-api 后调用")
       async def test_docker_restart_recovers(client):
           """Phase 1 验收 #4: docker restart 后流程进度无丢失。
           
           手动验证步骤：
             1. 跑 test_create_flow_and_advance_full_loop 到 Step 2 后暂停
             2. docker compose restart flow-api
             3. 等待 /api/health 200
             4. GET /api/flows/{id} 仍能查到节点状态
             5. POST advance 仍能推进
           
           自动化版本：留 Phase 2 完整双写规范后实现（需 docker SDK / subprocess 控制）。
           """
           pass
       
       
       # Phase 2+ 用例占位（这里只列名，Phase 2 才填实现）：
       # async def test_happy_path_full_10_nodes(client): ...
       # async def test_return_path_node_5_back_to_node_3(client): ...
       # async def test_reject_path_manager_review_terminates_flow(client): ...
       # async def test_aggregation_node_results_in_applicant_confirm(client): ...
       ```
  </action>
  <verify>
    <automated>test -f /Users/admin/ai/resume/interview/liuxin/hr/backend/tests/e2e/__init__.py && test -f /Users/admin/ai/resume/interview/liuxin/hr/backend/tests/e2e/test_full_flow.py && test -f /Users/admin/ai/resume/interview/liuxin/hr/backend/tests/e2e/README.md && cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run pytest tests/e2e/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>backend/tests/e2e/ 目录 3 文件落地；pytest --collect-only 能识别（但默认 skip，因为没有 OFFBOARDING_E2E_BASE_URL）</done>
</task>

<task type="auto">
  <name>Task 2: 前端 E2E 目录占位 + scripts/dev_up.sh + scripts/smoke_test.sh</name>
  <files>frontend/tests/e2e/.gitkeep, frontend/tests/e2e/README.md, scripts/dev_up.sh, scripts/smoke_test.sh</files>
  <action>
    1. mkdir -p frontend/tests/e2e + 写 `.gitkeep`（空）
    2. 写 `frontend/tests/e2e/README.md`：
       ```markdown
       # 前端 E2E 测试（Phase 5 才填）
       
       ## 范围
       
       这里放浏览器 E2E（Playwright spec）。Phase 1（本提交）只占位，实际 spec Phase 5 才落地。
       
       ## 实施方案（CLAUDE.md §2.2）
       
       - 主要用 `webapp-testing` skill（Playwright 后端）做浏览器自动化
       - spec 文件命名：`xxx.spec.ts`（Playwright 默认约定）
       - 覆盖场景：
         - 邮件深链点击 → 自动登录 → 看到正确角色视图 → 三态决策提交 → 浏览器看到流程推进
         - HR Dashboard 列出所有流程 + 筛选 + 重发通知按钮
         - 申请人最终确认页时间线 + GLM 摘要展示
         - Mattermost @bot 命令触发后浏览器端能看到 / 收到对应变化
       
       ## 怎么跑（Phase 5 落地后）
       
       ```bash
       cd frontend
       pnpm install
       pnpm exec playwright install chromium
       pnpm test:e2e   # 跑所有 .spec.ts
       ```
       
       ## Phase 1 / Phase 2 / Phase 3 / Phase 4
       
       这些 phase 没有 UI，只跑 `backend/tests/e2e/`（curl-based 后端 E2E）。
       ```
    3. 写 `scripts/dev_up.sh`：
       ```bash
       #!/bin/sh
       # 本地开发启动 — 起 postgres + redis + flow-api 三个容器
       # 用 docker-compose.dev.yml override 启用 volume mount + --reload
       
       set -e
       
       SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
       ROOT_DIR=$(dirname "$SCRIPT_DIR")
       
       cd "$ROOT_DIR"
       
       if [ ! -f .env ]; then
         echo "[dev_up] .env 不存在，从 .env.example 复制（请编辑并设置真实凭证）"
         cp .env.example .env
         echo "[dev_up] >>> 请编辑 .env 设置 POSTGRES_PASSWORD 等真实值，再重跑本脚本"
         exit 1
       fi
       
       echo "[dev_up] 起容器（postgres + redis + flow-api）..."
       docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
       
       echo "[dev_up] 等 flow-api 健康..."
       for i in $(seq 1 30); do
         if curl -sf http://localhost:8000/api/health > /dev/null 2>&1; then
           echo "[dev_up] ✓ flow-api 已健康"
           curl -s http://localhost:8000/api/health
           echo
           exit 0
         fi
         sleep 2
         printf "."
       done
       
       echo
       echo "[dev_up] ✗ flow-api 60s 内未健康，检查日志:"
       docker compose logs --tail 50 flow-api
       exit 1
       ```
    4. 写 `scripts/smoke_test.sh`：
       ```bash
       #!/bin/sh
       # Phase 1 手动冒烟脚本 — 跑 health + 起流程 + advance 全链路
       # 默认 BASE_URL=http://localhost:8000；部署到 44 时 export BASE_URL=http://192.168.2.44:8000
       
       set -e
       
       BASE_URL="${BASE_URL:-http://localhost:8000}"
       
       echo "[smoke] BASE_URL=$BASE_URL"
       
       echo
       echo "=== Step 1: GET /api/health ==="
       curl -sf "$BASE_URL/api/health" | python3 -m json.tool
       
       echo
       echo "=== Step 2: POST /api/flows ==="
       RESP=$(curl -sf -X POST "$BASE_URL/api/flows" \
         -H "Content-Type: application/json" \
         -d '{"employee_id":"zhang.san"}')
       echo "$RESP" | python3 -m json.tool
       
       FLOW_ID=$(echo "$RESP" | python3 -c "import json,sys; print(json.load(sys.stdin)['data']['flow_id'])")
       NODE_ID=$(echo "$RESP" | python3 -c "import json,sys; print(json.load(sys.stdin)['data']['current_node']['id'])")
       
       echo
       echo "[smoke] flow_id=$FLOW_ID node_id=$NODE_ID"
       
       echo
       echo "=== Step 3: GET /api/flows/$FLOW_ID/nodes ==="
       curl -sf "$BASE_URL/api/flows/$FLOW_ID/nodes" | python3 -m json.tool
       
       echo
       echo "=== Step 4: POST /api/flows/$FLOW_ID/nodes/$NODE_ID/actions advance ==="
       curl -sf -X POST "$BASE_URL/api/flows/$FLOW_ID/nodes/$NODE_ID/actions" \
         -H "Content-Type: application/json" \
         -d '{"action":"advance","result_text":"同意离职 — smoke 测试","actor":"li.si"}' \
         | python3 -m json.tool
       
       echo
       echo "=== Step 5: GET /api/flows/$FLOW_ID （应 status=completed） ==="
       curl -sf "$BASE_URL/api/flows/$FLOW_ID" | python3 -m json.tool
       
       echo
       echo "[smoke] ✓ Phase 1 冒烟通过"
       ```
    5. chmod +x scripts/dev_up.sh + scripts/smoke_test.sh
    6. git update-index --chmod=+x scripts/dev_up.sh + scripts/smoke_test.sh（确保 git 记录可执行位）
  </action>
  <verify>
    <automated>test -x /Users/admin/ai/resume/interview/liuxin/hr/scripts/dev_up.sh && test -x /Users/admin/ai/resume/interview/liuxin/hr/scripts/smoke_test.sh && test -f /Users/admin/ai/resume/interview/liuxin/hr/frontend/tests/e2e/.gitkeep && test -f /Users/admin/ai/resume/interview/liuxin/hr/frontend/tests/e2e/README.md && grep -q "POST /api/flows" /Users/admin/ai/resume/interview/liuxin/hr/scripts/smoke_test.sh</automated>
  </verify>
  <done>scripts/dev_up.sh + scripts/smoke_test.sh 落地且可执行；frontend/tests/e2e/ 占位 + README 落地</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG 标 Phase 1 完成 + 修 pyproject.toml e2e marker</name>
  <files>backend/pyproject.toml, CHANGELOG.md</files>
  <action>
    1. 编辑 backend/pyproject.toml 在 [tool.pytest.ini_options] 段加：
       ```toml
       [tool.pytest.ini_options]
       asyncio_mode = "auto"
       asyncio_default_fixture_loop_scope = "session"
       testpaths = ["tests"]
       markers = [
           "e2e: 需要真后端容器，默认 skip（OFFBOARDING_E2E_BASE_URL 环境变量启用）",
       ]
       addopts = "-m 'not e2e'"
       ```
       注意：如果 Plan 01 已经写了 [tool.pytest.ini_options] 段，本 Task 用 Edit 工具合并 marker + addopts 字段，不要重复写整段。
    2. 更新 CHANGELOG.md `[Unreleased] / Added` 段最后追加：
       - "2026-05-16 — Phase 1 Plan 07：E2E 测试骨架（backend/tests/e2e/ Phase 1 冒烟用例 + 演示模式 11 场景占位清单 + frontend/tests/e2e/ Phase 5 占位 + scripts/dev_up.sh & smoke_test.sh 手动冒烟 + pyproject.toml e2e marker 默认 skip）"
    3. 在 `[Unreleased]` 段下方新增 `### Phase 1 Complete` 子段：
       ```markdown
       ### Phase 1 Complete (2026-05-16)
       
       **交付**：
       - ✓ uv 项目骨架 + 完整 v1 依赖（langgraph 1.2 / fastapi 0.136 / sqlalchemy 2 async / psycopg 3 / asyncpg / structlog）
       - ✓ pre-commit hooks（gitleaks + ruff + mypy + check-yaml）
       - ✓ Docker Compose 编排（postgres 16-alpine 5433 / redis 7-alpine 6380 / flow-api 8000）
       - ✓ 多阶段 Dockerfile（uv builder + python 3.12-slim-bookworm runtime）
       - ✓ entrypoint.sh（alembic upgrade head → checkpointer.setup → uvicorn）
       - ✓ 双 schema 隔离（app + langgraph）+ alembic include_object 过滤 langgraph
       - ✓ 6 张业务表 schema（flow_instances / node_states / action_logs / users / notifications / notification_outbox）+ Repository 层（含 upsert 幂等）
       - ✓ LangGraph 引擎骨架（OffboardingState TypedDict + Annotated reducer + AsyncPostgresSaver + 2 节点 + dynamic interrupt + Command resume）
       - ✓ FastAPI 应用（lifespan + envelope + exception handler + /api/health）
       - ✓ 4 个业务 API 端点（POST /api/flows + GET /api/flows/{id} + GET /api/flows/{id}/nodes + POST /api/flows/{id}/nodes/{nid}/actions）
       - ✓ 业务表与 LangGraph checkpoint 双写规范最小版（业务事务 commit → graph.ainvoke）
       - ✓ 30+ 测试用例（含 InMemorySaver 测 graph 流转 / asgi-lifespan + httpx 测 API / 重复推进 409 / 跨流程 ID 400 / checkpoint 恢复）
       - ✓ E2E 骨架 + 手动冒烟脚本（scripts/dev_up.sh + scripts/smoke_test.sh）
       - ✓ CHANGELOG 全程更新
       
       **下一步**：
       - **Phase 2**（双写规范完整化 + 节点函数完整化 + 申请人最终确认节点）— 把 manager_review 后的 8 节点 + 5 并行节点 + applicant_final_confirm 全部落地
       - 或者：先在 192.168.2.44 上跑 `bash scripts/dev_up.sh && bash scripts/smoke_test.sh` 做一次部署冒烟
       
       **Phase 1 不做的（已推到对应 phase）**：
       - 鉴权 / 深链 JWT → Phase 3
       - 邮件 / Mattermost / GLM 摘要 → Phase 4
       - 前端 Next.js → Phase 5
       - nginx / 超时扫描 / 完整部署 → Phase 6
       ```
  </action>
  <verify>
    <automated>grep -q "Phase 1 Complete" /Users/admin/ai/resume/interview/liuxin/hr/CHANGELOG.md && grep -q "addopts.*not e2e" /Users/admin/ai/resume/interview/liuxin/hr/backend/pyproject.toml && cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>CHANGELOG 含 "Phase 1 Complete" 子段；pyproject.toml e2e marker + addopts 配置；pytest --collect-only 默认排除 e2e 不报错</done>
</task>

</tasks>

<verification>
1. backend/tests/e2e/ 目录 3 文件落地
2. frontend/tests/e2e/ 占位 + README 落地
3. scripts/dev_up.sh + scripts/smoke_test.sh 落地且可执行
4. pyproject.toml e2e marker 配置
5. CHANGELOG 含 Phase 1 Complete 子段
6. pytest 默认排除 e2e marker
</verification>

<success_criteria>
- Phase 1 完成后跑 `bash scripts/dev_up.sh && bash scripts/smoke_test.sh` 在 44 上能一键演示通过
- E2E 测试骨架已就绪，Phase 2+ 可在 backend/tests/e2e/ 追加场景
- frontend/tests/e2e/ 目录骨架就绪，Phase 5 可直接放 Playwright spec
- CHANGELOG 完整记录 Phase 1 交付清单 + 下一步
</success_criteria>

<output>
完成后写 `.planning/phases/01-langgraph-schema/01-07-SUMMARY.md`
</output>
