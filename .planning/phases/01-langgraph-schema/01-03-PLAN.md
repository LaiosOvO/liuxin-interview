---
phase: 01-langgraph-schema
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/alembic.ini
  - backend/migrations/env.py
  - backend/migrations/script.py.mako
  - backend/migrations/versions/0001_create_app_schema_tables.py
  - backend/src/offboarding_flow/state_store/__init__.py
  - backend/src/offboarding_flow/state_store/enums.py
  - backend/src/offboarding_flow/state_store/models.py
  - backend/src/offboarding_flow/state_store/session.py
  - backend/src/offboarding_flow/state_store/repositories.py
  - backend/tests/test_state_store.py
  - CHANGELOG.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-05
must_haves:
  truths:
    - "alembic upgrade head 在测试数据库上创建 6 张业务表（flow_instances / node_states / action_logs / users / notifications / notification_outbox），全部在 app schema"
    - "alembic env.py 的 include_object 过滤 schema == 'langgraph'，防 autogenerate 误删 LangGraph 表（PITFALLS #1）"
    - "所有表 id 用 uuid（PG gen_random_uuid()）默认值（PITFALLS #17 防自增并行冲突）"
    - "node_states.status 枚举（pending / waiting_human / in_review / done / rejected / returned）"
    - "notification_outbox 表预留 UNIQUE(flow_id, node_state_id, channel) 约束（Phase 4 用）"
    - "Repository 提供 flow_repo / node_repo / action_repo 三个对象 + upsert 幂等操作"
    - "pytest backend/tests/test_state_store.py 通过：能创建 flow + node + action + 查询，且重复 upsert 不报错"
  artifacts:
    - backend/alembic.ini
    - backend/migrations/env.py（含 include_object 过滤）
    - backend/migrations/versions/0001_create_app_schema_tables.py
    - backend/src/offboarding_flow/state_store/models.py
    - backend/src/offboarding_flow/state_store/enums.py
    - backend/src/offboarding_flow/state_store/session.py
    - backend/src/offboarding_flow/state_store/repositories.py
    - backend/tests/test_state_store.py
  key_links:
    - "session.py 通过 sessionmaker(expire_on_commit=False) + async_sessionmaker；DSN 锁 search_path 到 app（CONTEXT §2）"
    - "alembic env.py include_object 必须返回 False if schema == 'langgraph'"
    - "models.py Base 用 SQLAlchemy 2.x DeclarativeBase + MappedColumn 风格"
    - "所有 TIMESTAMP 列加 timezone=True（CONTEXT §7）"
---

<objective>
建立业务表 schema + Alembic 异步 migration + Repository 层：在 `app` schema 下落 6 张表（flow_instances / node_states / action_logs / users / notifications / notification_outbox），alembic env.py 通过 include_object 过滤掉 `langgraph` schema 防 autogenerate 误删；session.py 封装 async engine + sessionmaker；repositories.py 提供 upsert 幂等接口。

Purpose: 这是 Phase 2 双写规范的依赖前置。Phase 1 提供 schema 完整骨架（6 张表全建），但只有 3-4 张表（flow_instances / node_states / action_logs / users）在 Phase 1 实际写入；notifications / notification_outbox 建表但留给 Phase 4。

Output: 可运行的 alembic + 业务表 + Repository CRUD（含 upsert）+ 单元测试。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-langgraph-schema/01-CONTEXT.md
@PRD.md
@.planning/research/SUMMARY.md
@backend/pyproject.toml

<interfaces>
<!-- 业务表 schema 来自 PRD §6.1 + CONTEXT §7。Phase 1 完整建表，部分表 Phase 4 才写入。 -->

# flow_instances（流程实例）
- id: UUID PK (gen_random_uuid)
- employee_id: VARCHAR(64) NOT NULL  -- 离职员工 username（PRD §6 关联 users.username）
- template: VARCHAR(64) NOT NULL DEFAULT 'standard_offboarding'
- status: VARCHAR(32) NOT NULL DEFAULT 'in_progress'  -- in_progress / completed / cancelled / rejected
- context: JSONB NOT NULL DEFAULT '{}'::jsonb
- started_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- completed_at: TIMESTAMPTZ NULL
- created_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- updated_at: TIMESTAMPTZ NOT NULL DEFAULT now()

# node_states（节点状态）
- id: UUID PK
- flow_id: UUID NOT NULL REFERENCES flow_instances(id) ON DELETE CASCADE
- node_name: VARCHAR(64) NOT NULL  -- apply / manager_review / hr_initial / ...
- node_title: VARCHAR(255) NOT NULL  -- 中文标题
- status: VARCHAR(32) NOT NULL  -- pending / waiting_human / in_review / done / rejected / returned
- assignee: VARCHAR(64) NULL  -- 责任人 username
- result_text: TEXT NULL  -- 决策时填写的自由文本
- is_overdue: BOOLEAN NOT NULL DEFAULT false
- entered_at: TIMESTAMPTZ NULL
- completed_at: TIMESTAMPTZ NULL
- created_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- updated_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- UNIQUE(flow_id, node_name)   # 单流程内节点名唯一 → 支持 upsert

# action_logs（动作日志 append-only）
- id: UUID PK
- flow_id: UUID NOT NULL REFERENCES flow_instances(id) ON DELETE CASCADE
- node_state_id: UUID NULL REFERENCES node_states(id) ON DELETE CASCADE
- actor: VARCHAR(64) NOT NULL  -- 谁触发的（username 或 system:auto）
- action: VARCHAR(32) NOT NULL  -- advance / return / reject / restart / system
- result_text: TEXT NULL
- status: VARCHAR(32) NOT NULL DEFAULT 'success'  -- success / failed / retrying
- error_message: TEXT NULL
- payload: JSONB NULL  -- 额外 metadata
- created_at: TIMESTAMPTZ NOT NULL DEFAULT now()

# users（最小：username / email / role）
- id: UUID PK
- username: VARCHAR(64) UNIQUE NOT NULL
- email: VARCHAR(255) NOT NULL
- role: VARCHAR(32) NOT NULL  -- applicant / manager / hr / it_admin / finance / legal / kb_owner / archivist / admin
- display_name: VARCHAR(128) NULL
- department: VARCHAR(64) NULL
- manager_email: VARCHAR(255) NULL
- created_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- updated_at: TIMESTAMPTZ NOT NULL DEFAULT now()

# notifications（通知发送记录 — Phase 4 才写入，Phase 1 仅建表）
- id: UUID PK
- flow_id: UUID NOT NULL REFERENCES flow_instances(id) ON DELETE CASCADE
- node_state_id: UUID NULL REFERENCES node_states(id) ON DELETE CASCADE
- channel: VARCHAR(32) NOT NULL  -- email / mattermost
- recipient: VARCHAR(255) NOT NULL
- subject: VARCHAR(512) NULL
- body_summary: TEXT NULL
- status: VARCHAR(32) NOT NULL  -- pending / sent / failed
- retry_count: INTEGER NOT NULL DEFAULT 0
- error_message: TEXT NULL
- sent_at: TIMESTAMPTZ NULL
- created_at: TIMESTAMPTZ NOT NULL DEFAULT now()

# notification_outbox（待发送通知队列 — Phase 4 才写入，Phase 1 仅建表）
- id: UUID PK
- flow_id: UUID NOT NULL REFERENCES flow_instances(id) ON DELETE CASCADE
- node_state_id: UUID NULL REFERENCES node_states(id) ON DELETE CASCADE
- channel: VARCHAR(32) NOT NULL
- recipient: VARCHAR(255) NOT NULL
- payload: JSONB NOT NULL  -- {subject, body_html, role_label, ...}
- status: VARCHAR(32) NOT NULL DEFAULT 'pending'  -- pending / processing / sent / failed
- attempts: INTEGER NOT NULL DEFAULT 0
- next_attempt_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- last_error: TEXT NULL
- created_at: TIMESTAMPTZ NOT NULL DEFAULT now()
- UNIQUE(flow_id, node_state_id, channel)   -- Phase 4 outbox 幂等保护

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQLAlchemy 2.x ORM 模型 + enums + session 工厂</name>
  <files>backend/src/offboarding_flow/state_store/__init__.py, backend/src/offboarding_flow/state_store/enums.py, backend/src/offboarding_flow/state_store/models.py, backend/src/offboarding_flow/state_store/session.py</files>
  <action>
    1. 写 `state_store/__init__.py`：仅 `from .models import Base, FlowInstance, NodeState, ActionLog, User, Notification, NotificationOutbox; from .session import get_engine, get_sessionmaker, init_db; from .enums import FlowStatus, NodeStatus, ActionType, ActionStatus, NotificationChannel, NotificationStatus`
    2. 写 `state_store/enums.py`：用 `str, Enum` 定义所有枚举（FlowStatus / NodeStatus / ActionType / ActionStatus / NotificationChannel / NotificationStatus / Role）
       ```python
       from enum import StrEnum
       
       class FlowStatus(StrEnum):
           IN_PROGRESS = "in_progress"
           COMPLETED = "completed"
           CANCELLED = "cancelled"
           REJECTED = "rejected"
       
       class NodeStatus(StrEnum):
           PENDING = "pending"
           WAITING_HUMAN = "waiting_human"
           IN_REVIEW = "in_review"
           DONE = "done"
           REJECTED = "rejected"
           RETURNED = "returned"
       
       class ActionType(StrEnum):
           ADVANCE = "advance"
           RETURN = "return"
           REJECT = "reject"
           RESTART = "restart"
           SYSTEM = "system"
       
       # ... 其余按 [interfaces] 列出
       ```
    3. 写 `state_store/models.py`：使用 SQLAlchemy 2.x DeclarativeBase + Mapped/mapped_column 风格，按 [interfaces] 字段定义 6 个表；Base.metadata.schema = "app" 不写在 Base，而是每个表 `__table_args__ = {"schema": "app"}`；UUID 列用 `Mapped[uuid.UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, server_default=text("gen_random_uuid()"))`；TIMESTAMPTZ 用 `DateTime(timezone=True), server_default=func.now()`；UNIQUE 约束写 `__table_args__ = (UniqueConstraint("flow_id", "node_name", name="uq_node_states_flow_node"), {"schema": "app"})`
    4. 写 `state_store/session.py`：
       ```python
       """异步 DB session 工厂 + engine 单例。"""
       from typing import AsyncGenerator
       from sqlalchemy.ext.asyncio import AsyncEngine, AsyncSession, async_sessionmaker, create_async_engine
       from offboarding_flow.config import get_settings
       
       _engine: AsyncEngine | None = None
       _sessionmaker: async_sessionmaker[AsyncSession] | None = None
       
       def get_engine() -> AsyncEngine:
           global _engine
           if _engine is None:
               settings = get_settings()
               # 注意：asyncpg DSN 在 url 中写 options 不支持，改用 connect_args
               _engine = create_async_engine(
                   settings.postgres_dsn,
                   pool_size=5,
                   max_overflow=15,
                   pool_pre_ping=True,
                   connect_args={"server_settings": {"search_path": "app,public"}},
               )
           return _engine
       
       def get_sessionmaker() -> async_sessionmaker[AsyncSession]:
           global _sessionmaker
           if _sessionmaker is None:
               _sessionmaker = async_sessionmaker(get_engine(), expire_on_commit=False)
           return _sessionmaker
       
       async def get_session() -> AsyncGenerator[AsyncSession, None]:
           """FastAPI Depends 入口。"""
           async with get_sessionmaker()() as session:
               yield session
       
       async def init_db() -> None:
           """启动时调用：确认 engine 可连。"""
           engine = get_engine()
           async with engine.connect() as conn:
               await conn.execute(text("SELECT 1"))
       
       async def dispose_engine() -> None:
           global _engine, _sessionmaker
           if _engine is not None:
               await _engine.dispose()
               _engine = None
               _sessionmaker = None
       ```
       说明：config.get_settings() 在 Plan 05 实现，本 Plan 编写时可暂时 import 占位。
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run python -c "from offboarding_flow.state_store import Base, FlowInstance, NodeState, ActionLog, User, Notification, NotificationOutbox; tables = sorted(Base.metadata.tables.keys()); print('tables:', tables); assert len(tables) == 6, f'Expected 6 tables, got {len(tables)}'"</automated>
  </verify>
  <done>models.py 含 6 个表 + 所有 UUID/TIMESTAMPTZ/JSONB/UNIQUE 约束；可 import 不报错；Base.metadata.tables 长度为 6；表名都带 app schema 前缀</done>
</task>

<task type="auto">
  <name>Task 2: Alembic 异步配置 + 第一个 migration（创建 6 张表 + include_object 过滤）</name>
  <files>backend/alembic.ini, backend/migrations/env.py, backend/migrations/script.py.mako, backend/migrations/versions/0001_create_app_schema_tables.py</files>
  <action>
    1. 写 `backend/alembic.ini`：
       ```ini
       [alembic]
       script_location = migrations
       prepend_sys_path = src
       file_template = %%(rev)s_%%(slug)s
       version_path_separator = os
       sqlalchemy.url = driver://user:pass@localhost/dbname  # 占位，env.py 会用 settings 覆盖
       
       [post_write_hooks]
       hooks = ruff
       ruff.type = console_scripts
       ruff.entrypoint = ruff
       ruff.options = format REVISION_SCRIPT_FILENAME
       
       [loggers]
       keys = root,sqlalchemy,alembic
       [handlers]
       keys = console
       [formatters]
       keys = generic
       [logger_root]
       level = WARN
       handlers = console
       qualname =
       [logger_sqlalchemy]
       level = WARN
       handlers =
       qualname = sqlalchemy.engine
       [logger_alembic]
       level = INFO
       handlers =
       qualname = alembic
       [handler_console]
       class = StreamHandler
       args = (sys.stderr,)
       level = NOTSET
       formatter = generic
       [formatter_generic]
       format = %(levelname)-5.5s [%(name)s] %(message)s
       datefmt = %H:%M:%S
       ```
    2. 写 `backend/migrations/env.py`（异步 + include_object 过滤 + 用 settings）：
       ```python
       """Alembic env：异步 + 过滤 langgraph schema + 用 Settings 读取 DSN。"""
       import asyncio
       from logging.config import fileConfig
       
       from sqlalchemy import pool
       from sqlalchemy.engine import Connection
       from sqlalchemy.ext.asyncio import async_engine_from_config
       from alembic import context
       
       from offboarding_flow.config import get_settings
       from offboarding_flow.state_store.models import Base
       
       config = context.config
       
       if config.config_file_name is not None:
           fileConfig(config.config_file_name)
       
       target_metadata = Base.metadata
       
       
       def include_object(object, name, type_, reflected, compare_to):
           """关键：过滤 langgraph schema 的所有对象。
           
           PITFALLS #1: alembic autogenerate 会扫到 LangGraph 的 checkpoints 等表，
           如果不过滤会被误删（因为我们的 metadata 不知道这些表）。
           """
           if type_ == "table":
               schema = getattr(object, "schema", None)
               if schema == "langgraph":
                   return False
               if schema and schema.startswith("langgraph"):  # langgraph / langgraph_test
                   return False
           return True
       
       
       def include_schemas(name):
           """只 reflect app 相关 schema，跳过 langgraph / 系统 schema。"""
           return name in ("app", "app_test")
       
       
       def run_migrations_offline() -> None:
           settings = get_settings()
           url = settings.postgres_dsn_sync  # offline 用 sync URL
           context.configure(
               url=url,
               target_metadata=target_metadata,
               literal_binds=True,
               include_object=include_object,
               include_schemas=True,
               version_table_schema="app",
           )
           with context.begin_transaction():
               context.run_migrations()
       
       
       def do_run_migrations(connection: Connection) -> None:
           context.configure(
               connection=connection,
               target_metadata=target_metadata,
               include_object=include_object,
               include_schemas=True,
               version_table_schema="app",
           )
           with context.begin_transaction():
               context.run_migrations()
       
       
       async def run_async_migrations() -> None:
           settings = get_settings()
           # 用 settings.postgres_dsn (asyncpg DSN)
           configuration = config.get_section(config.config_ini_section, {})
           configuration["sqlalchemy.url"] = settings.postgres_dsn
           connectable = async_engine_from_config(
               configuration,
               prefix="sqlalchemy.",
               poolclass=pool.NullPool,
           )
           async with connectable.connect() as connection:
               await connection.run_sync(do_run_migrations)
           await connectable.dispose()
       
       
       def run_migrations_online() -> None:
           asyncio.run(run_async_migrations())
       
       
       if context.is_offline_mode():
           run_migrations_offline()
       else:
           run_migrations_online()
       ```
    3. 写 `backend/migrations/script.py.mako`：标准 alembic 默认模板（可直接 cp 自 alembic init 输出）
    4. 写 `backend/migrations/versions/0001_create_app_schema_tables.py`：
       - revision = "0001"
       - down_revision = None
       - 在 upgrade() 里：op.execute("CREATE SCHEMA IF NOT EXISTS app") + op.create_table(...) × 6 张（按 [interfaces] 字段，schema="app"），包含 UNIQUE 约束 / FK 约束 / 默认值
       - 在 downgrade() 里：op.drop_table(...) × 6 reverse order + op.execute("DROP SCHEMA IF EXISTS app CASCADE")
       - 注意：app schema 由 init-db.sql 在 docker 启动时已建，但本 migration 用 `CREATE SCHEMA IF NOT EXISTS` 兼容本地直接 alembic upgrade head（无 docker）的情况
    5. **本 Task 不真跑 alembic upgrade head**（需要数据库在线 — 留 Plan 07 冒烟） — 但要校验 alembic check 能离线读取 script 文件
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run alembic check 2>&1 | head -3 || cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run python -c "from alembic.script import ScriptDirectory; from alembic.config import Config; cfg = Config('alembic.ini'); script = ScriptDirectory.from_config(cfg); revs = list(script.walk_revisions()); print(f'revisions: {[r.revision for r in revs]}'); assert len(revs) == 1, 'expected 1 migration'"</automated>
  </verify>
  <done>alembic.ini + env.py + script.py.mako + versions/0001_*.py 落地；env.py 含 include_object 过滤 langgraph schema；migration 0001 含 6 张表 + UNIQUE 约束；alembic script directory 能读到 0001（无需在线 DB）</done>
</task>

<task type="auto">
  <name>Task 3: Repository 层（upsert 幂等）+ 单元测试</name>
  <files>backend/src/offboarding_flow/state_store/repositories.py, backend/tests/test_state_store.py</files>
  <action>
    1. 写 `state_store/repositories.py`：提供 3 个 Repository 类（FlowRepository / NodeRepository / ActionRepository）+ 1 个 UserRepository。每个 repo 接收 `AsyncSession` 注入，提供 CRUD + 上层服务需要的 upsert：
       ```python
       """业务表 Repository 层（双写规范的"业务侧"承担方）。
       
       关键约定：
       - 所有 INSERT/UPDATE 用 upsert（PG ON CONFLICT），防 LangGraph interrupt 重跑节点函数时重复写入（PITFALLS #16, CONTEXT §3.4）
       - 所有方法返回 ORM 实例 — 不返回 dict（保留 type 信息）
       """
       from __future__ import annotations
       
       import uuid
       from typing import Any
       
       from sqlalchemy import select, update
       from sqlalchemy.dialects.postgresql import insert as pg_insert
       from sqlalchemy.ext.asyncio import AsyncSession
       
       from .enums import ActionStatus, ActionType, FlowStatus, NodeStatus
       from .models import ActionLog, FlowInstance, NodeState, User
       
       
       class FlowRepository:
           def __init__(self, session: AsyncSession) -> None:
               self.session = session
       
           async def create(self, employee_id: str, template: str = "standard_offboarding", context: dict | None = None) -> FlowInstance:
               flow = FlowInstance(
                   employee_id=employee_id,
                   template=template,
                   status=FlowStatus.IN_PROGRESS.value,
                   context=context or {},
               )
               self.session.add(flow)
               await self.session.flush()
               return flow
       
           async def get(self, flow_id: uuid.UUID) -> FlowInstance | None:
               return await self.session.get(FlowInstance, flow_id)
       
           async def list_all(self, limit: int = 50) -> list[FlowInstance]:
               stmt = select(FlowInstance).order_by(FlowInstance.started_at.desc()).limit(limit)
               result = await self.session.execute(stmt)
               return list(result.scalars().all())
       
           async def mark_completed(self, flow_id: uuid.UUID) -> None:
               stmt = update(FlowInstance).where(FlowInstance.id == flow_id).values(status=FlowStatus.COMPLETED.value, completed_at=func.now())
               await self.session.execute(stmt)
       
       
       class NodeRepository:
           def __init__(self, session: AsyncSession) -> None:
               self.session = session
       
           async def upsert(
               self,
               flow_id: uuid.UUID,
               node_name: str,
               node_title: str,
               status: NodeStatus,
               assignee: str | None = None,
           ) -> NodeState:
               """幂等创建 / 更新节点状态。
               
               PITFALLS #16: LangGraph interrupt 重跑节点函数时，本方法被多次调用必须不报错。
               用 PG ON CONFLICT (flow_id, node_name) 实现。
               """
               stmt = pg_insert(NodeState).values(
                   flow_id=flow_id,
                   node_name=node_name,
                   node_title=node_title,
                   status=status.value,
                   assignee=assignee,
                   entered_at=func.now(),
               ).on_conflict_do_update(
                   index_elements=["flow_id", "node_name"],
                   set_={
                       "status": status.value,
                       "assignee": assignee,
                       "updated_at": func.now(),
                   },
               ).returning(NodeState)
               result = await self.session.execute(stmt)
               return result.scalar_one()
       
           async def get(self, node_id: uuid.UUID) -> NodeState | None:
               return await self.session.get(NodeState, node_id)
       
           async def list_by_flow(self, flow_id: uuid.UUID) -> list[NodeState]:
               stmt = select(NodeState).where(NodeState.flow_id == flow_id).order_by(NodeState.entered_at)
               result = await self.session.execute(stmt)
               return list(result.scalars().all())
       
           async def complete(self, node_id: uuid.UUID, result_text: str, new_status: NodeStatus = NodeStatus.DONE) -> NodeState | None:
               stmt = update(NodeState).where(NodeState.id == node_id).values(
                   status=new_status.value,
                   result_text=result_text,
                   completed_at=func.now(),
                   updated_at=func.now(),
               ).returning(NodeState)
               result = await self.session.execute(stmt)
               return result.scalar_one_or_none()
       
       
       class ActionRepository:
           def __init__(self, session: AsyncSession) -> None:
               self.session = session
       
           async def create(
               self,
               flow_id: uuid.UUID,
               actor: str,
               action: ActionType,
               node_state_id: uuid.UUID | None = None,
               result_text: str | None = None,
               status: ActionStatus = ActionStatus.SUCCESS,
               payload: dict | None = None,
           ) -> ActionLog:
               log = ActionLog(
                   flow_id=flow_id,
                   node_state_id=node_state_id,
                   actor=actor,
                   action=action.value,
                   result_text=result_text,
                   status=status.value,
                   payload=payload,
               )
               self.session.add(log)
               await self.session.flush()
               return log
       
           async def list_by_flow(self, flow_id: uuid.UUID) -> list[ActionLog]:
               stmt = select(ActionLog).where(ActionLog.flow_id == flow_id).order_by(ActionLog.created_at)
               result = await self.session.execute(stmt)
               return list(result.scalars().all())
       
       
       class UserRepository:
           def __init__(self, session: AsyncSession) -> None:
               self.session = session
       
           async def upsert(self, username: str, email: str, role: str, display_name: str | None = None) -> User:
               stmt = pg_insert(User).values(
                   username=username, email=email, role=role, display_name=display_name,
               ).on_conflict_do_update(
                   index_elements=["username"],
                   set_={"email": email, "role": role, "display_name": display_name, "updated_at": func.now()},
               ).returning(User)
               result = await self.session.execute(stmt)
               return result.scalar_one()
       
           async def get_by_username(self, username: str) -> User | None:
               stmt = select(User).where(User.username == username)
               result = await self.session.execute(stmt)
               return result.scalar_one_or_none()
       ```
       记得在文件顶部 `from sqlalchemy import func`。
    2. 写 `backend/tests/test_state_store.py`：使用 polyfactory 生成测试数据 + asyncpg 跑测试，但**Phase 1 测试不需要真数据库**（CI 环境可能没 PG）。改用 SQLite-in-memory + 跳过 ON CONFLICT 测试，或者用 testcontainers-postgres。
       **决策**：Phase 1 测试只做"模型可实例化 + 方法签名正确 + repository import 不报错"，不要求真数据库连接。真集成测试留 Plan 07 全流程冒烟。
       ```python
       """Phase 1 Repository 单元测试 — 不依赖真数据库。
       
       目标：验证 ORM 模型可实例化、Repository 方法签名正确、enum 值映射正确。
       真数据库集成测试见 Plan 07 全流程冒烟。
       """
       import uuid
       
       import pytest
       
       from offboarding_flow.state_store.enums import (
           ActionStatus,
           ActionType,
           FlowStatus,
           NodeStatus,
       )
       from offboarding_flow.state_store.models import (
           ActionLog,
           FlowInstance,
           NodeState,
           User,
       )
       from offboarding_flow.state_store.repositories import (
           ActionRepository,
           FlowRepository,
           NodeRepository,
           UserRepository,
       )
       
       
       def test_flow_instance_can_be_constructed():
           flow = FlowInstance(employee_id="zhang.san", template="standard_offboarding")
           assert flow.employee_id == "zhang.san"
           assert flow.template == "standard_offboarding"
       
       
       def test_node_state_can_be_constructed():
           flow_id = uuid.uuid4()
           node = NodeState(
               flow_id=flow_id,
               node_name="manager_review",
               node_title="上级审批",
               status=NodeStatus.WAITING_HUMAN.value,
           )
           assert node.node_name == "manager_review"
           assert node.status == "waiting_human"
       
       
       def test_action_log_can_be_constructed():
           log = ActionLog(
               flow_id=uuid.uuid4(),
               actor="li.si",
               action=ActionType.ADVANCE.value,
               status=ActionStatus.SUCCESS.value,
           )
           assert log.actor == "li.si"
           assert log.action == "advance"
       
       
       def test_user_can_be_constructed():
           user = User(username="zhang.san", email="zhang.san@example.com", role="applicant")
           assert user.username == "zhang.san"
           assert user.role == "applicant"
       
       
       def test_flow_status_enum_values():
           assert FlowStatus.IN_PROGRESS.value == "in_progress"
           assert FlowStatus.COMPLETED.value == "completed"
           assert FlowStatus.REJECTED.value == "rejected"
       
       
       def test_node_status_enum_values():
           assert NodeStatus.PENDING.value == "pending"
           assert NodeStatus.WAITING_HUMAN.value == "waiting_human"
           assert NodeStatus.DONE.value == "done"
           assert NodeStatus.REJECTED.value == "rejected"
           assert NodeStatus.RETURNED.value == "returned"
       
       
       def test_action_type_enum_values():
           assert ActionType.ADVANCE.value == "advance"
           assert ActionType.RETURN.value == "return"
           assert ActionType.REJECT.value == "reject"
       
       
       def test_repository_classes_have_expected_methods():
           # 验证 method 存在（mypy 不会报错）—— 实际调用留集成测试
           assert hasattr(FlowRepository, "create")
           assert hasattr(FlowRepository, "get")
           assert hasattr(FlowRepository, "list_all")
           assert hasattr(NodeRepository, "upsert")
           assert hasattr(NodeRepository, "complete")
           assert hasattr(ActionRepository, "create")
           assert hasattr(UserRepository, "upsert")
       ```
    3. 更新 CHANGELOG.md `[Unreleased] / Added`：
       - "2026-05-16 — Phase 1 Plan 03：业务表 schema（6 张表：flow_instances / node_states / action_logs / users / notifications / notification_outbox） + Alembic 异步 migration（include_object 过滤 langgraph schema 防 PITFALLS #1） + Repository 层（含 upsert 幂等，为 Phase 2 双写规范 + interrupt 重跑幂等做铺垫）"
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/hr/backend && uv run pytest tests/test_state_store.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>repositories.py 落地 + 4 个 Repository 类齐备；tests/test_state_store.py 所有用例通过；CHANGELOG 更新</done>
</task>

</tasks>

<verification>
1. backend/src/offboarding_flow/state_store/ 目录含 __init__.py / enums.py / models.py / session.py / repositories.py
2. backend/migrations/env.py 含 include_object 过滤 langgraph schema
3. backend/migrations/versions/0001_*.py 创建 6 张 app schema 表
4. pytest tests/test_state_store.py 通过（8+ 个用例）
5. alembic script directory 可读 0001（无需在线 DB）
6. CHANGELOG 更新
</verification>

<success_criteria>
- 6 张业务表的 SQLAlchemy 2.x ORM 模型 + Alembic migration 完整
- include_object 过滤防止 alembic 误删 langgraph schema（PITFALLS #1 防范）
- Repository 提供 upsert 幂等接口（为 Phase 2 节点函数双写做铺垫）
- 单元测试覆盖模型实例化 + enum 值 + repo 方法签名（真数据库集成测试留 Plan 07）
</success_criteria>

<output>
完成后写 `.planning/phases/01-langgraph-schema/01-03-SUMMARY.md`
</output>
